---
review_id: review-2026-05-20-16
generated_at: 2026-05-20T09:25:00Z
feedback_items_reviewed: []   # 0 open this tick
---

## Patterns observed

- **Quiet tick on protocol-feedback.** Zero open entries; the funnel has been clean for three consecutive reviews (#14 closed f-005, #15 zero, this one zero). Sub-B is structurally homogeneous and agents are no longer surfacing new protocol-level complaints. Healthy signal, not silence to worry about.

- **Sub-B has cleared all primary-priority questions; pending is now 37/37 `low`.** open-questions.json status counts: `answered: 82, pending: 37, duplicate: 1`. The pending-priority breakdown is `{ low: 37 }` — zero primary remain. The fallback rule at LOOP_PROMPT.md:296 (`If none, fall back to priority == "low" pending`) is exercised for the first time. No code change needed — the protocol handles the transition correctly — but reviewers should know future windows will mix lower-stakes investigations, so the "Phase 2 carry-in density" metric reported in #11-#15 will naturally drop.

- **Investigated_at timestamp drift — 45/82 followups (55%) use the placeholder `2026-05-20T00:00:00Z`.** Spot check of the 10 followups produced in this review window (q-097, q-099, q-107, q-110, q-111, q-112, q-114, q-115, q-117, q-119): exactly five carry the placeholder. Pattern: agents read the brief's literal `<ISO>` template at LOOP_PROMPT.md:313 and emit a midnight-of-the-day timestamp rather than an actual investigation time. Impact is low (orchestrator records the real timestamp in open-questions.json's `completed_at`), but the file-level `investigated_at` is unreliable for any post-hoc analysis. This has been present across the entire Sub-B run and was never flagged because the orchestrator's resumption logic doesn't read it. Worth a one-line clarification in the brief.

- **Followup quality remains high in the four cited recent runs (q-012, q-017, q-023, q-024).** All four have correct four-name frontmatter (no regressions), file:line citations on every claim, real `## Related findings` payload, and confidence statements that explain themselves. q-024 produces an immediately-actionable code fix (stale doc path in `TickRegistry.ts:71` warning string). q-023 correctly scopes a "is the 1 ms window wide enough?" question to current code without inventing future hazards. q-017 traces a load-bearing `composeFrom` gradient path through three files. q-012 catches a stale doc comment in `Tutorial.tsx:30-32`. No quality-regression signal.

- **No protocol drift detected in this window's brief outputs.** Frontmatter shape clean across all 10 samples; no scalar `lines_read`, no flow-syntax YAML, no `id:`/`parent:`/`status:`/`verdict:`/`files_read:` resurfacing. Edit 1 from review #13 (self-check sentence) continues to hold.

## Proposed LOOP_PROMPT.md edits

### Edit 1: Disambiguate `<ISO>` placeholder for `investigated_at`

- **Where:** Section "Sub-phase B — Resolve", paragraph at LOOP_PROMPT.md:307 (frontmatter template block).
- **Current text:** `> investigated_at: <ISO>`
- **Proposed text:** `> investigated_at: <ISO — use the actual UTC timestamp when you started the investigation, not a midnight placeholder like 2026-05-20T00:00:00Z>`
- **Why:** 45/82 followups (55%) and 5/10 in this window emit `2026-05-20T00:00:00Z`. The literal `<ISO>` template reads as "any ISO-shaped string" to roughly half the runs. Tightening the placeholder text reorients agents toward a real timestamp. Drift observed in recent runs: q-024, q-023, q-017, q-110, q-114, q-115, q-117, q-119 all carry the placeholder in this window.
- **Risk:** Minimal. The string only appears in the example block; the orchestrator's resumption logic and open-questions.json bookkeeping don't depend on this field. Worst case: agents keep producing the placeholder and we revisit next tick.

## Feedback items to mark wont-fix

None — no open items.

## Skip list — feedback that should remain open

None — no open items.
