---
review_id: review-2026-05-20-8
generated_at: 2026-05-20T00:42:00Z
feedback_items_reviewed: []
iterations_window: [t01-fluid-toy, t02-fractal-toy, phase-1.5-sub-a, q-001, q-002]
---

## Patterns observed

- **Phase 1 transitioned into Phase 1.5 cleanly and the watchlist item from review #7 did not materialise.** The two secondary surveys (t01-fluid-toy 80 files, t02-fractal-toy 14 files) completed without any new frontmatter-shape variant — Edit 1 from review #4 plus the audit brief's stabilised wording held against two different code-style boundaries, which closes out the parallel-brief-drift concern from review #6 / #7. Sub-phase A also ran one-shot as designed: `open-questions.json` now exists with 120 raw entries (76 primary, 43 low, 1 duplicate, 119 pending — the math holds: 76+43+1=120, with q-001 already answered = 119 pending). That's outside the 30-80 expected band on the *raw* side (review #6/#7 projected 50-70 after triage); the triage agent appears to have been generous about leaving questions as `primary` rather than demoting to `low`. Not a problem — see Notes for the human for the implication.
- **q-001 and q-002 are both exceptionally high-quality follow-up resolutions.** Both are `confidence: high`, both consulted exactly 3 files (well inside the ≤8 budget), both produced four-paragraph answers with file:line citations on every claim. q-001 traces a four-stage boot chain across three files and nails the README's ambiguity ("step 7 with setTimeout" — correct *where* the setTimeout lives but wrong about *what triggers boot*). q-002 disambiguates `isHydrated` vs `isReady` vs `_isSceneReady` (three "ready"-ish flags), names the contract documentation gap, and surfaces a silent-failure mode (30s timeout at `GmtRendererTickDriver.tsx:90-94` leaves the splash stuck with no error UI). Both produced 3-4 Related findings each — exactly the kind of preservable signal Phase 2 writers will want. **The Sub-phase B brief is working as designed.** No protocol issue to flag from these two — agents understood the brief, stayed in budget, returned PASS, and surfaced no protocol complaints.
- **Sub-phase B's workload is structurally homogeneous in a way Phase 1 was not.** Phase 1 had three action types (audit/create, summarize-existing) across 37 subsystems of varying file-counts (2 → 80), code styles (engine TS, React UI, GMT-tier, secondary apps), and shapes (live code vs existing docs). Each batch boundary was a potential failure point — which is exactly why the iterations-≥5 cadence earned its keep through reviews #4 (Edits 1+2), #6 (inline summarize-existing tighten), #7 (Edit 1 positive-example block). Sub-phase B by contrast is 117 remaining iterations of the same agent brief against the same kind of input (one open question, ≤8 file budget, fixed output shape). q-001 and q-002 demonstrate the brief is correctly speced. **This is the diagnostic case for cadence extension.** A review every 5 iterations here means ~23 review ticks ahead for a workload where the brief has been validated and no new variance is expected.
- **No `protocol-feedback.json` entries opened in this window** (all four entries remain in their terminal states from reviews #1 and #3: f-001/f-002 `wont-fix`, f-003/f-004 `addressed`). The orchestrator-level "catch shape errors inline" pattern continues to dominate over the feedback-pipeline pattern — agents complete cleanly, orchestrator notices anomalies and patches briefs, no formal feedback entry filed. For Sub-phase B's homogeneous workload this is also expected: the brief is small, well-scoped, and the orchestrator's anomaly-detection is the most likely route for any future issue.

## Proposed LOOP_PROMPT.md edits

**Edit 1 — Extend the iterations-≥5 review cadence to ≥10 *during Sub-phase B only*.**

- **Where:** Section 7, "Decide: normal iteration or self-review?", the bulleted trigger conditions paragraph (LOOP_PROMPT.md lines 172-174).
- **Current text:**
  > Before scheduling the next wakeup, check whether a **self-review tick** is due. A review tick is due if EITHER:
  > - The number of normal (non-review) iterations since the last review is ≥ 5, OR
  > - The number of `status: "open"` entries in `protocol-feedback.json` is ≥ 3.
- **Proposed text:**
  > Before scheduling the next wakeup, check whether a **self-review tick** is due. A review tick is due if EITHER:
  > - The number of normal (non-review) iterations since the last review is ≥ 5 (or ≥ 10 if the loop is currently in Phase 1.5 Sub-phase B — see below), OR
  > - The number of `status: "open"` entries in `protocol-feedback.json` is ≥ 3.
  >
  > **Sub-phase-B cadence relaxation:** Sub-phase B (resolve open questions, one per iteration) is structurally homogeneous — same brief, same input shape, same output shape, every iteration. After q-001 and q-002 both ran clean (review #8), the iterations-≥5 trigger is overserving for this sub-phase. While `open-questions.json` exists AND any of its entries have `status: "pending"` (i.e. Sub-phase B is the active mode), the cadence relaxes to ≥10. The open-feedback-≥3 trigger is unchanged — any genuine protocol issue still surfaces immediately. The global iterations-≥5 trigger applies everywhere else (Phase 1, Phase 1.5 Sub-phase A, Phase 2 when authored).
- **Why:** Addresses the cadence-extension authorisation surfaced in review #6 ("If reviews #6 and #7 also propose only clarifications or nothing, the iterations-≥5 cadence could safely extend") and refined in review #7 (which earned its #5 tick by proposing Edit 1, but explicitly noted Sub-phase B would be the right venue to revisit the cadence question). With 117 pending Sub-phase B iterations, ≥5 = ~23 review ticks vs ≥10 = ~12 ticks — ~11 review ticks saved at ~5-7 minutes each = ~55-75 minutes of overnight wall-clock recovered. The open-feedback-≥3 backstop preserves emergency-pull-cord semantics: if a Sub-phase B agent flags a real protocol issue, a tick still fires within three iterations regardless of the iterations counter. Does not change resumption logic, iteration step structure, or priority/sort order — only the cadence numeric threshold, conditioned on a phase predicate already visible from `open-questions.json` state.
- **Risk:** Low-moderate. The downside scenario: a subtle Sub-phase B issue accumulates over 6-9 iterations before a review tick catches it, vs ~3-4 under the ≥5 cadence. Mitigating factors: (a) agent briefs explicitly invite protocol feedback outside their PASS/FAIL report, which routes through the open-feedback-≥3 trigger that's *unchanged*; (b) the orchestrator-level inline-catch pattern (g04, d03, d08) has caught every prior shape error within a single iteration — no actual "drift accumulates for many iterations" failure has occurred yet; (c) the homogeneity argument is empirical now (q-001, q-002 both ran cleanly with the same brief); (d) the predicate is conditional and scoped — Phase 2's cadence stays at ≥5 by default when its instructions are authored.

## Feedback items to mark wont-fix

(none — `protocol-feedback.json` shows all four entries in terminal state from prior reviews: f-001/f-002 `wont-fix`, f-003/f-004 `addressed`. No new entries opened this window. q-001 and q-002 agents flagged no protocol issues outside their PASS reports.)

## Feedback items to keep open

(none — no open feedback at tick start.)

## Notes for the human

- **Sub-phase A's triage was generous about `primary`.** Counts: 120 raw, 76 primary, 43 low, 1 duplicate. Review #6/#7 projected 50-70 primary after triage; we landed at 76, ~10% above the upper bound. Reading q-001 and q-002, the resolutions are genuinely useful and deserve to be in the primary tier — so the triage agent's calibration looks fine, not lax. But it means Sub-phase B wall-clock at ~3-4 min/iteration × 76 primary = ~3.8-5 hours, plus optional 43 low at the same rate = up to another ~2.5 hours if all are run. The 200-iteration hard cap (LOOP_PROMPT.md line 343) is safe; the overnight schedule should plan for ~4-7 hours of Sub-phase B wall-clock before Phase 2 kickoff is feasible.
- **Edit 1 above is the cadence-extension proposal authorised by review #6** ("a Sub-phase-specific tweak, not a structural change to triggering logic"). It is scoped (Sub-phase B only), conditional (predicate on `open-questions.json` existence + pending entries), and reversible (a one-line edit can restore ≥5 if Sub-phase B turns out to be less homogeneous than predicted). It does not touch the open-feedback-≥3 backstop, resumption logic, iteration step structure, or priority sort. If the human prefers a more conservative step (e.g. ≥7 instead of ≥10) the same edit shape works with a different number.
- **q-001 and q-002 surfaced multiple cross-cuts useful for Phase 2 module-doc writers**: (a) the `_isSceneReady` dead parameter on `useAppStartup` (`hooks/useAppStartup.ts:53`) flagged in BOTH followups independently; (b) the `isHydrated` misnomer (q-002 Related finding 1) is a renaming candidate touching only LoadingScreen; (c) the silent 30s timeout in `GmtRendererTickDriver.tsx:90-94` is a UX bug worth surfacing to the maintainer (not Phase 2 doc scope strictly, but worth a one-line in Notes-for-the-human when the a01-boot-shell module doc gets written). The follow-up docs are doing exactly the institutional-knowledge-capture job Phase 1.5 was designed for.
- **Phase 1 final tally for the record:** 37 subsystems all in terminal status (35 survey_done/summarized + 0 failed + 2 secondary marked complete). Coverage manifest gained t01-fluid-toy (80 files) and t02-fractal-toy (14 files) entries in this window. Phase 1 wall-clock total per progress.json: 19:56:29Z (a01 start) → 00:19:27Z (t02 done) = ~4.5 hours. That is well inside the "overnight loop" target.
- **For the next review (#9 — whenever it fires under whichever cadence)**: watch for (a) whether Sub-phase B agents start exceeding the ≤8 file-read budget (would surface as `>40k tokens` cost-guard hits per LOOP_PROMPT.md line 343); (b) whether any question in the worklist proves genuinely unresolvable and the `unresolvable` disposition lands cleanly; (c) whether `_followups/` blob_sha entries on `files_consulted` introduce a new shape-variance class (the followup frontmatter has its own micro-contract that hasn't been stress-tested across many iterations yet — though q-001 and q-002 both used it correctly).
