---
review_id: review-2026-05-20-5
generated_at: 2026-05-20T00:45:00Z
feedback_items_reviewed: []
iterations_window: [g05-engine-gmt-features, g06-bucket-render, g07-mesh-export, g08-save-load-gmf, g09-modular-graph]
---

## Patterns observed

- **Edit 1 from review #4 is holding — 100% canonical prefix compliance in the post-edit window.** Across g05-g09, every orphan-sweep nomination uses the exact `Orphan-sweep candidate: <path>` form (capital O, full prefix, path-first). Counts in this window: g05 = 9, g06 = 5, g07 = 5, g08 = 4, g09 = 2; **25 canonical nominations, zero lowercase variants**. Compared with g04 (4 lowercase) and g02 (1 lowercase) before the edit, the discipline shift is clean. The strict wording change had immediate effect — no example block needed. Repository-wide totals now stand at **29 canonical** vs **5 lowercase** (all 5 from pre-edit g02 + g04).
- **Tier A/B/C strategy validated by g05 — the largest engine-gmt subsystem so far.** g05 had 29 file entries and used an inline `tier:` key per entry rather than a side note (4 A, 9 B, 16 C). Tier A files all show full `[1, N]` reads; Tier B files all show full reads (lighting/index.ts at 486 lines, geometry/index.ts at 281 lines, etc.); 13 of 16 Tier C files were read in full but **3 Tier C entries are explicitly `lines_read: [0, 0]` partial coverage** — `engine-gmt/features/coloring/MappingModes.ts`, `engine-gmt/features/geometry/transforms.ts`, `engine-gmt/features/geometry/types.ts`. These are honest skips (the agent reported them via the read_range honesty rule rather than fabricating coverage). For the survey at 217 lines with 5 top-level sections and clean drift table, the strategy reads as load-bearing and is worth memorializing as the recommended approach for any future subsystem >20 files.
- **Doc-drift severity in engine-gmt continues to outstrip engine-core, as flagged in review #4 — and the gap is widening.** g07-mesh-export's drift table cites stale claims in `30_Mesh_Export_Prototype.md` (CPU SDF path described as primary; reality is GPU-only Newton); g08-save-load-gmf finds that `05_Data_and_Export.md` doesn't yet reflect the engine-gmt fork's `FormulaFormat.ts` move + `installSceneIO` indirection; g09-modular-graph notes `03_Modular_System.md` is silent on `GraphCompiler.updateModularUniforms` and the runtime/compile split. g06 has no existing doc and g05 cites only `dev/HANDOFF.md` (not a doc-tier reference). The pattern is consistent enough that Phase 2 engine-gmt writers will be doing largely from-scratch composition, not edit-against-baseline. No protocol change — just a planner signal that's been compounding.
- **Adjacent-subsystem cross-references are surfacing through orphan-sweep nominations.** g05's nominations include foreign module callouts (`engine/features/post_effects`, `engine/features/color_grading`, etc. — engine-core re-registered into the GMT registry by module identity) and `engine/SDFShaderBuilder.ts` flagged as a cross-subsystem invariant (must mirror interlace + cutting-plane preamble changes). These aren't classic "file in wrong subsystem" orphans — they're cross-subsystem coupling notes. The current brief wording accommodates them ("a one-line reason they belong here") but the post-Phase-1 orphan-sweep design will need to triage path-nomination-orphans separately from cross-subsystem-coupling notes. Not a brief change for now; flagged for the planner.
- **Survey body sizes are shrinking even as file counts vary.** Surveys range 88-217 lines in this window (mean ~146) vs the prior window's 153-700+ range with g03's 56-file survey. The trend reflects subsystem-size variance rather than thinning rigor (g05 at 217 lines for 29 files is information-dense; g09 at 88 lines for 2 files is correct sizing). No quality regression. Drift tables remain present and explicit on every survey that has an existing_doc_ref.

## Proposed LOOP_PROMPT.md edits

(none — Edit 1 from review #4 closed the canonical-prefix gap. No new patterns require brief changes this tick.)

## Feedback items to mark wont-fix

(none — `protocol-feedback.json` shows all four entries in terminal state. Nothing new opened in this window.)

## Feedback items to keep open

(none — no open feedback entries at tick start.)

## Notes for the human

- **Phase 1 progress: 25/37 subsystems survey_done (~68%), with 22 of them in the engine and gmt tiers.** Remaining 12 entries: predominantly app-gmt/, secondary tier. At current ~3-4 min/iteration pace, Phase 1 completes in ~50-70 more minutes of loop wall-clock. **Phase 1.5 prep is warranted now** — see next bullet.
- **Phase 1.5 pre-flight items to surface to the human:**
  1. **Open-questions worklist size estimate.** A quick grep across 25 surveys for `## Open questions` body content shows the section is populated on every survey with typical 2-6 numbered entries. Projected total: ~80-120 raw questions, ~50-80 after de-duplication. That's within the 200-iteration hard cap and inside the 30-80 expected band — no design change needed.
  2. **Orphan-sweep worklist is now ~34 entries (29 canonical + 5 lowercase variants).** Projected at Phase 1 end: ~45-60 total. Still tractable for a single Phase 1.6 consolidation pass per target subsystem. The cross-subsystem-coupling sub-class noted above (e.g. g05's foreign-module nominations) is ~6 entries and may warrant a separate triage bucket — design call for Phase 1.6 kickoff, not now.
  3. **Aspirational doc rewrite-vs-migrate decisions are now four+ stacked** (review #4 listed three: f-001/e06, e07, e13; this window adds at minimum `30_Mesh_Export_Prototype.md` from g07 and `03_Modular_System.md` gaps from g09, both of which produced drift tables with break-level severity rows). A single planner session before Phase 2 to make these calls is worth more than letting writers re-litigate them per module doc.
- **Cosmetic `audited_at` drift continues** — g05/g06/g08/g09 use `T00:00:00Z` date-midnight, g07 uses `T22:31:00Z` near-wall-clock. Still not worth a fix; progress.json holds canonical timestamps.
- **Review-tick cadence note.** Tick #5 fired on the iterations-≥5 trigger again (zero open feedback this entire window since review #4). The open-feedback-≥3 trigger has not yet fired naturally. If reviews #6 and #7 also propose only clarifications or nothing, the iterations-≥5 cadence could safely extend — but that's a load-bearing trigger-logic change and out of scope per constraints. Worth a human eye after one more clean review.
- **For the next review (#6), watch:** whether app-gmt surveys re-introduce lowercase orphan-sweep variants (different agent batch — Edit 1 might need a reminder mid-Phase 1) and whether Tier C `[0, 0]` partial-coverage entries grow beyond the g05 baseline of 3 (signal of survey budget pressure on app-gmt's larger surface area).
