---
review_id: review-2026-05-20-17
generated_at: 2026-05-20T09:42:00Z
feedback_items_reviewed: [f-006]
---

## Patterns observed

- **f-006 is an orchestrator-behaviour bug, not a protocol gap.** LOOP_PROMPT.md Section 8 Sub-phase B (LOOP_PROMPT.md:299-336) defines the per-question brief template, and a grep for "sibling", "prior question", "context from", "prior investigation" against the prompt returns zero hits. The "context from sibling investigation q-030" sentence the q-031 investigator pushed back on was freelance text the orchestrator added to that one brief — not a documented step. Writing the pattern (even guarded with "tentative") into the protocol would be expanding the spec to legitimize a behaviour that should just stop.

- **The sibling-context generalisation was the failure mode, not the handoff itself.** q-030 found `Uniforms.CamType` is contributed via `params.<key>.uniform`, and noted explicitly in its Related findings that this is "not a separate extraUniforms array" — but that was a true statement scoped to CamType (`engine-gmt/features/optics.ts:23` is a `params.*.uniform` descriptor; no extraUniforms entry for it). The orchestrator dropped the scope when forwarding the context to q-031, turning a CamType-specific finding into a general claim. The q-031 agent then had to re-verify against `engine-gmt/features/lighting/index.ts:125-144` and `engine-gmt/features/core_math.ts:131` to confirm `extraUniforms` is actually the dominant path for the larger set of uniforms in question.

- **The loop self-corrected.** q-031's frontmatter is clean, file:line citations on every claim, and its Related findings section opens with "Sibling investigation q-030 was incomplete" — explicitly flagging the gap for a Phase 2 writer. So the protocol's "every claim cites file:line" rule (LOOP_PROMPT.md:325) caught the bad inherited framing the moment the investigator tried to ground it in code. The output is correct, the investigator paid a re-verification tax, and the cost is bounded by the existing citation rule. No process gate failed.

- **Quiet on everything else this window.** Of the spot-checked recent followups (q-051, q-050, q-049, q-048, q-047, q-044, q-039, q-035, q-031, q-030), all carry the four-name frontmatter cleanly, file:line citations are present, and `## Related findings` sections are substantive. q-051 produced an actionable file-move suggestion (`AdaptiveResolutionBadge.tsx` from `viewport/` to `topbar/`); q-044 corrected a misleading subsystem name. The `investigated_at` placeholder issue from review #16 (Edit 1) is still showing up in followups produced before that edit landed — q-031 and q-030 both carry `2026-05-20T11:42:00Z`/`11:47:00Z` which look like the model substituting a plausible-but-fictional time. Edit 1 has only had ~17 minutes to propagate; too early to evaluate.

- **No new protocol-shaped agent complaints.** f-006 was raised by the orchestrator against itself (self-reflection on q-031's pushback), not by a sub-agent. The feedback funnel from investigators remains clean — fourth consecutive review with zero externally-raised open items.

## Proposed LOOP_PROMPT.md edits

No proposed edits this tick — f-006 is about an ad-hoc pattern the orchestrator should stop performing, not a missing rule in the protocol. The protocol does not currently endorse sibling-context handoff in Sub-phase B (Section 8 / LOOP_PROMPT.md:299-336 has no mention of it), and codifying it (even with "tentative" guards) would expand the spec to legitimize a behaviour we want to discourage. The existing rule "every claim cites file:line" (LOOP_PROMPT.md:325) already provides the structural safety net that caught the bad framing in q-031.

## Feedback items to mark wont-fix

- **f-006** — orchestrator's ad-hoc "context from sibling investigation q-XXX" prefix is not part of the documented Sub-phase B brief (Section 8); it was freelanced into q-031 only. Resolution is behavioural, not structural: orchestrator should stop adding sibling-context prefixes to investigator briefs. Each question should stand on its own `target_hint` plus the investigator's own reading. If a prior followup is genuinely load-bearing, the orchestrator can surface it as a `target_hint` file path (e.g. point at `survey/_followups/q-030.md` so the investigator reads the source verbatim rather than the orchestrator's paraphrase). Logging this decision in review-log.md is sufficient; no LOOP_PROMPT edit needed.

## Feedback items to mark addressed

None.

## Skip list — feedback that should remain open

None — the only open item (f-006) is being closed wont-fix per above.
