---
question_id: q-108
source_subsystem: g04-navigation
investigated_at: 2026-05-20T10:09:57Z
files_consulted:
  - path: engine-gmt/navigation/usePhysicsProbe.ts
    blob_sha: a6a9759c031e96c858193ae02466620dc54566c1
  - path: data/constants.ts
    blob_sha: 86dc451925607f471981295c08b1ecf262032a4d
---

## Question
> `processDepthData` smoothing constants (`prev * 1.5` threshold, `0.08` blend) are inline magic numbers — could move to `data/constants` alongside `MAX_SKY_DISTANCE` if any tuning surface is added later.

## Answer
Confirmed. The two literals live at `engine-gmt/navigation/usePhysicsProbe.ts:67` (the `prev * 1.5` threshold) and `:69` (the `0.08` blend factor) inside the asymmetric-smoothing branch of `processDepthData`. Lines 64, 68 and 71 carry inline comments that explain the intent ("slow ramp-up prevents speed explosion, instant decrease for safety", "Large increase — blend at ~8% per frame (~60 frames to converge)", "Decrease: instant snap"), so the *meaning* is documented in code — they are not unannotated magic numbers. `MAX_SKY_DISTANCE` is defined at `data/constants.ts:26` (`export const MAX_SKY_DISTANCE = 50.0;`) and is already imported by `usePhysicsProbe.ts` further down, so the proposed colocation target exists and the import wiring is already in place.

Verdict: **trivial**. The recommendation is correct in spirit — if a tuning surface (UI slider, persisted setting, or per-formula override) is ever added, the constants should be promoted to `data/constants.ts` with names like `DEPTH_SMOOTHING_RAMP_THRESHOLD` and `DEPTH_SMOOTHING_BLEND_RATE`. Until then, the existing inline comments give them adequate semantic weight, and moving them now is pure code-cleanup without functional payoff. Agree with the recommendation as a conditional ("if a tuning surface is added"), no action required today.

## Confidence
high — both literals located with surrounding comments visible; target file `data/constants.ts` confirmed to exist and already host `MAX_SKY_DISTANCE`; same file is already imported by `usePhysicsProbe.ts`.

## Related findings
None.
