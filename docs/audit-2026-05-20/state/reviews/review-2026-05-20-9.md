---
review_id: review-2026-05-20-9
generated_at: 2026-05-20T01:25:00Z
feedback_items_reviewed: []
iterations_window: [q-001, q-002, q-003, q-005, q-006, q-007, q-008, q-009, q-011, q-013, q-014, q-015]
---

## Patterns observed

- **Sub-phase-B cadence relaxation (review #8 Edit 1) earned its keep.** 10 question resolutions ran between reviews #8 and #9 with zero protocol-feedback entries opened — empirically validating the homogeneity argument. Wall-clock for the window: q-001 start 00:34:58Z → q-015 end 01:23:06Z = ~48 min for 10 iterations including spawn/orchestration. That's ~4.8 min/iter — consistent with the ~3-4 min/iter projection. If the cadence had stayed at ≥5, two review ticks would have fired in this window for net-zero diagnostic value; the ≥10 cadence reclaimed ~10-14 min of wall-clock and one full agent-context spend.
- **Frontmatter shape variance in followups is REAL and recurring** — but the orchestrator's permissive path-extractor handles it. The window contains two clear deviants:
  - **q-003**: completely off-spec frontmatter (`id` / `parent_survey` / `target` / `status` / `verdict` — none of the contracted field names). No `files_consulted:` block at all.
  - **q-008**: contracted top-level fields BUT `files_consulted:` is a bare path list (no `path:`/`blob_sha:` subkeys per entry).
  - The remaining 10 (q-001, q-002, q-005, q-006, q-007, q-009, q-011, q-013, q-014, q-015) used the canonical block-YAML shape correctly.
  - **Orchestrator-side remediation working as advertised:** coverage.yaml shows `agent_run_id: q-003-followup` (3 entries) and `q-008-followup` (3 entries) — i.e. the path-extractor synthesised entries from disk despite the malformed frontmatter. SHAs were generated server-side rather than read from the doc. Net: no coverage gap; followups still landed; PASS status preserved.
  - **Counter-argument against tightening the brief:** the contract for q-003's shape was already non-spec from review #8's window (q-001/q-002 were canonical, so q-003 — the *first* deviant — landed in this window with no prior signal). Tightening the brief now would be solving a problem the orchestrator already absorbs. The cost of one in-place coverage-extraction fallback per ~10 iterations is lower than the cost of a stricter brief that might cascade into agent retries.
- **Question quality is genuinely useful for Phase 2 writers, not shallow.** Spot-checks of q-013 (DDFS auto-wiring across 4 sites: PresetLogic, AnimationEngine, binderRegistry, historySlice) and q-014 (boot-time registrars distinguishing featureRegistry vs presetFieldRegistry from the e01 four-file scope) both produce summary tables with per-site file:line citations, name the load-bearing string conventions (`set${FeatureId}`, `${featureId}.${paramKey}_<axis>`), and explicitly flag what the Phase 2 module doc should cross-reference. q-009 (boot-time plugin registration ordering) and q-011 (engine-gmt vs app-gmt boundary for formula registration) similarly surface cross-cuts that the source survey could not see from its bounded file set. These are exactly the "institutional knowledge that didn't fit in one subsystem's scope" Phase 1.5 was designed to capture.
- **Cost guard: no agent burn >40k tokens visibly.** File-consultation counts: q-001=3, q-002=3, q-003=3, q-005=4, q-006=2, q-007=2, q-008=3, q-009=6, q-011=7, q-013=9, q-014=5, q-015=6. q-013 is the only entry that exceeded the ≤8 file budget (9 files), but it produced a high-value cross-subsystem answer and its output is 67 lines — within healthy bounds. Followup doc lengths range 35-96 lines (mean ~56) — agents are staying tight, not padding. No iteration exceeded ~5 min wall-clock.
- **Cadence validity check: nothing went undetected.** The two shape deviants (q-003, q-008) were both absorbed inline by the orchestrator within their own iterations — under either the ≥5 or ≥10 cadence, this review (the first since the relaxation) would have observed the same pattern. No accumulating-drift failure occurred. The ≥10 cadence held for its first test cycle.

## Proposed LOOP_PROMPT.md edits

(none — the loop is healthy. Review #8's Edit 1 is doing its job; the followup-frontmatter variance is handled at the orchestrator layer where it belongs, and the permissive path-extractor was the right tool for the job. Tightening the agent brief to enforce frontmatter shape would be premature: shape variance shows up in ~2/10 iterations and is benign — the path-extractor recovers full coverage data. A stricter brief would risk retry loops without proportionate gain.)

## Feedback items to mark wont-fix

(none — `protocol-feedback.json` still shows all four entries in terminal state from prior reviews: f-001/f-002 `wont-fix`, f-003/f-004 `addressed`. No new entries opened this window.)

## Feedback items to keep open

(none — no open feedback at tick start.)

## Notes for the human

- **Sub-phase B is grinding cleanly.** 12 of 76 primary questions resolved (15.8%). At ~4.8 min/iteration the remaining 64 primary entries project to ~5 hours of wall-clock; if the 43 low-priority entries are also worked, add another ~3.4 hours. The 200-iteration hard cap is comfortable. The next review tick (#10) will fire after q-025 or thereabouts (10 more iterations, allowing for a few more skipped IDs due to duplicates).
- **Followup-frontmatter shape variance: the orchestrator's permissive extractor is the load-bearing safety net.** If anyone ever changes the orchestrator's path-extraction regex, retest against q-003 (totally off-spec keys, no `files_consulted:` block) and q-008 (canonical keys but bare path list as `files_consulted:` value) — these are the two real-world variants observed so far. Both files at `plans/doc-audit-state/survey/_followups/{q-003,q-008}.md`.
- **q-013 went 1 over the ≤8 file budget (9 files consulted).** Worth tolerating because the answer cross-cuts 4 subsystems (`PresetLogic.ts`, `AnimationEngine.ts`, `binderRegistry.ts`, `historySlice.ts`) plus the in-scope e01 trio plus the canonical track-binding convention header — genuine breadth, not sloppy reads. If this becomes a *pattern* (≥3 questions in a window exceeding the budget), revisit the budget or split such questions into two follow-ups. Single instance is fine.
- **Watchlist items carried from review #8 (status check):**
  - (a) Token-burn >40k: **not observed.** All iterations completed in single-digit minutes with single-digit file reads.
  - (b) `unresolvable` disposition: **not yet tested in Sub-phase B.** Every question this window answered PASS at high or medium confidence. The cost-guard path remains untouched.
  - (c) `_followups/` blob_sha shape-variance: **yes, observed and absorbed** (q-003 / q-008 as above). The orchestrator-side fallback is doing the work; the agent-side brief was not tightened (correctly, per the counter-argument above).
- **Preservable Phase-2 signal worth surfacing now (cross-followup):**
  - The `set${FeatureId}` action-naming convention (q-013) and the `${featureId}.${paramKey}_<axis>` track-id convention (q-013) are both load-bearing string contracts not enforced by types. Phase 2's `02_Feature_Registry.md` or successor must call these out explicitly.
  - The two-registry boot pattern (`featureRegistry` + `presetFieldRegistry`, both frozen post-population) from q-014 is currently undocumented except in registrar source comments. Phase 2 should pull this into the feature-system module doc.
  - Both items came from outside the e01 four-file scope and would have stayed invisible without Sub-phase B. This is the institutional-knowledge yield the phase was designed for.

PASS.
