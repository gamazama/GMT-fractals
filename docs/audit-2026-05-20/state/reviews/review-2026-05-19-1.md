---
review_id: review-2026-05-19-1
generated_at: 2026-05-19T20:25:00Z
feedback_items_reviewed: [f-001, f-002]
iterations_window: [a01-boot-shell, a02-panels-layout, a03-tutorial, e01-feature-system, e02-tick-registry]
---

## Patterns observed

- **Two distinct feedback shapes are being merged into one bucket.** f-001 is doc-vs-code drift severe enough to be a planner-level call ("rewrite vs treat as migration target"). f-002 is a pure code bug (FeatureSystem.ts jsdoc at :443-447 contradicts impl at :587-592). The current protocol funnels both through the same `protocol-feedback.json`, but only f-001 is actually about *protocol*. f-002 is signal the loop is correctly extracting (good) but has no destination — the survey already records it under "Drift from existing doc" or "Invariants and gotchas", and protocol-feedback is the wrong home.
- **Surveys are landing cleanly.** All 5 completed iterations (a01, a02, a03, e01, e02) are `survey_done`, file counts look sane (2-15 files), and the one survey I spot-checked (e01) has tight file:line citations throughout. Iteration wall-clock is 2-4 minutes per subsystem — well under the 25-minute guard. No FAILs, no >3-agent runs, no malformed frontmatter. The loop mechanics are working.
- **`audited_at` timestamps are date-only (`2026-05-19T00:00:00Z`) rather than the actual ISO time.** Both e01 and e02 surveys show this. Minor, but the brief says `<ISO>` and agents are interpolating midnight. Not worth a fix on its own.
- **Existing-doc drift severity will recur.** e01's 02_Feature_Registry.md is described as aspirational with almost nothing matching code. Several other primary subsystems have `existing_doc_ref` set (e02, e06, e07, e08, e09, e12, e13, g01, g02, g06, g07, g08, g09, a02). Expect more f-001-shaped notes. The current "Drift from existing doc" table in the survey shape handles this fine — what's missing is a severity escalation path when the table would have 20+ rows or every row is `break`.

## Proposed LOOP_PROMPT.md edits

### Edit 1 — Clarify scope of protocol-feedback vs code/doc drift

- **Where:** Section 6 "Capture protocol feedback", first paragraph
- **Current text:**
```
Survey agents are explicitly asked to surface protocol issues OUTSIDE their PASS/FAIL report. If the agent emitted any such note, append it to `plans/doc-audit-state/protocol-feedback.json` (create if missing) with shape:
```
- **Proposed text:**
```
Survey agents are explicitly asked to surface protocol issues OUTSIDE their PASS/FAIL report — meaning issues with the audit loop itself (brief wording, output shape, missing fields, ambiguous instructions). Code bugs and doc drift discovered during a survey are NOT protocol feedback — they belong in the survey's own "Invariants and gotchas" or "Drift from existing doc" sections and will be picked up by Phase 2 writers. If the agent emitted a protocol-shaped note, append it to `plans/doc-audit-state/protocol-feedback.json` (create if missing) with shape:
```
- **Why:** addresses f-002 directly (a code bug, not a protocol issue, was filed as protocol feedback). Reduces noise so the review tick can focus on actual loop improvements.
- **Risk:** agents may under-report borderline cases (e.g. "the brief says cite file:line but the file has no line numbers in generated code"). The "missing fields, ambiguous instructions" examples are intended to keep that channel open. If an agent suppresses a genuinely protocol issue because of this scope tightening, we lose the signal until the next review tick — but the same is true today.

### Edit 2 — Add a doc-drift severity escalation hint

- **Where:** Section 3 "Action: audit or create", the survey shape block, the "Drift from existing doc" line
- **Current text:**
```
> ## Drift from existing doc (if existing_doc_ref is set)
> <three-column table: Doc claim | Current code | Severity (info/warn/break)>
```
- **Proposed text:**
```
> ## Drift from existing doc (if existing_doc_ref is set)
> <three-column table: Doc claim | Current code | Severity (info/warn/break)>
> If the table would have >10 rows OR every row is `break`, add a one-line "Recommendation: rewrite | migrate-target | minor-edits" at the end of the table so the Phase 2 writer has a planner-level steer.
```
- **Why:** addresses f-001 — the e01 survey flagged "aspirational design doc rather than out-of-date reference" but the survey shape gave it nowhere to put that recommendation, so it ended up in protocol-feedback as a question. Giving the surveyor a single-line escalation slot keeps that signal inside the survey where Phase 2 will read it.
- **Risk:** surveyors may overuse the recommendation slot for medium-drift cases, biasing Phase 2 writers toward rewrite when minor edits would do. The threshold ">10 rows OR every row is break" is conservative enough that most subsystems won't trigger it. Worst case: an extra advisory line in a few surveys that Phase 2 can ignore.

## Feedback items to mark wont-fix

- **f-002** — code bug (FeatureSystem.ts:443-447 jsdoc vs :587-592 impl), not a protocol issue. The surveyor already captured the fact in the survey body; Phase 2 writer or a separate code-health pass should pick it up. Closing as out-of-scope for loop protocol. (Edit 1 above clarifies this for future iterations.)

## Feedback items to keep open

- **f-001** — kept open until Edit 2 lands. Once the survey shape supports a "Recommendation" line, this becomes a Phase 2 planner concern (which the human should weigh in on) rather than a loop-protocol concern. After Edit 2 is applied, this can be re-classified as `wont-fix` with reason "addressed by Edit 2 + deferred to Phase 2 planning".

## Notes for the human

- **f-001 is a planner-level call you should make sooner rather than later.** e01-feature-system's existing doc (`dev/docs/engine/02_Feature_Registry.md`) is described as aspirational with almost no code overlap. Two paths: (a) treat the doc as the migration target and let it drive code changes, or (b) rewrite it from the survey. Several other engine-core subsystems (e06, e07, e08, e09, e12, e13) point at older engine/ docs that may have similar drift. A decision here shapes the brief Phase 2 writers will eventually get.
- **Five iterations in, the loop is healthy.** No failures, citation discipline is intact, no scope creep. The two proposed edits are both clarifications, not corrections. If you wanted to extend the review interval (e.g. from "every 5" to "every 8"), the signal supports it — but the open-feedback-≥3 trigger will still fire on noisy days, which is the right safety net.
- **Consider whether code-drift findings (like f-002) need a separate tracking file.** They're real findings that today only live inside one survey markdown. A `plans/doc-audit-state/code-findings.json` would let Phase 2 or a follow-up health pass aggregate them. Not urgent, but if a few more like f-002 appear I'd suggest creating it.
