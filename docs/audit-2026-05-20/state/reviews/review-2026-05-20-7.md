---
review_id: review-2026-05-20-7
generated_at: 2026-05-20T00:05:00Z
feedback_items_reviewed: []
iterations_window: [d05-animation-refactor-1, d06-animation-refactor-2, d07-animation-refactor-3, d08-archive-docs, d09-root-research-specs]
---

## Patterns observed

- **Frontmatter-shape variance is now THREE distinct instances across the docs-existing tier alone: g04 flow-syntax → d03 bare-paths → d08 scalar-lines_read.** Each was a different mis-rendering of the same conceptual contract (`files:` block of `{path, blob_sha, lines_read: [1, N]}` entries). Each was caught by the orchestrator, not by an agent. Each was patched inline by tightening the matching brief. The brief tightening pattern has been: review #4 Edits 1+2 (audit brief), review #6 inline (summarize-existing brief — added bare-paths-rejection wording), d08 inline this window (summarize-existing brief — added `TWO-ELEMENT ARRAY — NOT a scalar like lines_read: N` wording). The current summarize-existing brief at line 99 of LOOP_PROMPT.md now enumerates three explicit failure modes ("bare path lists, flow syntax, missing blob_sha/lines_read, or scalar lines_read all cause zero files recorded"). **Diagnosis**: this is a mix of brief-underspecification AND LLM-variance-against-a-novel-shape. Two pieces of evidence: (a) the audit brief produced one shape error (g04 flow) and the tightened version has held clean for ten subsequent iterations — so tightening DOES work; (b) the three errors are all *different* shapes — no agent repeated a predecessor's mistake — which suggests each agent independently re-interpreted "frontmatter" from priors, and the brief's negative-enumeration approach is a corrective tail-chase. **Recommendation**: a single short positive-example block in the summarize-existing brief would short-circuit the variance. Concretely: paste one valid `files:` block (2-3 entries, real-looking SHAs) directly under the contract sentence so the agent has a concrete shape to mimic instead of synthesizing from prose constraints. This is a doc-the-output-shape edit, NOT a resumption/iteration-step change — within constraints. Proposed below as Edit 1.
- **The docs-existing tier produced exceptional Preservable signal across the whole window.** d03 (covered in review #6) gave the Troubleshooting+CodeHealth institutional dump. d05/d06/d07 captured the animation-refactor research arc — the three-batch sequence reads as a single coherent story of why the animation system was rebuilt (per-scope undo, audio remap, deterministic playback, FPS Keep/Match, 1ms tick dedup). d08 archive-docs preserved the *rationale* layer that current code drops (Fragmentarium importer V1→V2→V3 evolution; Mapping-Mode survey from 2025; Shader Architecture Refactor decisions; GLSL parser integration scope). d09 root-research/specs captured the largest single Preservable bullet density of the whole audit — CHANGELOG_DEV.md's session-by-session shader-perf wins, the fractal_pathtracing_research_2024_2026.md curated paper graph, the FractalShark deep-zoom perturbation architecture, the v3-v4 catalog auto-pick rationale, the dual-useEngineStore problem note. **Strategic implication for Phase 2** (compounding review #6's note): the writer's mandatory pre-read sequence for every module-doc should be `survey/_docs/d0X-*.md` FIRST (rationale), then `survey/<sub>.md` (citations and drift table), then code. Worth memorializing in the Phase 2 brief when it lands. Across d01-d09, the Preservable harvest is ~9 sessions of dense institutional knowledge that would otherwise be irrecoverable from code reading alone.
- **Phase 1 is 34/37 done (92%); 2 secondary iterations remain (t01, t02).** Per the window mechanics — 5 iterations completed this tick, docs-existing tier fully consumed (d01-d09 all summarized). At ~3 min/iteration for the small secondary apps, Phase 1 wall-clock remaining is ~6-8 minutes, plus Phase 1.5 Sub-phase A which is one-shot (gather + triage in a single iteration). **The watchlist item from review #6 about t01/t02 being a different code-style batch boundary remains active** — Edit 1/2 from review #4 (audit-brief tightening) haven't been tested against fluid-toy/fractal-toy yet, so a fourth frontmatter-shape variant is the most likely failure mode this tick predicts. If a fourth variant DOES appear, that's the trigger for restructuring rather than another inline patch — explicitly flagged below in Notes for the human.

## Proposed LOOP_PROMPT.md edits

**Edit 1 — Add a concrete frontmatter example block to the summarize-existing brief.**

- **Where:** Section 3, "Action: `summarize-existing` (existing docs batch)", paragraph starting "Read every existing doc in `<files>` top-to-bottom."
- **Current text:**
  > Read every existing doc in `<files>` top-to-bottom. The frontmatter `files:` block MUST use block YAML with one full entry per file (`path:`, `blob_sha:` from blob-sha.mjs, `lines_read: [1, N]` as a TWO-ELEMENT ARRAY — NOT a scalar like `lines_read: N`). Bare path lists, flow syntax, missing blob_sha/lines_read, or scalar lines_read all cause zero files recorded. For each doc, produce a 3-6 sentence summary capturing: what the doc covers, key decisions or invariants it records, anything that looks stale relative to current code (flag with "MAY BE STALE: …" but don't verify against code in this pass).
- **Proposed text:**
  > Read every existing doc in `<files>` top-to-bottom. The frontmatter `files:` block MUST use block YAML with one full entry per file (`path:`, `blob_sha:` from blob-sha.mjs, `lines_read: [1, N]` as a TWO-ELEMENT ARRAY — NOT a scalar like `lines_read: N`). Bare path lists, flow syntax, missing blob_sha/lines_read, or scalar lines_read all cause zero files recorded. Use this exact shape (replace SHAs and line counts with real values):
  >
  > ```yaml
  > files:
  >   - path: docs/example/alpha.md
  >     blob_sha: 8ad65e438e116f6f0580c3834bb45c1c4d1b7479
  >     lines_read: [1, 988]
  >   - path: docs/example/beta.md
  >     blob_sha: f344a2ccfb9eb0e555526cc1383da3d61a4fd893
  >     lines_read: [1, 1228]
  > ```
  >
  > For each doc, produce a 3-6 sentence summary capturing: what the doc covers, key decisions or invariants it records, anything that looks stale relative to current code (flag with "MAY BE STALE: …" but don't verify against code in this pass).
- **Why:** Three independent shape variants in this brief alone (g04 flow, d03 bare-paths, d08 scalar). Negative enumeration is reactive — each new agent invents a new way to be wrong. A positive example is mimicry-friendly; agents copy shapes well even when they parse constraints imprecisely. Addresses no specific f-NNN entry (the failures were caught and patched without ever being filed as feedback), but addresses the meta-pattern observed across reviews #4, #6, and #7.
- **Risk:** Low. The example block adds ~10 lines to a brief; it doesn't change iteration logic, resumption, or priority sort. Worst case: an agent copies the example SHAs literally and the orchestrator catches it via the SHA-mismatch check on next coverage record. Symmetric tightening of the audit brief is optional — that brief has held clean since review #4 — so leaving it asymmetric for now is fine.

## Feedback items to mark wont-fix

(none — `protocol-feedback.json` has all four entries in terminal state from prior reviews. No new entries opened in this window. The d08 scalar-lines_read bug was patched inline at the orchestrator level and never filed as a feedback entry; same disposition as g04 and d03 before it.)

## Feedback items to keep open

(none — no open feedback at tick start.)

## Notes for the human

This is review #7 — second-to-last of Phase 1. The human asked about Phase 2 readiness. Sorted by what's most likely to need a human decision:

**(a) What the orchestrator has explicitly stated is NOT yet ready for Phase 2:**

1. **Phase 2 brief itself is TBD** (LOOP_PROMPT.md line 336: "Phase 2 instructions are TBD"). The loop literally cannot transition past Phase 1.5 without human-authored Phase 2 instructions added to LOOP_PROMPT.md. This is the single hard blocker.
2. **Aspirational-doc rewrite-vs-migrate decisions are now FIVE+ stacked** (catalogued across reviews #4 and #5 and expanded in #6): `02_Feature_Registry.md` (f-001/e06 — aspirational), `engine/07_*.md` (e07), `engine/13_*.md` (e13), `gmt/30_Mesh_Export_Prototype.md` (g07 — break-severity drift), `gmt/03_Modular_System.md` (g09 — break-severity drift), plus this window's d04 finding that `gmt/30_Mesh_Export_Prototype.md` literally contradicts `gmt/24_*.md` on whether the implementation is "plain HTML + 8 ES2020 scripts" or `mesh-export/gpu/gpu-pipeline.ts` + `mesh-export/store/meshExportStore.ts`. A single planner session resolving rewrite-vs-migrate per doc is more efficient than letting per-module writers re-litigate.
3. **d03's coverage entries lack blob_sha** (caught review #6) — synthesized from disk after the original agent failed to report them. For Phase 2's "did the doc change since survey?" check, those 10 entries should be re-blob-sha'd at Phase 2 start.

**(b) Other gaps spotted from reading prior reviews end-to-end:**

4. **Orphan-sweep worklist has grown to ~34+ entries** (29 canonical + 5 lowercase variants from pre-Edit-1 g02/g04). Phase 1.6 (consolidation per target subsystem) was deferred to post-Phase-1 in review #3. Review #5 surfaced a sub-class of "cross-subsystem coupling notes" (g05's foreign-module nominations) that may need separate triage from path-only orphans. This is a Phase 1.6 design call that's been waiting since review #3 and still doesn't have a designated owner — the human should at minimum acknowledge whether Phase 1.6 runs before, alongside, or after Phase 2 module-doc writing.
5. **Engine-gmt-tier drift is structurally deeper than engine-core-tier drift** (review #5). Phase 2 engine-gmt writers will be doing largely from-scratch composition (not edit-against-baseline). This isn't a blocker but it does mean engine-gmt module docs will take longer per-iteration than engine-core ones — a scheduling/budgeting note.
6. **Open-question worklist projection has firmed up** at ~50-70 primary after triage (review #6, refined from review #5's 50-80 estimate). Sub-phase A is one-shot and well-speced; Sub-phase B at ~3-4 min/iteration is ~3-4 hours of loop wall-clock. Inside cost guards.
7. **Cosmetic `audited_at: T00:00:00Z` midnight-stamp drift** persists across docs-existing batches. progress.json holds canonical timestamps. Not worth a fix; flagged for completeness.
8. **Review cadence**: tick #6 explicitly said "If review #7 also proposes nothing, that's two clean ticks in a row and worth surfacing the cadence-extension to a human deliberately." This review #7 DOES propose an edit (Edit 1 above), so cadence-extension is NOT yet warranted — the iterations-≥5 cadence is paying for itself this tick.

**(c) Recommended sequence of human actions when Phase 1.5 finishes:**

1. **Before authoring Phase 2 brief, hold a one-pass planner session** to resolve item (a)2 (rewrite-vs-migrate per stale doc). Use the matrix from review #6: structural-mismatch=rewrite, paths-only-wrong=migrate-target, aspirational-planning=preserve-with-pointer. Output: a `plans/doc-audit-state/phase-2-disposition.json` mapping each existing doc to its strategy. Writers consume this; no per-module re-litigation.
2. **Decide Phase 1.6 ordering**: orphan-sweep before, alongside, or after module-doc writing. Recommendation: alongside (a writer producing the e09 module doc absorbs e09's orphan-sweep nominations naturally during composition). This avoids a separate sweep phase entirely for ~80% of orphans and only requires a dedicated cleanup pass for cross-subsystem-coupling entries.
3. **Re-blob-sha d03's 10 doc entries** at Phase 2 start (item (a)3). Trivial script run — could even be a one-line addition to the Phase 2 brief's preamble.
4. **Author the Phase 2 brief and update LOOP_PROMPT.md line 336**, replacing "TBD" with concrete iteration shape. Suggested skeleton (drawn from observed Phase 1 patterns): pick next subsystem with `status: survey_done`, spawn writer agent with mandatory pre-read order (d0X summary → survey → code → followups), output `dev/docs/modules/<id>.md`, verify with `verify-doc.mjs` (which DOES exist for Phase 2 per line 133), mark `status: written` then `verified`.
5. **Skim review-log.md and the seven review docs in one sitting** before launching Phase 2 — the cumulative orchestrator-level observations across reviews capture context (drift severity, doc-tier-by-doc-tier strategies, cross-subsystem coupling notes) that no single document holds in one place. ~20 minutes of reading saves multi-hour Phase 2 missteps.

If Edit 1 above is accepted and applied, this review tick is done. If Edit 1 is rejected (e.g. you'd rather not add an example block), the orchestrator should still continue normal work — the inline scalar-lines_read fix has already addressed the immediate d08 failure.
