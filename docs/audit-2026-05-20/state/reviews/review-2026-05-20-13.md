---
review_id: review-2026-05-20-13
generated_at: 2026-05-20T04:32:00Z
feedback_items_reviewed: []
iterations_window: [q-062, q-064, q-065, q-068, q-069, q-070, q-071, q-075, q-077, q-078]
---

## Patterns observed

- **Frontmatter variance regressed — 1/10 deviant after three windows at 0/10.** q-070 reverts to the full pre-Edit-2 shape: `id`, `parent`, `status`, `verdict`, `target_hint`, `files_read`. That is six of the nine explicitly blacklisted field names at LOOP_PROMPT.md:307, present in a single file. The other nine followups in this window are canonical. Review #12's watchlist item (e) flagged "another bare-string `files_consulted`" as the variance to watch for; the actual variance shape is different — a full-old-shape revert, not a single-field-shape drift. The brief's nine-name blacklist is therefore necessary but not sufficient: an agent that pattern-matches on "frontmatter looks roughly like the q-016 era" reproduces the entire old shape rather than the canonical one. Window-by-window deviant count is now `q-001..q-015: 2/10 → q-016..q-028: 0/10 → q-029..q-042: 4-5/10 → q-043..q-061: 0/10 → q-062..q-078: 1/10`. One occurrence in 62 is not a re-tightening trigger by itself — the cleaner brief is still working at ~98% — but the watchlist item (e) condition from review #12 is met (second deviant in the last 20 followups), so this review proposes a small clarifying edit. See edit proposal below.

- **Followup-corrects-followup is a new but bounded pattern.** q-071 explicitly corrects q-068 in a "Correction to q-068" section: q-068 claimed `componentRegistry` is "host-app-owned" and "not a structure owned by FeatureSystem itself"; q-071 verifies the class + singleton actually live at `components/registry/ComponentRegistry.tsx` (engine-owned module), and the host-app owns *what gets registered into it*, not the registry. q-071's correction is well-formed: it cites the specific q-068 claim it's overriding, gives the file path that disproves it, and explains the residual sense in which q-068 was partially correct (FeatureSystem doesn't own it; the engine layer does). This is the **first case where one followup found a real error in another followup**, after 51 followups of followups-correcting-surveys. The brief's verification regime didn't catch it earlier because q-068 wasn't wrong about anything its own files-consulted block could disprove — it was a structural claim about a module it didn't read. q-071's `files_consulted` (which DOES include `components/registry/ComponentRegistry.tsx`) is what made the correction possible. **This is verification working as designed, not a process failure** — the prompt asks every followup to read the files it cites, and when q-071 read the registry file as part of its own answer, the q-068 claim collapsed naturally. No brief edit needed for this specifically; the "followups overrule surveys on facts" contract from review #12 generalises cleanly to "followups overrule followups on facts" with the same per-question file-citation requirement.

- **Phase 2 cumulative carry-in is now substantial — keeping a rough tally is worthwhile.** Counting across the full 52-followup Sub-B run so far:
  - **Production bug surfaces: 2** — q-019 (`DuplicatePresetFieldError` on app-gmt double-register), q-064 (StateLibrary action-name collision is HMR-only, silent-data-loss risk if a second library is added that shares action names; ~6-line fix proposed at q-064:30-34).
  - **New uncovered subsystems: 2** — e09b-state-library-ui (q-061, three generic UI primitives keyed on createStateLibrarySlice), **g11-gmt-camera-manager** (q-062, three files totalling 592 lines including the entire 310-line `engine-gmt/store/cameraSlice.ts`). q-062 is the more consequential of the two — it's a complete subsystem with its own load-order invariant (`installGmtCameraSlice` must run before `CameraManagerPanel` first render, `cameraSlice.ts:20-22`) and a four-way boundary (engine-core via `installStateLibrary`, g05 via empty DDFS stub, g04 as a sibling navigation subsystem, and `engine-gmt/topbar.tsx` for the hand-wired Camera menu).
  - **Doc-rewrite targets: ~30** — including the highest-value q-054 (three-layer SceneIO loader pipeline), q-070 (AnimationParams + offset/base composition contract — five branches in AnimationSystem.tick plus the camera offset-only exception), q-065 (the two-system Camera vs StateLibrary split with the `hideShortcuts: true` coexistence pattern), and the e10 → e01 pointer-back set (q-068, q-071).
  - **Dead-code / dead-comment cleanups: ~15** — including q-053 (`uninstallHelp` never called anywhere), q-056 (inverted comment in `app-gmt/main.tsx:408-414`), q-060 (dead defensiveness guard), q-077 (audio-UI `(store as any)` casts that the 2026-03-13 audit claimed were converted to `@ts-expect-error` but live files show the unconverted casts — doc/code drift in `docs/gmt/07_Code_Health.md:316-326`).
  - **Code drift / staleness findings: ~5** — q-077's 07_Code_Health.md drift, q-075's missing `timeupdate` plumbing (poll-based AudioPanel is load-bearing for play/pause icon truthfulness), q-078's `setEngineProxy` install pattern at `engine-gmt/renderer/install.ts:30+61` being out of g01's scope (likely g06 gap).

  The Phase 2 writer brief will need a structured "carry-in" appendix per subsystem, not just a flat list. Recommend the orchestrator emit a `phase-2-carry-in.json` at Sub-B end indexed by `source_subsystem` → `[{question_id, kind: 'bug'|'subsystem-gap'|'doc-rewrite'|'cleanup'|'drift', summary, target_file}]`. That index walks straight into Phase 2's "module-doc writer reads `_followups/q-NNN.md` for any survey claim that overlaps" contract from review #12. Don't add this to LOOP_PROMPT.md (Sub-B is mid-flight) — it's a Phase 2 prep note. Logged here for the human's attention.

- **Production bug surface rate updates to 2-in-52 = 1-in-26.** Review #12 projected 1-in-25-35; q-064 lands at the tight end of that range. The honest projection is now **1 production bug per ~25 followups**. With 67 pending (24 primary + 43 low), expect **~2-3 more genuine bug finds** before Sub-B end. Phase 2 carry-in for "production bugs" probably tops out at 4-5, not the 5-7 I'd implied a few reviews ago.

- **Cadence at ≥10 — hold, do NOT extend to ≥12.** Three reasons:
  - **(a) Frontmatter variance is back at 1/10.** Holding at ≥10 catches single-window regressions within a review cycle; extending to ≥12 would have meant the q-070 deviation surfaces 2 iterations later, with up to one more deviation potentially absorbed before discovery. Cheap to keep tight.
  - **(b) The q-064 bug-surface and q-062 subsystem-gap both fired in this window's first three iterations.** A ≥12 cadence would have meant the g11-gmt-camera-manager recommendation surfaces ~7 iterations later (one full extra Sub-B segment of low-priority questions), which is mild but measurable lag on Phase 2 planning input. The bug surfaced is HMR-only and not user-facing today, so urgency is low — but the subsystem-gap finding is genuinely time-sensitive because if the orchestrator picks up new pending subsystems, sooner-is-better.
  - **(c) Followup-correcting-followup (q-071 → q-068) is a new mode that the brief hasn't explicitly addressed and that reviews are the only sanity check on.** Holding at ≥10 keeps the review cycle tight enough to spot a multi-correction cluster early if it emerges. One occurrence isn't a cluster, but extending cadence on the first occurrence is overconfident.
  - **Verdict:** Hold at ≥10 for #14. Re-evaluate ≥12 at #15 only if (i) frontmatter variance returns to 0/10 for two consecutive windows, (ii) zero new bug surfaces, and (iii) no further followup-correcting-followup cases. Tightening on lagging indicators is acceptable; loosening should require leading indicators.

- **Sub-B finish projection trues up to user estimate.** User wrote "67 × ~3.7 min ≈ 4.1 hours wall-clock". Re-checking against this window's actual times: q-062 03:47:31 → q-078 04:27:01 = 39.5 min for 10 iters = 3.95 min/iter (slightly slower than the steady-state 3.7, probably driven by q-062's 6-file investigation and q-070's 6-file investigation both being on the heavier side). Updated projection: **67 × 3.95 min ≈ 4.4 hours wall-clock remaining**. Plus the orphan-sweep (Phase 1.6) prep work. Still comfortably under the 200-iteration cap (currently at 52, leaving 148 of headroom). The 4.4h figure suggests Sub-B finish around 2026-05-20T09:00Z if the loop runs continuously, or sometime later depending on user-initiated pauses.

## Proposed LOOP_PROMPT.md edits

**One small edit.**

**Edit 1 (frontmatter regression guard).** The q-070 deviation reverted to six of the nine blacklisted field names at line 307. The blacklist alone clearly isn't enough for an agent that pattern-matches on "frontmatter looks roughly like an older example". Propose adding **one sentence** to line 307's deviant-names paragraph immediately after the existing "those are alternative names that have appeared in past agent output…" clause:

> If your draft frontmatter contains ANY of `id:`, `parent:`, `status:`, `verdict:`, `files_read:` at the top level, you are almost certainly producing the old shape — stop, copy the four-name template below verbatim, and re-fill the values.

Rationale: turns the blacklist from a "don't do these" into an "if you see any of these in your draft, you've already started wrong, start over". Adds a self-checking instruction that's testable inside the agent's own output before it commits. Cost: ~30 tokens per agent invocation; benefit: catches the single-field-revert and the full-old-shape-revert with the same rule. Strictly additive.

**Not proposing:** any changes to resumption logic, iteration step structure, or priority sort (constraint honored). Not proposing brief changes for the followup-correcting-followup pattern — the existing "every followup MUST cite files for every claim" implicitly handles it (q-071 only beat q-068 because it cited a file q-068 didn't).

The Sub-A retrospective edit from review #11 (Edit 1, explicit `defer:<subsystem-id>` hints in question triage) remains held back per "DO NOT modify open-questions.json mid-Sub-B" constraint. Not relevant to this window.

## Feedback items to mark wont-fix

(none — `protocol-feedback.json` still shows all four entries in terminal state: f-001/f-002 `wont-fix`, f-003/f-004 `addressed`. Zero new entries opened this window. Six consecutive windows without protocol feedback.)

## Skip list — feedback that should remain open

(none open.)

## Notes for human

- **q-062's g11-gmt-camera-manager recommendation is the highest-priority Phase 1-completion task surfaced this window.** Stronger case than q-061's e09b proposal because:
  - **Scope is larger** — 592 lines uncovered (cameraSlice.ts 310 + CameraManagerPanel.tsx 154 + logic.ts 128) vs e09b's ~3 small UI primitive files.
  - **Has a load-bearing invariant** — `installGmtCameraSlice()` must run before `CameraManagerPanel` first render (`cameraSlice.ts:20-22`); skipping the audit means this lives only in a code comment.
  - **Has four cross-subsystem boundaries** that need explicit documentation: engine-core (`installStateLibrary`), g05 (DDFS empty-stub convention), g04 (sibling navigation), `engine-gmt/topbar.tsx` (hand-wired Camera menu). Each boundary is currently an inference from grep, not a documented contract.
  - **Same two-option resolution as e09b** — (a) append to subsystems.json as `pending`/`primary`, loop picks it up automatically; (b) roll into Phase 2 as "spin out from g05/e09" at writing time. Recommend (a) for both e09b AND g11, run them as a small Phase 1.6 "uncovered-subsystem sweep" mini-cycle after Sub-B finishes. Audit cost is ~20 min wall-clock for both together.

- **q-064 production bug surface details.** The bug: `installStateLibrarySlice` collision guard at `createStateLibrarySlice.ts:150-154` is an `arrayKey + actions.add` AND check. Two scenarios silently mis-install:
  1. Same `arrayKey`, different `actions.add` — second install clobbers the array, wiping all saved snapshots.
  2. Different `arrayKey`, same `actions.add` — second install's `addSnapshot` overwrites the first; calling `addSnapshot()` only appends to the second library (the first library's data persists but its actions all silently redirect to the second library).

  Neither scenario is reachable today (only one library type exists with one set of action names), but the header comment at `createStateLibrarySlice.ts:1-7` mentions planned palettes / brush-presets libraries — both would trip scenario 2 if they reused `actions.add: 'addSnapshot'`. q-064 proposes a 6-line fix: two module-scope `Set<string>` registries, one for `arrayKey` and one for action names. Phase 2 code-health doc should call this out as a "future-library landmine" with the fix already specified.

- **q-077 documents a docs/code drift in `docs/gmt/07_Code_Health.md:316-326`.** The 2026-03-13 Cat 3 audit claims `AudioSpectrum.tsx:20`, `AudioLinkControls.tsx:17`, and the `applyPreset` callsites were converted from `@ts-ignore` to `@ts-expect-error` with comments. q-077 verifies that the live files still have plain `(store as any)` casts — neither `@ts-ignore` nor `@ts-expect-error`. Either the conversion was reverted in a later commit or the audit was aspirational. Also flags that `AudioPanel.tsx:134` uses the same `(store as any).updateModulation(...)` pattern but is missing from the Cat 3 table entirely. Worth a Phase 2 reconciliation pass on `07_Code_Health.md` to either re-converge the table with the current files or note the regression.

- **q-070's frontmatter shape is the one to spot-check first if any Phase 2 indexer breaks on followups.** It's the only file in this window using `id:`, `parent:`, `status:`, `verdict:`, `target_hint:`, `files_read:`. Other 51 followups all conform to the canonical 4-name shape. If an indexer hits a `KeyError` on `question_id` or `files_consulted` for one file out of 52, that's q-070. Easy to hand-patch (10 minutes wall-clock) versus tightening the brief further to avoid the third occurrence — recommend hand-patching rather than re-tightening, since the brief edit proposed above already addresses the underlying pattern.

- **Sub-B progress: 52 of 76 primary answered (~68%), 67 pending overall** (24 primary + 43 low). At 3.95 min/iter steady-state: **24 × 3.95 ≈ 1.6 hours of primary remaining**, then **43 × 3.95 ≈ 2.8 hours of low remaining**. Total ≈ **4.4 hours**. Confirms the user's ~4.1h estimate within rounding. If the user wants Sub-B to finish before the next natural break, the next ~4-5 hours of orchestrator time covers it.

- **Watchlist for the next review (#14, fires after ~q-088):**
  - (a) Bug-surface rate: if #14 has zero new bugs, the running 1-in-26 rate falls to 1-in-32; if it has 1+, the rate stays at 1-in-22. Either is fine for Phase 2 planning purposes — the rate has stabilised inside the "useful but not critical" band.
  - (b) Frontmatter variance: holding at 0/10 in #14 means Edit 1 above (if applied) worked. Holding at 1+/10 means an even more explicit brief edit is needed — possibly inverting the structure to "here is the canonical template, copy it; here is what NOT to produce."
  - (c) Followup-correcting-followup count: q-071→q-068 is one occurrence. If #14 has another, the pattern is real and recurring; consider adding an explicit "before claiming X about a module you didn't read, cite the survey that DID read it" prompt clause. One occurrence isn't enough.
  - (d) Subsystem-gap finds: q-062's g11 and q-061's e09b are the only two so far. If #14 finds a third, the orphan-sweep / Phase 1.6 mini-cycle becomes more clearly worth the wall-clock; if not, the current two-subsystem list is the stable carry-in for Phase 1.6.
  - (e) Cadence decision: hold at ≥10 through #14 regardless of (a)-(d). Re-evaluate ≥12 at #15.

PASS.
