---
review_id: review-2026-05-20-15
generated_at: 2026-05-20T08:25:00Z
feedback_items_reviewed: []
iterations_window: [q-097, q-098, q-099, q-100, q-101, q-102, q-107, q-110, q-111, q-112]
---

## Patterns observed

- **Frontmatter shape: 10/10 canonical — third consecutive clean window.** Every followup in this window uses the four-name canonical shape (`question_id`, `source_subsystem`, `investigated_at`, `files_consulted`) with the nested `- path: ... blob_sha: ...` structure under `files_consulted`. None of the nine blacklisted alternative names appears. Window-by-window deviant count is now `q-001..q-015: 2/10 → q-016..q-028: 0/10 → q-029..q-042: 4-5/10 → q-043..q-061: 0/10 → q-062..q-078: 1/10 → q-083..q-096: 0/10 → q-097..q-112: 0/10`. Three clean windows in a row after the q-070 regression. Edit 1 from review #13 (the "if your draft contains ANY of `id:`, `parent:`, `status:`, `verdict:`, `files_read:` — stop and start over" self-check sentence) plus Edit 1 from review #14 (confidence-value cleanup) have stabilised the brief. **Variance is now solved at protocol level; no further frontmatter edits warranted.**

- **One new production bug surfaced — q-112 `MAX_SKY_DISTANCE` divergence.** This is genuine and impactful: `WorkerDepthReadback.ts` uses `MAX_SKY_DISTANCE` (=50.0) for the auto-readback path at lines 75/80/126 but hardcodes `< 1000` for the focus-pick path at lines 151/169. Both filters answer the same "is this depth a real surface hit?" question on the same alpha-channel sentinel. **User-visible impact**: click-to-focus on a depth in `[50, 1000)` will set `dofFocus` to that value while auto-readback would reject it as sky. Currently unreachable for the 42 shipped formulas (surface hits cluster well under 50, sky writes much larger sentinel) — but any future formula using a worldspace larger than ~50 units would silently get wrong DoF focus from click-to-pick while auto-focus works correctly. **One-line fix per site** (`< 1000` → `< MAX_SKY_DISTANCE`). The doc at `docs/gmt/02_Rendering_Internals.md:664` already asserts they agree — code drift, not intentional. Running production-bug count: 3 (q-019 `DuplicatePresetFieldError`, q-064 StateLibrary action-name collision HMR-only, q-112 `MAX_SKY_DISTANCE` divergence). Rate updates to 3-in-72 = 1-in-24 followups.

- **No followup-correcting-followup cases this window.** Every correction in this window is followup-correcting-survey or followup-correcting-code-comment, not followup-overrules-followup. q-071 → q-068 from window #13 remains the unique instance.

- **Engine-gmt vs engine-core divergence is the dominant content theme.** Six of ten followups (q-097, q-098, q-099, q-100, q-101, q-102) interrogate the dev/engine-vs-engine-gmt fork boundary explicitly. Concrete findings:
  - **q-097 (`UniformSchema.ts` "duplicate state" claim)**: Refutes the `21_Code_Review` claim that the two copies are deduplicatable. The one-line import-path divergence (`./features` vs `../features`) is a **parametrisation hook**, not duplication — pulling out to a shared module would compose the wrong registry into the wrong-engine schema. Recommends downgrading the survey item from "convert to re-export" to "parametrised by sibling `features/` directory; cannot be re-exported without breaking pure-core vs GMT separation."
  - **q-098 (`ConfigManager` divergence)**: 74-line delta is intentional and consistent with the engine-extraction pattern. Engine-gmt carries four GMT-specific couplings (`detectEngineProfile`, `FractalEvents`, batched compile log, lighting/Modular special cases) that were deliberately stripped from the engine extract. JSDoc on extracted file is the canonical contract and should NOT be back-ported.
  - **q-099 (`UniformManager` doc path drift)**: Six concrete `docs/gmt/02_Rendering_Internals.md` and `docs/specs/10_Shaped_Emission.md` references drop the `engine-gmt/` prefix and would lead a contributor to a non-existent file. Plus one broken relative link in `docs/gmt/43_Bucket_Render_Overhaul.md:23`. CHANGELOG_DEV entries are correctly left alone (historical record).
  - **q-100 (`FeatureStateMap` TODO)**: Engine-gmt's comment lags the engine-core direction. `FeatureStateMap` IS implemented; the unfinished work is intersecting it into `ShaderConfig`. Engine-core has moved past this model entirely to per-app declaration merging (`engine/ShaderConfig.ts:5-10`). Stale comment in engine-gmt should either point at `07_Code_Health.md` Category 3 or be rewritten to match engine-core's "augment via declaration merging" framing.
  - **q-101 (`pipelineRevision` required field)**: Confirmed required + every construction site supplies a value. Engine-gmt makes it required; legacy engine still has it optional. The `[key: string]: any` index signature defeats strict checking — required typing is documentation more than enforcement, but discipline holds across all four observed call sites.
  - **q-102 (`FormulaType` hand-maintained union)**: Real drift instance — five registered aliases (`HyperTorus`, `UberMenger`, `FoldingBrot`, `HyperbolicMandelbrot`, `RhombicIcosahedron`) are NOT in the union. Stable branch already mitigates with `FormulaType = string` opaque tag — dev/engine-gmt fork has regressed to literal union. Three SSoT options ranked by intrusiveness.

  This concentration is not a window artefact — it's the natural product of Sub-B reaching the `g0*` GMT-subsystem cluster. Phase 2 will need a dedicated **"engine vs engine-gmt fork audit"** appendix (or section in `docs/engine/06_Code_Health.md`) consolidating these six findings: each one diagnoses a different aspect of the same divergence (deliberate parametrisation, intentional coupling deltas, stale doc references, stale TODOs, structural drift in shared interfaces). Recommend folding all six into one cross-cutting Phase 2 doc rather than scattering across per-subsystem module docs.

- **Followup-corrects-survey/code rate this window: 5/10.** q-097 (survey "convert to re-export" recommendation refuted), q-099 (six doc-path errors confirmed), q-100 (stale TODO comment in engine-gmt source), q-102 (drift in hand-maintained union), q-112 (production bug — focus-pick threshold drift from `MAX_SKY_DISTANCE`). Falls in the 30-50% stable band. Two pure confirmations (q-098, q-101), three Phase-2-context findings (q-107, q-110, q-111).

- **New sub-pattern: "claim revision" framing for surveyor-flagged items.** q-097 explicitly opens "Verbatim-copy claim revision" and walks through the surveyor's claim, the current evidence, and a recommended doc edit. This is a step beyond the q-052/q-053/q-056 corrections (which simply contradict the survey) — q-097 names the survey's exact recommendation and proposes alternative text. Phase 2 module-doc writers should adopt this as the preferred shape: cite the survey claim verbatim, then revise. Helps the rolling audit corpus stay readable as a delta rather than a series of unconnected corrections.

- **Window timing: ~5.5 min/iter — heavier still than last window.** Per open-questions.json: q-097 at 07:30:00Z → q-112 at 08:15:00Z spans ~45 min but only over a non-contiguous set of 10 questions (q-103..q-106, q-108, q-109 fell in between). Direct steady-state per-iter timing requires the orchestrator log; conservative estimate is 5-5.5 min/iter for this engine-vs-engine-gmt-heavy cluster (mean files_consulted in this window ≈ 4.3, up from 3.6 last window). The cross-fork investigations cost more wall-clock per iteration because each one requires reading both copies plus consumers.

- **Cadence at ≥10 — hold for #16, do NOT extend to ≥12.** Per review #13's contract: re-evaluate ≥12 at #15 only if (i) frontmatter variance returns to 0/10 for two consecutive windows, (ii) zero new bug surfaces, AND (iii) no further followup-correcting-followup cases. Status:
  - **(i) PASS** — 0/10 for the third consecutive window.
  - **(ii) FAIL** — q-112 is a real production bug (`MAX_SKY_DISTANCE` divergence). User-visible impact is currently unreachable but the bug is real (code/doc agree on intent; only code drifted), one-line fix, would trip any future large-worldspace formula. Adds the 3rd production bug to the Sub-B run.
  - **(iii) PASS** — no followup-correcting-followup case.

  Condition (ii) fails → **HOLD at ≥10**. The fact that the bug surfaced at iteration ~5 of this window (q-112) validates the tight cadence: at ≥12 the bug would have surfaced ~2 iterations later, and at ≥15 (the originally-proposed jump) ~5 iterations later. With three bugs across 72 followups, expecting a 4th within the remaining ~40 iterations is reasonable; keeping cadence tight means we catch it within ~5 iterations of opening rather than ~7-12.

  **Verdict:** Hold at ≥10 for #16. Re-evaluate ≥12 at #17 only if all three conditions are clean for that window AND for #15 in retrospect. The bar should ratchet — extending on partial-clean evidence repeatedly is overconfident.

## Proposed LOOP_PROMPT.md edits

**None.** All three patterns observed (frontmatter cleanup is stable across three windows, the dominant theme is content-density not protocol-friction, q-112's bug is a code drift not a protocol miss) point to no brief change needed this tick. The protocol is in a stable steady state — the last three reviews (#13 added self-check sentence, #14 fixed stale confidence values, #15 nothing) show natural deceleration.

The Sub-A retrospective edit from review #11 (Edit 1, explicit `defer:<subsystem-id>` hints in question triage) remains held back per "DO NOT modify open-questions.json mid-Sub-B" constraint — not applicable until Sub-B end.

## Feedback items to mark wont-fix

(none — `protocol-feedback.json` shows all five entries in terminal state: f-001/f-002 `wont-fix`, f-003/f-004/f-005 `addressed`. Zero new entries opened this window.)

## Feedback items to keep open

(none open.)

## Notes for the human

- **q-112 is a real production bug — please fix.** `WorkerDepthReadback.ts:151,169` should change `< 1000` to `< MAX_SKY_DISTANCE`. Two-line edit. The bug is currently unreachable for shipped formulas (sky writes much larger than 1000), but the divergence is silent and any new formula with a larger worldspace would silently get wrong DoF focus from click-pick while auto-focus works correctly. Doc at `docs/gmt/02_Rendering_Internals.md:664` already asserts the constant is shared by all readback paths — code/doc mismatch.

- **Engine vs engine-gmt fork audit is now well-justified as a Phase 2 cross-cutting deliverable.** Six followups in this window (q-097..q-102) diagnose six aspects of the same divergence pattern. Recommend a dedicated section/doc rather than scattering across per-subsystem module docs:
  - **Parametrisation hooks that look like duplication** (q-097 `UniformSchema` import-path divergence — must NOT be deduplicated)
  - **Intentional coupling deltas in extracted modules** (q-098 `ConfigManager` 74-line GMT-coupling delta — must NOT be back-ported)
  - **Doc-path references that drop the `engine-gmt/` prefix** (q-099 — six concrete sites in `docs/gmt/` and `docs/specs/`, plus one broken relative link)
  - **Stale TODOs in engine-gmt source that the engine-core fork has already moved past** (q-100 `FeatureStateMap` TODO vs declaration-merging substrate)
  - **Optional-vs-required type tightening that hasn't migrated across the fork** (q-101 `pipelineRevision?` in engine vs `pipelineRevision` in engine-gmt)
  - **Hand-maintained types that drift from runtime registrations** (q-102 `FormulaType` literal union vs stable's `string` opaque tag — dev fork has regressed)

  Phase 2 carry-in for this cross-cutting story: one doc (~3-4 pages) covering the fork-divergence taxonomy + the six concrete instances, with the canonical rule being "engine/ stays generic; engine-gmt/ keeps GMT-specific behaviour; cross-fork claims must cite the exact path." This anchors `feedback_duplicate_module_state.md` (the existing memory note) with concrete examples.

- **q-099's `docs/gmt/` path-drop set is a small but visible doc bug surface.** Six bare `engine/managers/UniformManager.ts` references in `02_Rendering_Internals.md` (×3), `43_Bucket_Render_Overhaul.md` (×1, also broken relative link), `10_Shaped_Emission.md` (×2). All would 404 from `docs/gmt/`. Per audit protocol `docs/` is read-only historical reference, so the fix surface is whichever future doc-refresh window the user runs — but the corrections are now indexed and ready to apply.

- **q-110's "apps layer their own" verdict on the hint system is a clean Phase 2 module-doc bullet.** `engine-gmt/navigation/HudOverlay.tsx` deliberately ships without a hint overlay. The `activeHint?` / `onDismissHint?` props are app-extension hooks; the `_activeHint` rename signals intent explicitly. The wider tutorial/hint system lives in `engine/plugins/Tutorial.tsx` and is composed in by the app. Add to `docs/engine/05_Shared_UI.md` (the same doc q-088/q-089 from last window will touch) — the "navigation HUD does not render hints; apps mount their own widget alongside" rule.

- **q-107's pivot-reticle imperative DOM mutation is a small unification target.** The honest characterisation is "lifecycle-and-coordinate-system convenience, not perf" — unification would add a `canvasOverlay` slot to `HudOverlay` so both reticles render there. Not urgent (current code is correct, cleanup is sound) but worth noting as a low-risk consolidation when `HudOverlay` is next touched.

- **q-111's `isCameraLocked` vs `ignoreCamera` finding is "not duplicate, dual".** No drift between the e03 and g04 surveys — they accurately describe opposite halves of the same flag-set. The only real refactor opportunity is naming: a shared `selectCameraLocked` / `selectIgnoreCamera` selector on the animation store would centralise the convention, but today's code is correct and both surveys are consistent. Worth a one-line note in `docs/engine/04_Animation.md` (if/when written) that the predicates share inputs but encode opposite outputs and have **three independent consumers** (Navigation, AnimationEngine, PauseControls) reading the same flags with different conjunctions.

- **Phase 2 cumulative carry-in tally** (refreshing from review #14):
  - **Production bugs: 3** — q-019, q-064, **q-112 (new this window — `MAX_SKY_DISTANCE` divergence)**.
  - **New uncovered subsystems: 2** — e09b-state-library-ui, g11-gmt-camera-manager (unchanged).
  - **Doc-rewrite targets: ~32-33** (added q-097/q-098/q-099/q-100/q-101/q-102/q-110 in some form).
  - **Dead-code / cleanups: ~18-20** (unchanged — this window had no new dead-code finds).
  - **Code drift / staleness: ~6-7** — added q-099 doc-path drift (6 sites), q-100 stale TODO comment, q-102 union drift, q-112 `MAX_SKY_DISTANCE` divergence (code drift even though it's also a bug).
  - **New cross-cutting deliverable: 1** — engine vs engine-gmt fork audit doc (consolidates q-097..q-102).

- **Sub-B progress estimate.** With ~72 of 76 primary answered (~95%) and ~40-50 low remaining, finish projection at 5-5.5 min/iter is ~3.5-4.5 hours wall-clock. Comfortably under the 200-iteration cap (currently at ~72, leaving ~128 of headroom).

- **Watchlist for review #16 (fires after ~q-122):**
  - (a) Frontmatter variance: holding at 0/10 for a fourth consecutive window confirms the protocol is stable. Don't expect further tightening edits.
  - (b) Bug surface: if #16 has zero new bugs, the running 1-in-24 rate falls back to 1-in-29; if it has 1+, the rate stays at 1-in-22. Either is consistent with the "useful but not critical" band — but conditions for ≥12 cadence extension require zero new bugs.
  - (c) Followup-correcting-followup count: only q-071→q-068 still. If #16 has another, the pattern becomes worth a brief clause.
  - (d) Engine-vs-engine-gmt drift findings: if #16 surfaces 3+ more of the same shape, the cross-cutting Phase 2 deliverable proposal hardens; if it's 0-1, the q-097..q-102 cluster was a window artefact (Sub-B reaching the `g0*` subsystem cluster) rather than ongoing.
  - (e) Cadence decision: if #16 conditions match (i)+(ii)+(iii) AND #15 partial-clean is treated as one strike against extension, propose ≥12 at #17. Don't extend at #16.
  - (f) q-112 fix landing: spot-check that the `MAX_SKY_DISTANCE` substitution happens in a near-future commit. If not, the bug carry-in stays on the Phase 2 list as a code-health item.

PASS.
