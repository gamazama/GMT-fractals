---
review_id: review-2026-05-20-6
generated_at: 2026-05-20T01:15:00Z
feedback_items_reviewed: []
iterations_window: [g10-audio-fps-sync, d01-engine-docs-1, d02-engine-docs-2, d03-gmt-docs-1, d04-gmt-docs-2]
---

## Patterns observed

- **The docs-existing tier is producing the highest-density Preservable signal of the whole audit so far — d03 in particular.** d03-gmt-docs-1 yielded Preservable bullets that read like a complete institutional-knowledge dump: full ShaderBuilder hook catalog (18 hooks × position × scope), the requestShading/addShadingLogic deferred-generation pattern, Henyey-Greenstein + Beer-Lambert constants with the exact source-paper attributions (Veach 1995, Heitz 2018 §3 eq.17), the Firefox VideoEncoder quirk cluster (PTS reconstruct, default duration miss, latencyMode quality), the entire Troubleshooting_and_Quirks doc captured as "every section is a Preservable bullet" with concrete WebGL/raymarching gotchas (TDR, biasEps for concave fractals, DOF blur stability, HalfFloat16 readback). d04 is similar shape: full preambleVars contract, cutting-plane estimator architecture, V4 paused-state rationale, mesh-export 6-phase pipeline. **Strategic implication**: when Phase 2 module-doc writers start, the gmt-tier writers should read the matching `survey/_docs/d0X-*.md` BEFORE the matching `survey/gXX-*.md` — the Preservable bullets capture *why* far better than the survey's drift-table-and-citation format does. Worth a Phase 2 brief note when those instructions are added.
- **The frontmatter-format issue is on its second occurrence (g04 yaml-flow → d03 bare-paths), both caught by orchestrator not by an agent.** g04 used flow syntax `{path: ..., blob_sha: ...}`. d03 used bare path lists `- docs/gmt/01_*.md` under `files:` with no blob_sha or lines_read at all. Different failure modes, same root cause: agents interpret "frontmatter shape" with high variance when the shape diverges from typical YAML they've seen. Both were resolved inline (g04: tightened brief at line 52 + 39; d03: regenerated coverage from disk + tightened summarize-existing brief at line 99). **Pattern observation**: the audit-action brief got its tightening in review #4 Edits 1 & 2 (Section 3 paragraph + section preamble); the summarize-existing brief got *its* tightening only this window (this iteration's d03 fix). The two briefs were drifting in parallel — same shape problem in both, but only one tightened at a time. The brief-tightening that already happened this window is exactly the right level of intervention; I'm flagging the parallel-drift dynamic so future review ticks remember to check that fixes in one action brief have their equivalent applied to the *other* action briefs when the underlying contract is shared. Two occurrences = pattern; if there's a third occurrence on a third brief variant, it warrants a structural refactor of "describe the frontmatter once, link from each brief". Not now — both briefs are tightened and d04 ran clean.
- **d04 ran clean immediately after the d03 brief tightening — a positive signal that the inline fix worked.** d04's frontmatter uses fully canonical block YAML (`- path: ... blob_sha: ... lines_read: [1, N]`) for all 9 entries. d03's regenerated-from-disk frontmatter has only path bullets (no blob_sha or lines_read); the regeneration captured what was readable post-hoc but didn't backfill blob_sha. **Implication for coverage.yaml integrity**: d03's coverage entries were synthesized from disk reads, not from agent-reported blob_sha. For Phase 1 they're fine (the docs aren't churning). For Phase 2 it means any "did the docs change since survey?" comparison should not rely on d03's blob_shas. Worth a one-line note in d03's coverage entries or in review-log.md so Phase 2 doesn't trust those SHAs as snapshots.
- **Phase 1 is 30/37 done (81%). Remaining 7 = 5 docs-existing (d05-d09) + 2 secondary (t01-t02).** Of the remaining docs-existing, three are animation-refactor batches (d05/d06/d07), one is archive docs (d08), one is root-research/specs (d09). All five are read-only summarization with the now-tightened brief. ETA at ~5min/iteration: ~25 min for the docs-existing tail. Then the two secondary apps (fluid-toy, fractal-toy) which are out-of-scope from the GMT engine perspective but on the priority list — those are likely smaller surveys (~3 min each). **Phase 1 total wall-clock remaining: ~30-35 minutes.** Then Phase 1.5 Sub-phase A (gather + triage) fires in a single iteration.
- **Open-question worklist size estimate is firming up.** Reviewing surveys produced so far: ~3-5 numbered entries per survey, occasionally more for the larger-surface subsystems (g03 with 56 files, e13 with 29). Across 25 module surveys, projected total is ~90-110 raw questions. After de-duplication and "low priority curiosity" demotion (Section 1.5 Sub-phase A triage), expect ~50-70 primary-priority answerable questions. **That's inside the 30-80 expected band and well inside the 200 hard cap.** No protocol change needed; Phase 1.5 should run smoothly.

## Proposed LOOP_PROMPT.md edits

(none — the summarize-existing brief tightening already happened inline this window and addresses the d03 bare-paths failure. No new patterns require brief changes this tick. Edit 1 from review #4 (canonical Orphan-sweep prefix) and Edit 2 from review #4 (block-syntax preamble) both continue to hold cleanly. The parallel-brief-drift pattern noted above is a watchlist item, not yet warranting an edit.)

## Feedback items to mark wont-fix

(none — `protocol-feedback.json` shows all four entries in terminal state from prior reviews: f-001/f-002 `wont-fix`, f-003/f-004 `addressed`. No new entries opened in this window. Both inline fixes this window (g04 yaml-flow on prior tick, d03 bare-paths this tick) were caught by orchestrator without ever being filed as protocol-feedback entries — they didn't need triage.)

## Feedback items to keep open

(none — no open feedback at tick start.)

## Notes for the human

- **Phase 1.5 prep is now actionable.** With Phase 1 wall-clock remaining at ~30-35 minutes (per the projection above), the human should have the following items pre-positioned before Phase 1.5 fires:
  1. **Triage agent context (Sub-phase A)** is already speced in LOOP_PROMPT.md lines 268-275 — that step is self-contained and needs no human prep.
  2. **Aspirational doc rewrite-vs-migrate decisions are now five stacked** (review #4 listed three: f-001/e06, e07, e13; review #5 added two more from g07's `30_Mesh_Export_Prototype.md` and g09's `03_Modular_System.md`). **This window adds further drift evidence from d03/d04**: `docs/gmt/01_System_Architecture.md` describes pre-extraction `engine/` paths; `docs/gmt/30_Mesh_Export_Prototype.md` claims "plain HTML + 8 ES2020 scripts" while doc 24 references `mesh-export/gpu/gpu-pipeline.ts` and `mesh-export/store/meshExportStore.ts` (direct contradiction noted in d04 summary); `docs/gmt/07_Code_Health.md` flags multiple "MAY BE STALE" items including the FeatureStateMap aspiration. **The Phase 2 kickoff session should resolve rewrite-vs-migrate per doc in one pass** rather than letting per-module writers re-litigate. Suggested triage matrix: (a) docs with structural mismatch to current code (01, 30) = rewrite; (b) docs that are still architecturally correct but reference wrong paths (02, 03, 06) = migrate-target (writer updates paths during module-doc composition); (c) docs that are purely aspirational planning (43, 44 already noted shipped-with-changes) = preserve as historical record with explicit "STATUS: shipped — see [module doc] for current" pointer.
  3. **Orphan-sweep worklist is now ~34 entries (29 canonical + 5 lowercase from g02/g04 pre-Edit-1).** No new orphan-sweep nominations from docs-existing batches (correct — they're catalog-summaries not surveys). Final orphan-sweep input at Phase 1 end will be ~34 + whatever t01/t02 produce + the lowercase-needing-retag count of 5. Still tractable single-pass-per-subsystem.
- **d03's coverage entries lack blob_sha (synthesized from disk after agent failed to report them).** For Phase 2's "has this doc changed since survey?" check, d03's 10 doc entries should be excluded or re-shafted. Suggest adding a note to `review-log.md` (or to a comment field in coverage.yaml itself if schema allows). Not blocking — Phase 2 might just re-run blob-sha on those 10 docs at the start of any "regenerate" pass.
- **The d03 summary survey is uncommonly information-dense — at 332 lines for 10 docs (~33 lines/doc) vs d01/d02's ~10-15 lines/doc.** This is *not* a quality regression — d03 covered the user-facing doc tier including Troubleshooting_and_Quirks.md which is itself ~hundreds of lines of institutional knowledge. d04 is 296 lines for 9 docs (~33 lines/doc) — same density. The earlier d01/d02 covered engine-API docs which are leaner. Different sources, different densities, both honest.
- **Cosmetic `audited_at` drift continues** — d03/d04 both use `T00:00:00Z` midnight stamps. progress.json holds the real timestamps. Still not worth a fix.
- **Review-tick cadence.** This is tick #6, fired on the iterations-≥5 trigger. Open-feedback-≥3 has never fired naturally — feedback entries are being resolved inline at orchestrator level faster than they accumulate. Per review #5's note: "If reviews #6 and #7 also propose only clarifications or nothing, the iterations-≥5 cadence could safely extend — but that's a load-bearing trigger-logic change and out of scope per constraints." This review proposes nothing. If review #7 also proposes nothing, that's two clean ticks in a row and worth surfacing the cadence-extension to a human deliberately rather than just observing it.
- **For the next review (#7), watch:** (a) whether t01/t02 (secondary apps, different code styles) reintroduce frontmatter shape variability — they're the next agent-batch boundary and Edit 1/2 from review #4 haven't been tested against fluid-toy/fractal-toy code surface yet; (b) whether the Phase 1 → Phase 1.5 transition fires cleanly (Sub-phase A is one-shot and well-speced, but the orchestrator's never run it before — first execution is highest-variance); (c) the actual primary-priority count after triage vs my 50-70 projection — if it's 100+, Sub-phase B will need cost-guard attention.
