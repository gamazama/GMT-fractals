---
review_id: review-2026-05-19-2
generated_at: 2026-05-19T20:58:00Z
feedback_items_reviewed: []
iterations_window: [e02-tick-registry, e03-animation, e04-shader-builder, e05-render-pipeline, e06-adaptive-resolution, e07-plugins-host]
---

## Patterns observed

- **Edit 1 from review #1 is working — zero new protocol-feedback entries since the last review.** Six iterations have completed (e02..e07) with no new f-NNN noise. The two existing entries (f-001, f-002) are both already `wont-fix`. Surveys have started embedding code/doc-drift findings inside the survey body where they belong (e.g. e07's Drift table, e03's Drift table, e04/e05 Open questions) rather than escalating them to protocol-feedback. The scope-tightening in Section 6 landed cleanly.

- **Edit 2 from review #1 (the `Recommendation:` line on drift tables) is being used as intended and with good discrimination.** Three of the six iterations had `existing_doc_ref` set and used the slot:
  - e07-plugins-host: `Recommendation: rewrite.` — 13 rows, mostly break; called out a closed decision (2026-04-22 nine-plugin lineup) that's been silently reversed. Correct call.
  - e06-adaptive-resolution: `Recommendation: rewrite.` — 8+ break rows; primary public APIs (`useViewport()`, `viewport.onResize`, `<ViewportArea>`, `<PerformanceWarning>`) don't exist in code. Correct call.
  - e03-animation: `Recommendation: minor-edits` — explicit reasoning ("most rows are info/warn pointing at specific corrections; one break for non-float track silent-drop, one break for log/Bezier"). Correct call — the doc's architecture is mostly accurate, just type signatures and an API table need updates.
  The slot is NOT being over-used (no recommendation on the small-drift cases, agents are reasoning about severity).

- **Surveys with no `existing_doc_ref` cleanly skip the Drift section.** e04-shader-builder and e05-render-pipeline both render `## Drift from existing doc\n(no existing doc — skip)` or similar. Good — agents read the conditional in the brief correctly.

- **`audited_at` ISO timestamp inconsistency persists across iterations.** e02/e06/e07 use date-only midnight (`2026-05-19T00:00:00Z`), e03 uses HH:MM (`T20:45:00Z`), e04 uses `T14:32:00Z` (wall-clock-ish but the actual completion was 20:37 per progress.json), e05 uses local TZ offset `T22:40:47+02:00`. Review #1 flagged this as "not worth a fix on its own." With six iterations of evidence I still agree — it doesn't affect any downstream consumer because the canonical timestamp lives in `progress.json` with proper ISO format. Not proposing an edit.

- **Iteration mechanics remain healthy.** Six clean `survey_done`s, no FAILs, file counts 2–16 (well within agent budget), zero `>3-agent` retries, no >25-minute runs. Spot-checked surveys are dense with file:line citations and substantive invariant capture (e.g. e05's `writeIndex inverted vs intuition`, e07's `Menu queueMicrotask defer is load-bearing`). The loop is producing high-value output.

## Proposed LOOP_PROMPT.md edits

**No edits proposed.** The two changes from review #1 are doing their job. The `audited_at` cosmetic drift is not worth a brief change. No new agent confusion has surfaced in the six-iteration window.

If the loop continues this clean for another 5–10 iterations, the human may want to consider extending the review-tick interval from `≥ 5` to `≥ 8` normal iterations (the open-feedback-≥3 trigger would still fire on noisy days as a safety net) — but that's a load-bearing change to the trigger logic and explicitly out of scope for this review per constraints.

## Feedback items to mark wont-fix

(none — all existing items are already resolved)

## Feedback items to keep open

(none — all existing items are already resolved)

## Notes for the human

- **Planner decision on existing-doc rewrites is becoming urgent.** Two of the last six iterations (e06-adaptive-resolution, e07-plugins-host) emitted `Recommendation: rewrite` with substantive evidence. e07's drift includes a *closed architectural decision* (2026-04-22 "nine core plugins, not fewer or more") that has been silently reversed — help-browser and PWA-update were promised app-specific but now ship as engine plugins. Phase 2 writers will need a steer: rewrite from survey, or treat the doc as the migration target and adjust code? f-001 raised the same question for e01-feature-system. You now have three primary subsystems lined up with the same question. Worth answering once, generically, before Phase 2 begins.

- **Recommendation slot discrimination is good signal about agent judgment.** e03's "minor-edits" call (with explicit reasoning that the doc's architecture is sound) shows agents are not pattern-matching "drift = rewrite". This is the right calibration — but it also means surveys without a Recommendation are NOT implicitly "rewrite", they're "no recommendation needed". Phase 2 writers should read the entire Drift table, not just the Recommendation line.

- **Six clean iterations, no protocol noise.** If this pattern holds through e08+, the loop is essentially self-sustaining and review ticks become low-yield. Mentioning so you can decide whether to lengthen the cadence after the next review.

- **e07's audit confirms a "no uniform plugin contract" finding** (each plugin invented its own state-capture pattern: SceneIO module singletons, Menu queueMicrotask fanout, Hud insertion-counter ordering, RenderDialog generics, BucketRender controller interface). This is the `21_Code_Review_2026-04-25.md "plugin pattern not uniform"` note materializing in survey form. Worth surfacing as a Phase 2 cross-subsystem theme — it's not just a doc-drift issue, it's a code-architecture finding that affects how engine plugins should be documented uniformly.
