---
review_id: review-2026-05-20-4
generated_at: 2026-05-20T00:15:00Z
feedback_items_reviewed: []
iterations_window: [e13-shared-ui, g01-renderer, g02-shader-pipeline, g03-formula-registry, g04-navigation]
---

## Patterns observed

- **YAML flow-vs-block frontmatter trap (resolved by orchestrator inline).** g04-navigation emitted file entries using YAML flow syntax (`{ path: ..., blob_sha: ..., lines_read: N }`) rather than block syntax. The frontmatter parser only accepted block, so 0 files were initially recorded. Orchestrator patched the parser and tightened Section 3 of LOOP_PROMPT.md to mandate block syntax. Fix is already applied this tick — not re-proposing. The brief now reads "(block-style YAML, NOT flow syntax — one entry per file, indented 2 spaces under `files:`)" at LOOP_PROMPT line 52, which directly addresses the trap going forward. No further protocol change needed.
- **Tier A/B/C reading strategy is paying off for large subsystems.** Three of the five iterations crossed the high-file-count threshold and used tier-based reading: e13-shared-ui (29 files), g01-renderer (15 files), g03-formula-registry (56 files). All three produced surveys with file:line citations across the full set and explicit drift tables — no skim symptoms. g03 in particular (56 files in one survey at 4:33 wall time) demonstrates the brief scales further than expected. Worth keeping as-is; no edit needed.
- **Orphan-sweep nominations are flowing as expected** (review #3 Edit 2 has caught on). Total across the 5 surveys: **16 entries** using the canonical `Orphan-sweep candidate:` prefix (e13: 11, g01: 3, g02: 1, g03: 1, g04: 0). e13's eleven nominations alone validate the prefix — a future orphan-sweep phase will have a real worklist instead of having to re-derive one from scratch. The "one-line reason" requirement is being honored: most nominations include why the file belongs in that subsystem (e.g. "listed as primitive in 05_Shared_UI.md:27 but not in Tier A claim list").
- **Prefix inconsistency surfaced in g04.** g04-navigation uses a lowercase variant `orphan-sweep: <topic>` (4 entries at lines 97-100) instead of the canonical `Orphan-sweep candidate: <path>`. The lowercase form scans more like a tag than a path nomination, and a future `grep -r "Orphan-sweep candidate:"` worklist build would miss them. Worth a small wording reinforcement in the brief — exact-case + path-first form. (g02 also has a hybrid form with a trailing `Prefix:` annotation but its leading marker is canonical, so grep would still pick it up.)
- **engine-gmt drift against pre-extraction docs is materially larger than engine-core drift was — expected, but worth flagging for the planner.** g01-renderer cites `dev/docs/gmt/01_System_Architecture.md` which still describes the pre-extraction `engine/` layout; g02-shader-pipeline finds that `02_Rendering_Internals.md` mentions `engine-gmt/` paths but doesn't yet cover the builder API surface or ConfigManager/UniformManager internals; g03 finds 56-file scope against a still-comprehensive `25_Formula_Dev_Reference.md` with mostly gap-shaped drift; g04 has no existing doc at all. None of these are protocol issues — they're the substrate of why Phase 2 module docs are being commissioned. But the planner should expect that the gmt-tier writers will be doing more from-scratch composition than the engine-core writers (where doc 02/06 etc. already had a recent baseline to edit against). This shifts Phase 2 effort estimates: engine-gmt module docs likely run ~1.5x the engine-core writer cost.

## Proposed LOOP_PROMPT.md edits

### Edit 1 — Tighten the Orphan-sweep prefix wording to enforce exact-case and path-first form

- **Where:** Section 3 "Action: audit or create", the Constraints paragraph (introduced by review #3 Edit 2).
- **Current text** (LOOP_PROMPT.md Section 3, lines 71-72):
```
> Constraints: every architectural claim cites file:line. If a file is >800 lines, read it in chunks but report read_range honestly — do NOT skim. If you found things outside `files_claimed` that belong to this subsystem, note them under "Open questions" with the prefix "Orphan-sweep candidate:" followed by the file paths and a one-line reason they belong here — don't expand scope unilaterally, but DO surface them so a later orphan-sweep phase can pick them up. Existing doc at `<existing_doc>` is for cross-reference only — do NOT edit it. Report PASS or FAIL with reason. If you need to report back something to update our working protocol, say so outside the report.
```
- **Proposed text:**
```
> Constraints: every architectural claim cites file:line. If a file is >800 lines, read it in chunks but report read_range honestly — do NOT skim. If you found things outside `files_claimed` that belong to this subsystem, note them under "Open questions" with the exact prefix `Orphan-sweep candidate: <path>` (capital O, full prefix, file path first — not a topic tag), followed by a one-line reason they belong here. One entry per file. Don't expand scope unilaterally, but DO surface them so a later orphan-sweep phase can pick them up via grep. Existing doc at `<existing_doc>` is for cross-reference only — do NOT edit it. Report PASS or FAIL with reason. If you need to report back something to update our working protocol, say so outside the report.
```
- **Why:** g04-navigation produced four nominations in the lowercase `orphan-sweep: <topic>:` form, which the future grep-based worklist builder would miss. The intent of review #3 Edit 2 was a greppable artifact; tightening to exact-case + path-first preserves that guarantee. Also makes "one entry per file" explicit so multi-file aggregation lines like "components/Histogram.tsx, components/Hint.tsx, components/StatusDot.tsx …" (e13:232) get split into per-file entries.
- **Risk:** very low. Existing surveys with canonical prefix are unaffected. g04's lowercase entries remain in the survey as-is — re-running a triage pass to re-tag them is cheap and only needed when the orphan-sweep phase fires.

### Edit 2 — Note the YAML block-syntax fix in the section preamble for discoverability

- **Where:** Section 3 "Action: audit or create", just above the survey shape block.
- **Current text** (LOOP_PROMPT.md Section 3, around line 39):
```
> Read every file in `<files>` top-to-bottom. To record current blob SHAs, run `node plans/doc-audit-state/scripts/blob-sha.mjs <path1> <path2> …` — the script accepts one or more paths and prints `<path><TAB><sha>` per line (single-arg form still works and prints the bare sha). Produce a survey doc at `plans/doc-audit-state/survey/<id>.md` with this exact shape:
```
- **Proposed text:**
```
> Read every file in `<files>` top-to-bottom. To record current blob SHAs, run `node plans/doc-audit-state/scripts/blob-sha.mjs <path1> <path2> …` — the script accepts one or more paths and prints `<path><TAB><sha>` per line (single-arg form still works and prints the bare sha). Produce a survey doc at `plans/doc-audit-state/survey/<id>.md` with the exact shape below. The `files:` block MUST use block YAML (one `- path:` entry per file with two-space indented `blob_sha:` and `lines_read:` keys) — flow syntax (`{ path: ..., blob_sha: ... }`) is rejected by the orchestrator's frontmatter parser and will record zero files:
```
- **Why:** the block-syntax rule was added inline at line 52 inside the shape block, but the rejection consequence (zero files recorded) isn't surfaced. Mentioning it once above the block makes it more likely an agent reads it before producing the file list rather than after. Cheap insurance against a re-occurrence of the g04 trap on a different shape variant.
- **Risk:** none structural; pure restatement of an existing rule with consequence noted. The inline note inside the shape block stays where it is.

## Feedback items to mark wont-fix

(none — `protocol-feedback.json` shows all four entries already in terminal state: f-001/f-002 `wont-fix`, f-003/f-004 `addressed`. Nothing new opened this window.)

## Feedback items to keep open

(none — no open feedback at tick start. The YAML-flow issue was caught and fixed inline by the orchestrator without ever being filed.)

## Notes for the human

- **The orphan-sweep worklist is now real.** With 16 canonical nominations across 20 surveys (and ~17 more iterations to go before Phase 1 ends), expect the Phase 1.6 orphan-sweep input to be ~40-60 file nominations. That's tractable — single consolidation pass per target subsystem, no need to design a phase-internal triage loop unless the count blows past 100.
- **Three planner-level items still stacked from review #3** (re-stating since they're still load-bearing for Phase 2 kickoff): (1) f-001 / e06 / e07 rewrite-vs-migration-target call for aspirational engine docs (now joined by e13's `05_Shared_UI.md` rewrite recommendation — that's four aspirational docs); (2) e07's "no uniform plugin contract" cross-subsystem note; (3) the Phase 1.6 orphan-sweep design. e13's "rewrite" recommendation reinforces the pattern — Phase 2 needs an explicit decision tree for surveys that recommend rewrite vs migrate-target vs minor-edits.
- **Cosmetic `audited_at` drift continues** (g04 uses date-midnight `T00:00:00Z`). Still not worth a fix; progress.json holds canonical timestamps.
- **Review-tick cadence note.** This tick fires on the iterations-≥5 trigger (no open feedback). Triggers continue to earn their keep — review #3 fired on iterations, the open-feedback trigger has not yet fired by itself but came close at ≥2. No proposal to change trigger thresholds — load-bearing per constraints.
- **For the next review (#5), watch:** whether Edit 1 above lands enough discipline that the canonical-prefix count is 100% in the next iteration window, or whether agent variability needs a stronger signal (e.g. an example block showing one canonical entry verbatim).
