---
review_id: review-2026-05-20-10
generated_at: 2026-05-20T02:10:00Z
feedback_items_reviewed: []
iterations_window: [q-016, q-018, q-019, q-020, q-021, q-022, q-025, q-026, q-027, q-028]
---

## Patterns observed

- **Relaxed ≥10 cadence (review #8 Edit 1) earning its keep on a second cycle.** This is the second full ≥10-window since the relaxation. Zero `protocol-feedback.json` entries opened across q-016..q-028; all 10 followups returned PASS. Window wall-clock per progress.json: q-016 start 01:31:25Z → q-028 end 02:08:09Z = ~37 min for 10 iterations including spawn/orchestration = **~3.7 min/iter** (vs ~4.8 min/iter last window, ~3-4 min/iter projected in review #7). Faster than projection, no quality loss visible. Under the old ≥5 cadence two extra review ticks would have fired in this window for net-zero diagnostic value; the ≥10 cadence preserved ~10-14 min of wall-clock and one full agent-context spend. Cumulative across cycles 1 and 2 of the relaxation: ~20-28 min and 2 agent contexts saved with zero missed signal — argument strengthened, not weakened.
- **File-consultation discipline solid; mean inside the ≤8 budget with one tolerated overshoot.** Per-iter counts: q-016=3, q-018=2, q-019=4, q-020=5, q-021=4, q-022=4, q-025=4, q-026=5, q-027=4, q-028=5. Mean **4.0** files, max **5**, zero budget exceedances (vs review #9's window where q-013 went 9 — single instance tolerated). The ≤8 budget is healthy headroom for this question shape. Agents are not padding.
- **Phase 2 carry-in yield is unusually high this window — 8 of 10 followups produced module-doc-shaping signal, not just clarifications.** Spot-checks:
  - **q-018 (PanelManifest API):** full type-surface audit of the engine-core *producer* paired against a02-panels-layout's *consumer* audit. The 9-variant `PanelItem` union, the `peekLiveStore()` pre-boot-freeze workaround, and the `applyPanelManifest` active-tab resolution rules are all load-bearing and undocumented except in source comments. Phase 2's panels module doc must absorb these.
  - **q-019 (presetFieldRegistry):** full 5-method API surface, freeze semantics, exhaustive 5-site consumer list, AND a production-only bug (see next pattern). Module-doc gold.
  - **q-022 (runTicks delta units):** confirms callers/handlers agree on seconds; identifies two stale doc sites (`docs/engine/01_Architecture.md:83` "deltaMs" wording, `docs/engine/04_Core_Plugins.md:206` synthetic `16.67` example) for the doc-rewrite pass.
  - **q-026 (backingOnly enforcement):** pinpoints the single enforcement site, names the symmetric opt-in contract (backing via flag, declaration via `builder.addUniform`), flags a one-line JSDoc gap at `engine/UniformSchema.ts:19-21`.
  - Roughly 8/10 followups in this window produced concrete Phase 2 anchors (drift items, undocumented contracts, cross-subsystem cross-cuts). vs ~6/10 last window estimated from review #9 spot-checks. This is the institutional-knowledge yield Sub-phase B was designed for and it is *accelerating* now that the residual questions are getting into engine-internals territory.
- **Bug surface this window: one real production bug (q-019), one stale-doc-only finding (q-022), several JSDoc/code-comment gaps.** q-019's `DuplicatePresetFieldError` on app-gmt's double-registration path is genuinely production-affecting — flagged in progress.json with `phase2_priority_finding`. In DEV the duplicate branch warns + replaces (HMR-friendly); in prod it throws. App-gmt's `engine/plugins/camera/presetField.ts` runs first via side-effect import; `defaultPresetFields.registerDefaultPresetFields()` runs second from `createFeatureSlice.ts:24`. In a prod build this would crash boot. No other followup in the window surfaced a real bug — q-022's "milliseconds in docs" is a doc bug, q-026's JSDoc gap is documentation-completeness, q-018's pre-boot freeze is intentional design. So: **1 production bug + 1 doc bug + ~3 JSDoc-completeness gaps** is the bug-finding rate this window. The q-019 hit validates that Sub-phase B is not just a doc-prep exercise — it doubles as a fragility audit. Reasonable expectation: ~1 production bug per 10-15 followups going forward.
- **Cadence at ≥10: extending further is plausible but not justified yet.** Evidence: two consecutive ≥10 windows with zero protocol feedback, zero shape variance (this window — vs 2/10 deviants last window), tighter wall-clock (3.7 vs 4.8 min/iter). The ≥10 cadence is comfortably overserving. BUT: q-019's production-bug surface is exactly the kind of finding that benefits from a review-tick mention so it doesn't slip past Phase 2 planning — and the reviewer (this tick) is the natural place to flag it. Extending to ≥15 would push q-019 visibility by ~5 more iterations. Net: hold at ≥10 for one more cycle (the next review fires after ~q-040). If cycle 3 also shows zero feedback, zero shape variance, and continued high yield, then ≥15 becomes defensible. **No edit proposed this tick.**

## Proposed LOOP_PROMPT.md edits

(none — the loop is healthy. Review #8's Edit 1 continues to validate; review #9 correctly held; this window adds a second clean cycle. Cadence extension to ≥15 is *defensible* on wall-clock grounds but not yet *justified* — the bug-surface pattern (q-019) argues for keeping the human-visibility cadence where it is. Revisit at review #11 if cycle 3 is also clean.)

## Feedback items to mark wont-fix

(none — `protocol-feedback.json` still shows all four entries in terminal state from prior reviews: f-001/f-002 `wont-fix`, f-003/f-004 `addressed`. No new entries opened this window.)

## Feedback items to keep open

(none — no open feedback at tick start.)

## Notes for the human

- **q-019 is a real production bug, not a doc gap.** `app-gmt`'s preset-field registration path double-registers `cameraRot`/etc. via two routes that fire in known order: (a) side-effect import of `engine/plugins/camera/presetField.ts` runs first at module load, (b) `defaultPresetFields.registerDefaultPresetFields()` runs second from `createFeatureSlice.ts:24` during store creation. In DEV both routes hit `PresetFieldRegistry.register()`'s duplicate branch (lines 63-78), which warns + replaces — HMR-safe. In a **production build** the same path **throws `DuplicatePresetFieldError`** at boot. As-shipped this would crash app-gmt's prod build the first time the store materialises. Two ways to address: (i) make the prod-mode duplicate path warn-and-replace like DEV (i.e. unify the policy), or (ii) gate one of the two registrars so only one fires. The followup at `plans/doc-audit-state/survey/_followups/q-019.md` has the full file:line evidence. Worth surfacing to a maintainer before Phase 2 module-doc writing begins, since the fix may invalidate part of the doc text.
- **Phase 2 carry-in inventory growing — consider a running anchor list.** Three windows in (reviews #8/#9/#10), Sub-phase B has surfaced at least:
  - q-013: `set${FeatureId}` action-naming convention + `${featureId}.${paramKey}_<axis>` track-id convention (string contracts not type-enforced).
  - q-014: two-registry boot pattern (`featureRegistry` + `presetFieldRegistry`, both frozen post-population).
  - q-018: 9-variant `PanelItem` union + `peekLiveStore()` pre-boot-freeze workaround.
  - q-019: `presetFieldRegistry` 5-method API + freeze semantics + 5-consumer surface + production-only `DuplicatePresetFieldError` surface.
  - q-022: `runTicks(delta)` is seconds, not ms; two stale doc sites named.
  - q-026: `backingOnly` enforcement at exactly one site; symmetric opt-in contract for backing vs declaration.
  - q-001 / q-002 (older window): `_isSceneReady` dead param; `isHydrated` misnomer; silent 30s timeout in `GmtRendererTickDriver.tsx:90-94`.
  - When Phase 2 begins, the writer for each affected module doc should cross-reference its source followup(s) by `q-NNN` ID. The followup docs themselves are the durable artefact — they survive the loop and outlast `open-questions.json`.
- **Sub-phase B progress: 22 of 76 primary answered (~29%), still under the projected ~5-hour total wall-clock for primaries.** At ~3.7 min/iter the remaining 54 primary entries project to ~3.3 hours; the 43 low-priority entries project to another ~2.7 hours if all are worked. Both still comfortably inside the 200-iteration hard cap. Next review tick (#11) will fire at q-040 or thereabouts (10 more iterations, allowing skipped IDs from duplicate/low triage).
- **Watchlist for the next review (#11):**
  - (a) Token-burn >40k: **not observed in this window** (max files = 5; all PASS). Cost-guard remains untouched.
  - (b) `unresolvable` disposition: **still not exercised** in Sub-phase B. Every question this window answered PASS. The cost-guard path is genuinely cold — if a question hits it for the first time, double-check the post-condition (status="unresolvable", reason logged, no retry).
  - (c) Followup frontmatter shape variance: **zero deviants this window** (vs 2/10 last window — q-003, q-008). Two consecutive clean windows would be evidence the agent self-stabilised on the canonical shape; this window is the first of those. Watch for a repeat at #11 before declaring the variance pattern dormant.
  - (d) New: **bug-surface rate.** q-019 is the first production-affecting bug discovered by Sub-phase B (vs Phase 1 surveys which found code-bug f-002 etc.). If #11 surfaces another production bug, that's signal Sub-phase B's audit value is meaningfully higher than the "prep-for-Phase-2" framing implies and the loop's framing in `LOOP_PROMPT.md` could acknowledge that explicitly.

PASS.
