---
review_id: review-2026-05-20-11
generated_at: 2026-05-20T02:55:00Z
feedback_items_reviewed: []
iterations_window: [q-029, q-032, q-033, q-034, q-036, q-037, q-038, q-040, q-041, q-042]
---

## Patterns observed

- **Wall-clock per iteration trending down again — but the absolute floor is the agent dispatch itself.** Window span per progress.json: q-029 start 02:13:14Z (inferred from prior tick + ~90s wakeup) → q-042 end 02:49:52Z ≈ ~37 min for 10 iterations including orchestration = **~3.7 min/iter**, identical to the prior window. The two short pointer-back iterations (q-033 ≈ 1m6s, q-036 ≈ 44s investigate-to-answer per progress.json) absorbed about half a full iteration each, but the saved wall-clock was spent on the deeper full-investigation iterations (q-029 ≈ 1m43s, q-037 ≈ 4m, q-041 single file but 4m). The mean stays at 3.7 — the ≥10 cadence is the durable optimisation, not the cadence-relaxation-plus-short-pointers combo. No new evidence to push to ≥15 yet (see last bullet).

- **Phase 2 carry-in density holds at ~80% even as the question pool moves into pointer-back territory.** Spot-check yield (concrete Phase-2-shaping signal vs pure clarification):
  - q-029 (UniformSchema dedup): documents **two** silent-collision paths (base-vs-feature filter at `engine/UniformSchema.ts:77-83`, plus feature-vs-feature last-wins via `UNIFORM_DEFAULTS` reduce at `:85-88`). Surfaces an undocumented naming convention (`uPT*`, `uLight*`, `uModular*` prefixes) and proposes a verbatim doc addendum. High Phase-2 value — module doc for shader-builder must absorb both paths.
  - q-032 (raymarching plugin sections): 17-position assembly contract + variant-gate behaviour. Hard architectural anchor.
  - q-033 / q-036 (pointer-backs): low *new* signal but high *cross-link* signal — they explicitly map question text to the prior survey/followup that resolved it. Useful for Phase 2 writers walking from question → answer.
  - q-034, q-037 (BloomPass composition): JS-bridge + GLSL-injection split is the exact pattern Phase 2's render-pipeline module doc needs.
  - q-040 (ViewportAdaptiveConfig): catches **two wrong field-name guesses** in the source survey (`activityGraceMs` vs `graceMs`, `engageOnAccumOnly` vs `gateOnAccumOnly`) and a **missing 6th field** (`minQuality`). Without q-040 the e06 module doc would have shipped wrong field names.
  - q-041 (viewportSlice ownership): exhaustive method-by-method ownership map + a correction (`setAdaptiveSuppressed` is NOT in this slice). Module-doc gold.
  - q-042 (small one-file followup): direct answer with file:line.
  - **~8/10 iterations producing actionable Phase-2 signal**, same density as last window. The "trending down" hypothesis from the brief is **not** supported — pointer-backs do not represent decay, they represent triage hygiene (see Sub-A retrospective below).

- **Frontmatter shape variance is back, not dormant.** Five of ten followups in this window use the non-canonical frontmatter shape: q-029, q-037, q-038, q-040 use `id` / `parent` / `status` / `verdict` / `files_read`; q-036 uses `subsystem_id` instead of `source_subsystem` (and lacks `files_consulted` entirely — the answer is a pointer-back paragraph with no consulted-files list). Canonical (per LOOP_PROMPT.md Sub-phase B brief): `question_id`, `source_subsystem`, `investigated_at`, `files_consulted` with `path` + `blob_sha`. Window-by-window:
  - review #9 window (q-001..q-015): 2/10 deviants (q-003, q-008)
  - review #10 window (q-016..q-028): **0/10**
  - this window (q-029..q-042): **4-5/10**
  - The "agent self-stabilised" hypothesis from review #10 is wrong. The clean window was the outlier, not the trend. The deviants do not appear to break anything downstream (orchestrator records `answered` regardless, coverage entries are appended from the brief's instruction rather than the frontmatter), but if a Phase 2 indexer ever wants to walk `_followups/*.md` by frontmatter, it will fail on 4-5 of every 10 entries. Worth a defensive tweak — see Proposed edits.

- **Pointer-back iterations are real triage signal that Sub-phase A could have caught.** Both q-033 and q-036 had explicit phrases in the question text: "reserved for g02" (q-033) and "out of scope here" (q-036). These weren't ambiguous — the surveyor literally said "ask the other subsystem". The Sub-A triage agent's job is to fill `target_hint` (1-2 words, based on question text alone) and dedupe near-duplicates. It is NOT currently asked to detect "this question explicitly defers to another subsystem's survey". Adding that detection would let those questions be marked at triage time (e.g. `status: "covered-by"` with a `covered_by: <subsystem-id>` field, or `priority: "low"` with a `defer_to: <subsystem-id>` annotation) and either auto-answered with a one-line pointer at investigate-time or skipped entirely if the deferred-to subsystem's survey actually covers it. Counterfactual cost: q-033 + q-036 together burned ~1.5 min of agent time + 2 iterations of orchestrator dispatch (~3 min wall-clock). Across the remaining ~87 pending questions, if 10-15% are similar explicit-defer cases (plausible — the surveys often hand off cross-subsystem questions), that's ~10-13 iterations that could collapse to triage-time annotations. **This is a Sub-A retrospective tweak** — a useful one, and the kind the brief explicitly invited me to flag.

- **Bug surfaces this window: zero new production-only bugs beyond q-019.** Window findings are documentation gaps (q-029 missing convention doc), correctness gaps in surveys themselves (q-040's wrong field names — caught by the followup, never reaches the module doc), and pure pointer-backs (q-033, q-036). The expectation from review #10 ("~1 production bug per 10-15 followups") is not realised in this window — running rate is now 1 in 22 (just q-019). One window of zero is not a trend; revisit at #12. The bug-surface watchlist item from review #10 stays open.

- **Cadence at ≥10 still earning, no extension to ≥15 justified by this window.** Same argument as #10: zero protocol-feedback entries opened, but shape variance is back at 50% of iterations and a Sub-A retrospective just emerged (this tick) that wouldn't have surfaced any sooner at ≥15. The ≥15 cadence would have pushed both the frontmatter-variance finding and the pointer-back-triage finding back by ~5 iterations, with no offsetting cost saving (the review tick itself is the only cost, and it's <5 min). Hold at ≥10 for one more cycle.

## Proposed LOOP_PROMPT.md edits (Sub-A retrospective tweak + frontmatter defensiveness)

### Edit 1 — Sub-A: detect explicit-defer questions

- **Where:** Section "### Sub-phase A — Gather (one iteration, runs once)" — the triage agent brief (the bulleted list of fields to fill).
- **Current text:**
  ```
  > Read `plans/doc-audit-state/open-questions.json`. For each question:
  > - Fill `target_hint` with a 1-2 word hint (file path, module name, or topic) of what to investigate. Do not read code yet — base the hint on the question text alone.
  > - Identify near-duplicate questions (same target, same uncertainty). Mark the later one `status: "duplicate"`, `duplicate_of: "q-XYZ"` (the earlier).
  > - Mark `priority: "low"` for questions that are obviously curiosity-only ("why did the author choose X over Y?") and not architecturally load-bearing. Keep `priority: "primary"` for anything that would change a module doc's structure.
  > Write the updated JSON back. Report: counts of primary, low, duplicate, total.
  ```
- **Proposed text:**
  ```
  > Read `plans/doc-audit-state/open-questions.json`. For each question:
  > - Fill `target_hint` with a 1-2 word hint (file path, module name, or topic) of what to investigate. Do not read code yet — base the hint on the question text alone.
  > - Identify near-duplicate questions (same target, same uncertainty). Mark the later one `status: "duplicate"`, `duplicate_of: "q-XYZ"` (the earlier).
  > - Detect explicit-defer questions — those whose text contains phrases like "reserved for <id>", "out of scope here", "see <other-subsystem>", "covered by <id>", or any clear signal that the surveyor handed the question off to another subsystem. For these, set `target_hint` to `defer:<subsystem-id>` (e.g. `defer:g02`) so the Sub-B investigator knows to produce a short pointer-back answer (cite the deferred-to survey/followup, no fresh code reads) instead of re-investigating. Keep `status: "pending"` and `priority` as normal — these still need an answer doc for cross-link purposes.
  > - Mark `priority: "low"` for questions that are obviously curiosity-only ("why did the author choose X over Y?") and not architecturally load-bearing. Keep `priority: "primary"` for anything that would change a module doc's structure.
  > Write the updated JSON back. Report: counts of primary, low, duplicate, defer, total.
  ```
- **Why:** Pattern noted above — q-033 and q-036 were explicit-defer questions the Sub-A triage missed, leading to ~3 min of avoidable orchestrator + agent time. The remaining ~87 pending questions likely include ~10-15 more of the same shape. The edit is a clarification to the triage brief (does not change resumption logic, iteration step structure, or priority sort).
- **Risk:** **DO NOT APPLY THIS TICK.** Sub-A has already run (per the brief's constraint, `open-questions.json` is load-bearing for resumption and must not be modified mid-Sub-B). This edit would only take effect if a future loop runs Sub-A again, which only happens if `open-questions.json` is deleted (e.g. a different audit). I am surfacing it as a documented learning for the next loop or for a human who chooses to apply a one-shot re-triage pass mid-Sub-B. **Skip the Edit-tool apply step for this tick** and let the human decide. Logging the proposal here so it isn't lost.

### Edit 2 — Sub-B: tighten frontmatter shape enforcement

- **Where:** Section "### Sub-phase B — Resolve (one iteration per pending question)" — the agent brief, the frontmatter-shape paragraph.
- **Current text:**
  ```
  > Investigate. Read only the files you need (Glob/Grep/Read). Stay tight — aim for ≤8 file reads total. Produce an answer doc at `plans/doc-audit-state/survey/_followups/<id>.md` with this EXACT frontmatter shape (use these field names, NOT alternatives like `id` / `parent_survey` / `target` / `status` / `verdict`):
  ```
- **Proposed text:**
  ```
  > Investigate. Read only the files you need (Glob/Grep/Read). Stay tight — aim for ≤8 file reads total. Produce an answer doc at `plans/doc-audit-state/survey/_followups/<id>.md` with this EXACT frontmatter shape. Use **these four field names verbatim**: `question_id`, `source_subsystem`, `investigated_at`, `files_consulted`. Do NOT use `id`, `parent`, `parent_survey`, `subsystem_id`, `target`, `status`, `verdict`, `files_read`, `target_hint` — those are alternative names that have appeared in past agent output and the orchestrator's resumption logic will not recognise them. The `files_consulted` block must be present even for pointer-back answers (cite the survey or prior followup you're pointing at, with its blob_sha or the literal string `audit-snapshot` if you genuinely did not re-fetch the sha):
  ```
- **Why:** Frontmatter variance is back at 5/10 this window after a clean prior window (see Patterns bullet 3). The current brief lists `id` / `parent_survey` / `target` / `status` / `verdict` as wrong, but agents still emit them (and now also `parent`, `subsystem_id`, `files_read` — variants not yet enumerated). Tightening the list and explicitly calling out pointer-back answers' obligation to include `files_consulted` (q-036 omitted it) should reduce variance.
- **Risk:** Low. Pure brief-clarification, no behavioural change. The orchestrator already tolerates the variants (coverage entries are recorded from the brief instruction, not parsed from the followup's frontmatter), so this is purely about preventing downstream-indexer breakage when a Phase 2 tool wants to walk `_followups/*.md` programmatically.

### Application plan for this tick

- **Edit 1: DO NOT APPLY.** Surface to human via Notes section; honours the "DO NOT modify open-questions.json mid-Sub-phase-B" constraint.
- **Edit 2: APPLY.** Pure brief clarification. Apply via Edit tool below.

## Feedback items wont-fix / keep open

(none — `protocol-feedback.json` still shows all four entries in terminal state from prior reviews: f-001/f-002 `wont-fix`, f-003/f-004 `addressed`. No new entries opened this window.)

## Notes for human

- **Sub-A retrospective: explicit-defer questions are a missed triage signal.** q-033 ("reserved for g02") and q-036 ("out of scope here") were resolvable at triage time as `defer:<subsystem-id>` annotations rather than full Sub-B iterations. Counterfactual saving on this window alone: ~3 min wall-clock + one agent context. If you want to extract more of that saving across the remaining ~87 pending questions, you could:
  - (a) Run a one-shot re-triage pass over `open-questions.json` with the prompt: "Find all pending entries whose question text contains 'reserved for', 'out of scope here', 'see g0X / e0X / a0X', 'covered by'. For each, append `defer:<subsystem-id>` to its target_hint." — then either (b1) auto-answer them as pointer-backs (write minimal `_followups/<id>.md` citing the deferred-to survey, mark answered) or (b2) keep them pending but with the hint so Sub-B agents go straight to the pointer-back template. Either path collapses ~10-15 future iterations to ~0-2.
  - Edit 1 above documents the upstream fix (next time Sub-A runs from scratch). Edit 1 was NOT applied this tick per the load-bearing-state constraint.
- **Phase 2 carry-in inventory keeps growing.** Adding to the running list from review #10:
  - q-029: silent uniform-name collisions in two paths (base-vs-feature filter, feature-vs-feature last-wins). Naming-convention documentation gap.
  - q-032: 17-position shader-assembly contract + variant-gate "inject every feature regardless of enabled state" pattern.
  - q-034, q-037: BloomPass two-site composition (JS uniform bridge + GLSL `+=` blend). No screen-blend, no Reinhard.
  - q-040: e06 survey had wrong field names for `ViewportAdaptiveConfig` (`graceMs`/`gateOnAccumOnly` → `activityGraceMs`/`engageOnAccumOnly`) AND missed `minQuality`. **The Phase 2 module doc must use q-040's canonical list, not the e06 survey's guess.**
  - q-041: `viewportSlice.ts` ownership map + `setAdaptiveSuppressed` NOT in this slice (lives elsewhere).
  - This window confirms the q-019-style "followup catches survey error" pattern is recurring (q-040 is the second instance). Phase 2 writers should consult `_followups/q-NNN.md` BEFORE trusting any survey field-name list verbatim — followups are the more reliable source where they overlap.
- **Sub-phase B progress: 32 of 76 primary answered (~42%), 87 pending overall (43 low + 44 primary remaining after 32 answered).** Wait — recomputing: triage reported 76 primary + 43 low + 1 duplicate = 120 total. 32 answered + 87 pending + 1 duplicate = 120. If the 32 answered are all primary (likely — Sub-B picks primary first), then **32/76 primary done = 42%**, **44 primary + 43 low pending**. At ~3.7 min/iter, primaries project to ~2.7 more hours; low project to ~2.7 hours after that. Total Sub-B remaining: ~5.4 hours wall-clock if all worked. Comfortably under the 200-iteration cap (87 remaining + 32 done = 119 < 200). If Edit 1's re-triage idea were applied as a one-shot human action, ~10-15 of those iterations would collapse, saving ~40-55 min.
- **Watchlist for the next review (#12, fires after ~q-053):**
  - (a) Bug-surface rate after the zero-bug window. If #12 also has zero, the q-019 hit was likely a one-off and the "1 per 10-15" projection from review #10 was too optimistic. If #12 has 1+, the rate stabilises at ~1 per 20-25 — still genuinely useful.
  - (b) Frontmatter variance after Edit 2 lands. If next window drops back to ≤1/10 deviants, Edit 2 worked. If still 4-5/10, the brief tightening was insufficient and an orchestrator-side validator might be needed (post-Sub-B, since modifying anything orchestrator-side mid-Sub-B violates the load-bearing constraint).
  - (c) Pointer-back rate. If next window has 0-1 pointer-backs (i.e. the deferred-to subsystems are mostly handled), the Sub-A retrospective is moot. If 3+ pointer-backs in window #12, the Edit-1 re-triage one-shot becomes more attractive as a manual action.
  - (d) `unresolvable` disposition: still cold (32 PASS, 0 unresolvable). Phase will likely complete without exercising it.

PASS.
