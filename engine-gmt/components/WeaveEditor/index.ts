/**
 * WeaveEditor — the user-facing weaver (ADR-0089 P3). Host-agnostic: first
 * mounted as a tab in the Import Mandelbulb3D modal; panel promotion (P4) is a
 * re-mount of the same pane.
 */
export { WeaveEditorPane } from './WeaveEditorPane';
