# Interaction Session — multi-session rollout plan

Operational tracking for implementing **ADR-0061** (a single `InteractionSession` primitive as the source of truth for "is the user interacting?"). This fixes the remaining accumulation/adaptive issues (sliders never reach target FPS, ~1s engagement lag) by replacing ~6 fragmented interaction inferrers with one declared signal.

- **Design / spec:** `docs/adr/0061-interaction-session-single-source-of-truth.md` (5 review angles folded in: architecture / correctness / migration / alternatives / performance).
- **Background + current state:** memory `project_accumulation_reset_overhaul`.
- **Already done:** RC A (delta-based modulation reset) + interim adaptive fixes — commit `f8fa698`. ADR hardened — `9c6c301`, `7efd0c9`.

## How to run this across sessions (operating protocol)
**Branch:** all implementation lives on `feat/interaction-session` (branched off `feat/context-loading-protocol`; ADR-0061 is **Accepted**). The design-doc commits already on the parent branch carry forward.
1. **One phase per session.** Read ADR-0061 + this doc + memory `project_accumulation_reset_overhaul` first.
2. Do the phase; **update its Status + Notes here**; commit docs + code together (Co-Authored-By trailer). Each phase is independently shippable + revertible per the ADR.
3. **Order (edges E1–E5 are DONE):** P2 → P3a → **go/no-go checkpoint** → P3b → P4 (additive) → P5.
4. **Behavior changes land behind per-consumer flags** (off through P3b; P4 flips them with the accum-drop proxy still running in parallel). **Visual verification is the user's** (renderer not observable headlessly) — I land flagged/inert code + the objective instruments; the user does the visual pass at each behavior-changing checkpoint.
5. **Verification — three tiers, cheapest first:** (a) **logic** — the pure-machine tsx test (`debug/test-interaction-session.mts`), deterministic, pre-push gate; (b) **latency + outcome** — the P2 harness records a baseline (input→downscale latency + did quality/fps reach target, via store values not pixels); P4 re-runs + diffs, baseline is a hard gate; (c) **behavior/feel** — user visual pass; the overlay's **divergence counter** (session vs. accum-drop) makes the parallel run objective, not vibes — but note that counter + the adaptive-scale row are only meaningful once a consumer is live (P4); they were correctly removed from the P3a-inert overlay.
   - **Env gotcha (P3a):** `debug/engine-gmt-smoke.html` is absent from the checkout — vite serves a fallback — so "smoke clean" boot-verification runs against the real `app-gmt.html` entry (same Navigation / tick-driver / gizmo code). Use that for boot checks until the smoke harness is restored.

---

## Edge investigations — close before/with P2 (may revise the ADR)

### E1 — Touch / multi-pointer / pinch  · Status: DONE (2026-05-30)
Do touch drags already flow through the same pointer handlers (→ free session wiring), or are **pinch-zoom / two-finger pan** separate gestures needing their own begin/end? Read `engine-gmt/navigation/*` (pointer vs touch/gesture handling), `hooks/useMobileLayout.ts`, any pinch/gesture handler. **Output:** which mobile gestures need explicit session wiring → fold into ADR producers + P3.

**Notes (resolved → ADR "Touch / multi-pointer / pinch" subsection):**
- **No new source token, no separate touch producer.** Touch is covered by the producers P3a/P3b already wire.
- **Camera touch (1-finger rotate, 2-finger pinch-zoom, 2-finger pan)** all flow through drei `<OrbitControls>` (`Navigation.tsx:1353`, `touches` at `:1372`), which fires ONE `onStart`/`onEnd` pair (`:1373`/`:1395`) for the whole gesture. Wiring `begin/endInteraction('camera')` there — a P3a site already in scope — covers all of touch. The 1→2→1-finger transition does **not** re-fire onStart, so there's no multi-pointer double-begin. Already the de-facto signal today via `isOrbitDragging` → `isCurrentlyActive` (`Navigation.tsx:1096`).
- **Pinch ≠ wheel:** mouse wheel = discrete `poke` (`useInputController.ts:183`); touch pinch = part of the continuous `camera` session. Don't model pinch as a poke.
- **Pickers / drawing / gizmo / sliders / scrub** all ride shared PointerEvents (`onPointerDown/Move/Up`) → touch is free once those are wired. Mouse-only: custom wheel zoom (poke) + Fly-mode mouse-look (blocked on touch, `useInputController.ts:237`).
- **Safety delta → ADR mitigation #1 widened:** touch interruption fires `pointercancel` (not `pointerup`); custom drag handlers (`Navigation` custom orbit/middle-drag, `useInteractionManager`, `DrawingOverlay`) listen for `pointerup` only and would strand a session — `useInteractionDrag` must supply `onPointerCancel` + `onLostPointerCapture`. drei-driven `camera` is exempt (drei fires `onEnd` on its own pointercancel).
- **`hooks/useMobileLayout.ts` is layout-only** (no input handling) — not a producer.

### E2 — Export / bucket / video-export suppression  · Status: DONE (2026-05-30)
What should a session do during a bucket render / video export? Confirm `adaptiveSuppressed` already neutralizes adaptive; ensure sessions don't perturb deterministic export frames. Read `engine-gmt/engine/GmtBucketHost.ts`, `worker/WorkerExporter.ts`, the `adaptiveSuppressed` path. **Output:** a suppression rule (ignore sessions while `isExporting`/`isBucketRendering`) → add to ADR.

**Notes (resolved → ADR "E2 — Export / bucket suppression"):** Sessions are export-safe by construction.
- **Write-side invisible to export:** begin/end/poke = refs + one edge boolean, nothing new crosses to the worker; export frames consume no interaction signal.
- **Adaptive already doubly gated:** resize block skipped while `isExporting||isBucketRendering` (`UniformManager.ts:97`) AND `adaptiveSuppressed`→scale 1.0 (`:127`→`AdaptiveResolution.ts:221-228`). `adaptiveSuppressed` set by BucketRenderPanel + render-dialog while mounted.
- **Hold already gated:** `compute()`/`update()` early-return on `isExporting` (`FractalEngine.ts:553`/`:493`); bucket never holds (`:563`); `selectMovementLock` locks camera (`engineStore.ts:464`).
- **Rule:** keep the `!isExporting && !isBucketRendering` gate at the GMT derivation site (`GmtRendererTickDriver.tsx:256`), NOT in the core slice (stays domain-agnostic). P4 must preserve it if wiring a new determinism-affecting consumer; existing adaptive+hold guards already cover it.

### E3 — Interaction-state consumer audit (the expanded phase 2.5)  · Status: DONE (2026-05-30)
Map ALL readers of interaction-ish state and decide migrate-vs-keep for each. **Output:** per-consumer decision → drives P4; resolves ADR open-Q #6.

**Full cited reader table** (decisions summarized in ADR "E3"):

| Reader | file:line | Does | Decision |
|---|---|---|---|
| Adaptive input | `UniformManager.ts:118` | passes `isGizmoInteracting\|\|cameraInUse` → `tickAdaptiveResolution` | **MIGRATE** → `session.isInteracting()` |
| GMT cameraInUse derivation | `GmtRendererTickDriver.tsx:256` | ORs `isCameraInteracting\|\|isPlaying\|\|isScrubbing` into renderState | **MIGRATE** (+ keep `isSceneAnimating` for playback) |
| Gizmo (dual state) | engine `isGizmoInteracting` (`GmtRendererTickDriver.tsx:257`) + `uiSlice` `isGizmoDragging` | unsynchronized dual flag | **MIGRATE** → unify under `gizmo` source |
| Render idle-pause | `FractalEngine.ts:557` | `isPaused && now-lastInteractionTime>1000` → early-return | **MIGRATE** → `session.isIdle(1000)` |
| `selectIsGlobalInteraction` | `engineStore.ts:444` | `isUserInteracting \|\| interactionMode!=='none'` | **MIGRATE** → `session.isInteracting() \|\| mode!=='none'` |
| PauseControls | `engine/plugins/topbar/PauseControls.tsx` | reads the selector for button tone | **MIGRATE** (follows selector) |
| HUD auto-fade | `HudOverlay.tsx:63` | fades crosshair/pill on `isCameraInteracting` | **MIGRATE w/ filter** → `isInteracting({only:['camera','scrub']})` |
| Undo coalescing | `historySlice.ts:161/166/206` | transaction snapshot/diff/DPR-restore/1500ms camera debounce | **KEEP SEPARATE** (transaction concept; wired at same producer sites) |
| Camera-block | `selectMovementLock` `engineStore.ts:463` (+ drawing `blockCamera`) | permission gate (incl. `isExporting`/`isBucketRendering`/`isGizmoDragging`/`interactionMode`) | **KEEP OUTSIDE** (orthogonal axis; repoint gizmo check in P5, don't drop) |
| `mouseOverCanvas` | `AdaptiveResolution.ts:275-281`, computed `GmtRendererTickDriver.tsx:258` | dead in decision path | **REMOVE** (P4/P5) |
| accum-drop activity | `AdaptiveResolution.ts:240` | `accumCount<prev` infers activity | **REMOVE for GMT** (keep for sibling via `gateOnAccumOnly`) |
| `gateOnAccumOnly` | `AdaptiveResolution.ts:284`, `viewportSlice.ts:180` | sibling-app config | **KEEP** |
| `selfResized` | `AdaptiveResolution.ts:244` | adaptive-internal | **KEEP** |

**Open-Q #6 RESOLVED — undo coalescing stays separate.** It's a transaction-boundary marker (verified: `endParamTransaction` snapshots+diffs params, restores DPR, pushes one undo entry), not "is active". The session is wired at the same begin/end producer sites but doesn't replace `isUserInteracting`; after P4, only undo reads it.

### E4 — Downstream consumers / final API scope  · Status: DONE (2026-05-30)

### E4 — Downstream consumers / final API scope  · Status: TODO
Decide the consumer set the API must serve **beyond adaptive + hold**, so we build it once: HUD auto-fade, **defer-expensive-work-during-interaction** (shader compiles, autosave, gallery sync, heavy panel re-renders), tutorial detection, interaction telemetry, a "performance mode." Shape `isInteracting()` (+ source filter, + `isSceneAnimating`) to be a general capability. **Output:** finalized API + a "downstream consumers" section in the ADR.

**Notes (resolved → ADR "E4" + API block):** Final API = `beginInteraction`/`endInteraction`/`pokeInteraction`, `isInteracting(filter?)`, **`isIdle(ms?)`** (new — centralizes the idle check for render-pause + defer-work), `interactionSources`, `lastActivityTime`, plus a reactive **`useIsInteracting(filter?)`** hook (coarse edge boolean, for the few low-freq UI consumers — generalizes the lone existing subscription in `Viewport.tsx`). `isSceneAnimating` stays separate (playback ≠ gesture). Consumer→call map: adaptive=`isInteracting()`; hold/idle-pause=`isIdle(1000)`; HUD-fade=`isInteracting({only:['camera','scrub']})`; defer-work (`CompileScheduler.ts` coalesces bursts but doesn't yet defer on interaction; autosave; gallery sync)=`isInteracting()`/`isIdle`; tutorial/telemetry=`interactionSources`+`lastActivityTime`. Source-filter is load-bearing (HUD-fade) → keep it. Hot-path consumers read via `getState()`, never subscribe.

### E5 — Testing + observability design  · Status: DONE (2026-05-30)
Design (a) a **unit-testable session state machine** — ref-count / debounce / watchdog, pure, no render — with cases incl. stranded-session / unmount-mid-drag / lostpointercapture; and (b) a **dev overlay** showing live active sources + `isInteracting` + adaptive scale (extend `AdaptiveResolutionBadge` or a HUD). **Output:** test-file plan + overlay spec → both built in P2.

**Notes (resolved → ADR "E5"):**
- **Pure machine, `now` injected.** Extract `InteractionSessionMachine` mirroring `engine/AdaptiveResolution.ts` (`createState()` + `tick`-style reducer, no DOM/React/Three). MUST take `now` as a param (never call `performance.now()` internally) or debounce/watchdog aren't deterministically testable. `createInteractionSlice` = thin wrapper owning refs + gating the edge boolean (`viewportSlice._adaptive` pattern).
- **No vitest/jest in repo** — tests are `tsx` scripts in `debug/*.mts` with inline asserts + snapshot diffing. Test file: `debug/test-interaction-session.mts` (mirror `debug/test-compat.mts`); add to `package.json`. Cases: ref-count balance, unbalanced-end dev-warn, debounce tail, watchdog force-clear at `MAX_SESSION_MS`, unmount-mid-drag cleanup, `lostpointercapture`+`pointercancel` release, poke throttle (~50ms, no store write).
- **Overlay:** add `interactionSessionOpen` toggle to the existing `debug_tools` feature (`engine/features/debug_tools/`); badge mirrors `engine/plugins/viewport/AdaptiveResolutionBadge.tsx`; reads `interactionSources` via `getState()`, `isInteracting`, `1/qualityFraction`; advanced-menu gated; mounts via ComponentRegistry overlay slot. No new framework.

---

## Implementation phases (from ADR-0061)

### P2 — Inert primitive + bridge  · Status: DONE (core `e87498f` 2026-05-30; wiring landed 2026-05-30)
**Done (core, `e87498f`):** the pure `engine/InteractionSessionMachine.ts` (gesture-activity core, `now`-injected) + `debug/test-interaction-session.mts` (`npm run test:interaction`, 37 assertions green, tsc clean). API as specced: `begin/end/poke/isInteracting(filter)/isIdle(ms)/tickWatchdog/interactionSources`; `hardActive` edge-only, debounce tail on the poll, watchdog backstop.

**Done (wiring, this session) — all INERT (zero behaviour change; no consumer switched):**
- **`store/slices/createInteractionSlice.ts`** — wraps the machine in a MODULE-LEVEL `_session` ref (mirrors viewportSlice `_adaptive`), throttles `pokeInteraction` (~50ms, timestamp-only, no store write), writes the coarse reactive `interacting` boolean ONLY on the machine `edge` (idle↔active). Composed into `store/engineStore.ts`. Exposes `isInteracting(filter)/isIdle(ms)/getInteractionSources()/getLastActivityTime()/tickInteractionWatchdog()` via getState(). **Watchdog → boolean:** `tickInteractionWatchdog` writes `interacting:false` on `cleared` (a force-clear is an active→idle edge) so the low-frequency reactive UI can't latch stale-true — hot-path consumers already poll `isInteracting()` which collapses on its own.
- **`engine/hooks/useInteractionDrag.ts`** — dispatch-only (reads nothing), returns `{ onPointerDown, onPointerUp, onLostPointerCapture, onPointerCancel }` + a `useEffect` cleanup that ends an active drag on unmount; one outstanding begin per mount (multi-release collapses to one end). Authoritative unbalanced-end dev-warn lives in the slice (machine ref-count underflow).
- **Worker bridge** — `interacting` + `isSceneAnimating` added to `EngineRenderState` (FractalEngine.ts) + derived in `GmtRendererTickDriver` via the pure **`engine-gmt/renderer/renderInteractionState.ts`** (SSOT for the key names — pinned to `EngineRenderState` via `Pick`). SENT BUT UNUSED. `isSceneAnimating` = playback ∥ active-LFO (the separate autonomous-animation axis; playback is NOT a gesture).
- **Per-consumer kill-switch flags** — `interactionConsumerFlags { adaptive, hold, hudFade, idlePause }` (+ `setInteractionConsumerFlag`), declared, defaulted OFF, UNUSED until P4.
- **Dev overlay** — `interactionSessionOpen` toggle on the `debug_tools` feature + `engine/plugins/viewport/InteractionSessionBadge.tsx` (mirrors `AdaptiveResolutionBadge`): live `interactionSources` (getState()), the polled `isInteracting` (with a `(tail)` marker when the debounce tail outlives the edge boolean), adaptive scale (`1/qualityFraction`), and the **divergence counter STUB** (`interactionDivergenceCount`, never incremented in P2 — display path only).
- **Latency+outcome harness** — `debug/interaction-latency-harness.mts` (`npm run bench:interaction-latency`) → `debug/interaction-latency-baseline.json`. Deterministic (injected `now`, no clock/random ⇒ byte-identical re-runs), drives the real `tickAdaptiveResolution` + the viewportSlice qualityFraction conversion (store values, not pixels). Records input→first-reduced-scale-frame latency (p50/p95/min/max/mean) + outcome (qualityFraction dropped / fpsSmoothed reached target). **Baseline captured:** legacy mode — camera engage p50 **66ms** / p95 **330ms** (cold first gesture), quality always drops + fps reaches target; **slider INVISIBLE** (latency ∞, never drops, never reaches target — the documented bug). P4 runs `--mode=session --check` and the slider going finite (camera not regressing) is the gate.
- **Read-path + inertness tests** — `debug/test-interaction-wiring.mts` (`npm run test:interaction:wiring`, 26 assertions): worker read-path round-trips known-true through the SSOT + an `Object.assign` mirroring `setRenderState` (guards the silent-false-on-typo failure); sibling inertness proves the slice makes ZERO reactive writes and the watchdog is a no-op with no producer, then a "wired-not-dead" pass shows a real gesture flips the edge boolean exactly once per edge.
- **Pre-push gate** — `npm run test:gate` (= `test:interaction` + `test:interaction:wiring`, 63 assertions) wired into a version-controlled `.githooks/pre-push` (activate with `git config core.hooksPath .githooks`).
- **Verification:** `test:interaction` 37 ✓, `test:interaction:wiring` 26 ✓, `tsc --noEmit` clean (the only 3 errors are pre-existing in the orphaned `FlowEditor.tsx`, present on the clean tree — unrelated to this work), `engine-gmt-smoke` boots with zero page errors.

_Original P2 spec:_ Pure `InteractionSessionMachine` (`now`-injected, per E5) + `createInteractionSlice` wrapper (transient-reactive per ADR Performance: refs for hot state, reactive boolean only on idle↔active edges), `useInteractionDrag` hook (dispatch-only), derive `interacting` + `isSceneAnimating`, send `interacting` in `renderState` **UNUSED**, worker-read-path test (guard silent-`false`-on-typo), **per-consumer kill-switch flags** (adaptive/hold/HUD-fade/idle-pause flip independently), the E5 tsx tests (`debug/test-interaction-session.mts`) + dev overlay. **Keep the session NARROW** — gesture activity only, not "should we work" (ADR Scope boundary).
- **Verification harness (load-bearing — see Verification protocol):** instrument input→first-reduced-scale-frame latency into a ring buffer the overlay shows (p50/p95); a scripted gesture sequence captures `debug/interaction-latency-baseline.json` (latency **and** the outcome: did `qualityFraction` drop below 1.0 / `fpsSmoothed` reach target). Reads store values, not pixels (headless-friendly). The SAME harness re-runs in P4 and diffs.

**Acceptance:** zero behavior change; latency + outcome baseline captured via the harness; **siblings (fluid-toy/fractal-toy) unaffected — slice + watchdog provably inert when no producer calls `beginInteraction`**; the E5 machine test green + added to a pre-push gate; typecheck + smoke clean.

### P3a — Producers: camera / gizmo / picker (~10 sites)  · Status: DONE (2026-05-30)
Wire begin/end + the BLOCKING safety (lostpointercapture, unmount cleanup, picker RAF guards, gizmo single-source). Still inert (consumers not switched). **Acceptance:** overlay shows correct sessions for these gestures; no regressions.

**Done (this session) — all INERT (no consumer switched, per-consumer flags still OFF; only observable change is the dev overlay showing sessions):**
- **Canonical token module** `engine-gmt/interaction/interactionSources.ts` — GMT declares its set (`camera|gizmo|picker|slider|drawing|scrub`) as one frozen `INTERACTION_SOURCES` table (engine-core machine/slice stay `InteractionSource = string`, ADR-0059 open-token). Removes the typo-strands-under-an-unwatched-name failure class across the 10 P3a + 90 P3b sites. P3a uses camera/gizmo/picker.
- **(a) Camera (`camera`)** — `engine-gmt/navigation/Navigation.tsx`:
  - drei `<OrbitControls>` `onStart`/`onEnd` (:1373/:1395) → `begin/endInteraction('camera')`. Covers touch rotate/pinch/pan for FREE (E1) — drei fires ONE start/end pair per gesture (no multi-pointer double-begin) and self-heals on its own pointercancel, so this path is **exempt** from mitigation #1 cancel/capture wiring.
  - Custom cursor-anchored **left-drag orbit** (:680/:808) and **middle-drag** dolly. _(SUPERSEDED — see Follow-up round 2: ref-counting across these paths + drei stranded the count because drei suppresses its own `onEnd` mid-gesture; consolidated in `c9011b0` to ONE **release-driven** `camera` session — open on first real move, close on window `pointerup` with `e.buttons === 0`. Single boolean, not ref-counted.)_
  - **Wheel** → `pokeInteraction('camera')` (discrete; pinch-zoom is the continuous one and rides OrbitControls, E1). Poke is throttled (~50ms) in the slice.
- **(b) Gizmo (`gizmo`)** — `engine-gmt/features/lighting/LightGizmo.tsx`: a single `applyGizmoInteracting(active)` helper flips the legacy dual flags (`engine.isGizmoInteracting` + uiSlice `setGizmoDragging`) **and** the session begin/end together, here and nowhere else (mitigation #4 — they can't drift). `gizmoSessionActive` ref keeps begin/end balanced. **Dual state NOT collapsed and `selectMovementLock` NOT touched — that's P5.**
- **(c) Picker (`picker`)** — `engine-gmt/hooks/useInteractionManager.ts`: `begin/end('picker')` for focus + julia pick-drags (one token, mutually exclusive via `interactionMode`). **RAF guards (mitigation #3):** each pick loop ends the session + exits when `canvasRef.current` goes null; every async pick (`startFocusPick`/`sampleFocusPick`/`pickWorldPosition`) gets `.catch(() => end…Drag())` so a rejected worker RPC ends + exits instead of soft-hanging.
- **(d) Safety (mitigation #1, BLOCKING)** — every CUSTOM imperative drag wired here gets `pointercancel` (touch/OS interruption) + unmount cleanup; the gizmo (the only one using `setPointerCapture`) also gets `lostpointercapture`. The pickers/nav-custom paths use window listeners (no `setPointerCapture`), so they need only `pointercancel` + cleanup. (These are `addEventListener` loops, not JSX components, so they can't spread `useInteractionDrag` — they replicate its exact safety inline; the hook stays the mechanism for the P3b JSX slider/knob components.)
- **(e) Watchdog** — `tickInteractionWatchdog()` called every frame in `GmtRendererTickDriver` `useFrame` (constant-time; INERT — nothing reads `interacting`). **Resolved the watchdog-liveness open-Q:** all four continuous-drag move handlers (orbit, middle, gizmo, picker) now `pokeInteraction()` on real movement (throttled, ref-only, **no store write** — the ADR-sanctioned exception), so a long ACTIVE drag isn't force-cleared at `MAX_SESSION_MS`. A held-MOTIONLESS drag still legitimately settles (no poke) — the post-clear pointerup then fires a benign unbalanced-end dev-warn; acceptable for a crude last-resort backstop.
- **Verification:** `test:gate` 63 ✓ (machine 37 + wiring 26); `tsc --noEmit` clean (the only errors are the 3 pre-existing in the orphaned `FlowEditor.tsx`, present on the clean tree); the real engine-gmt app boots clean via `app-gmt.html` (2 canvases, worker booted + shader compiled + accumulating, 0 page errors); a headless gesture probe proved a real left-drag opens a `camera` session (`sources:['camera']`, `interacting:true`) and ENDs on release — `sources` empties immediately while the polled `isInteracting()` rides the 200ms debounce tail then collapses to idle; a ~1.2s continuous moving drag stays alive (move-pokes) and still ends clean (no strand). _(NB: `debug/engine-gmt-smoke.html` is missing from this checkout — vite serves the dev-preview fallback for it — so the boot check ran against the real `app-gmt.html` entry instead, which exercises the same Navigation / tick-driver / LightGizmo code.)_
- **Deferred (out of the enumerated P3a scope, flagged for follow-up):** **Fly-mode mouse-look** (`useInputController.ts:242/266`) + **WASD / joystick** camera movement (the `markActivity` poke + `isInteracting()` keyboard/joystick signal) are camera producers NOT wired here — they weren't in P3a's enumerated site list and wiring them entangles the lagged `markActivity`/`isCameraInteracting` proxy that E3 marks for P4 migration. Wire alongside the P4 proxy work (or a small P3a-follow-up) once that signal's semantics are settled. Drawing / timeline-scrub / sliders = **P3b**; `interactionMode` region-select (`selecting_region`/`selecting_preview`) is not a focus/julia picker — handled separately in P3b.
- **Open-Q dispositions:** *tap-vs-drag* — _(SUPERSEDED by round 2 below)_ initially begun on pointerdown (instant-begin, accepted); **round 2 changed it to open on the first real movement** — a plain click now opens nothing (visually verified). `frameloop:'always'` idle-edge delivery still only bites once a consumer reads the disengage edge (P4).

**Follow-up — user visual-pass fixes (2026-05-30, same session):**
- **Multi-button strand (BUG) — fixed.** Pressing two mouse buttons (e.g. right-pan held, then left-orbit grabbed) stranded `camera` true until the 8s watchdog. Root cause: a custom cursor-anchor handler sets `orbitRef.enabled=false` to take over, and drei only fires `onEnd` while enabled → its `begin('camera')` was never matched. Fix: track drei's session in a `dreiCamSession` ref and **force-close it at the three points that disable drei** (custom orbit onDown, middle onDown, wheel burst-start) + guard the onStart begin. The custom paths' own begin/end stay reliable (window pointerup). Verified headlessly: right+left press→release leaves `sources:[]`, `interacting:false` (no strand).
- **Light-drag-from-HUD — wired.** Dragging a light in from the CenterHUD light studio (`draggedLightIndex` placement in `useInteractionManager.handlePointerMove`) fired no session. Now opens/closes the **`gizmo`** token (it positions a light; self-gates on `!isGizmoInteracting` so it never overlaps the gizmo-handle drag) with the same pointerup / pointercancel / unmount release paths.
- **Wheel observability.** Mouse-wheel is a `poke` (no hard-active source, by ADR design), so the overlay correctly showed `sources: —  interacting: true (tail)` — but that read as "didn't register". Added `getRecentInteractionSources()` (active **or** within the debounce tail; pure `recentInteractionSources()` in the machine) and the badge now appends `+camera (tail)`, so a wheel legibly reads as `camera`. Wheel stays a poke (unchanged semantics).
- **Overlay repositioned** to vertically-centred, 50px from the left edge (was bottom-left, slightly obscured).

**Follow-up round 2 — second visual pass:**
- **Tap-vs-drag (all camera paths).** A click / right-click that doesn't move the view opens NO session (it changes nothing, so it needs none). Open on the **first real move** (any non-zero delta — the camera already moves + resets accumulation on a 1px delta, so the session tracks the same threshold; no magic px number). Custom orbit + middle open on the first non-zero pointer move (past the existing `dx===0&&dy===0` guard); the drei path (cursor-anchor right-PAN) arms on `onStart` and opens in the orbit `useFrame` on the first frame it actually moves the camera (reusing the pan-absorb's "moved this frame" signal — `camera.position.lengthSq() > 1e-8`). Non-anchor mode keeps immediate begin (movement-detection isn't reliable for drei rotate there; non-default). _(An initial 4px threshold was dropped — it left a dead zone where a 1–3px drag moved the view but opened no session.)_
- **Concurrent-button strand (still-open BUG) — fixed at the real cause.** The earlier `dreiCamSession` close at the 3 gesture-start disable points missed the **per-frame gating** (`orbitRef.enabled = … && !customActive`) which ALSO disables drei mid-gesture when a custom gesture takes over — and drei won't fire `onEnd` while disabled. Added a per-frame reconciliation right after the enable-gating: `if (!orbitRef.enabled && (dreiCamSession || dreiArmed)) closeDreiCamSession()`. Verified headlessly: two concurrent buttons (click AND drag, both orders) leave no stranded session.
- **Native context menu on the canvas — suppressed.** Right-drag is a camera gesture, so a `contextmenu` `preventDefault` is bound directly to the WebGL canvas (UI overlays are separate elements and keep their own menus). This was an existing issue blocking gesture testing.
- **Overlay "adaptive scale" + "divergence" rows removed.** Both were misleading in P3a: the session is inert, so adaptive scale is driven by the legacy proxy independent of the session, and `interactionDivergenceCount` is a P4 stub (always 0). Replaced with an honest `inert · consumers + divergence wire in P4` line; the rows return in P4 as a real session-vs-accum-drop comparison. (Sources + interacting + the wheel `(tail)` marker remain — that's the signal the go/no-go needs.)

**→ GO/NO-GO CHECKPOINT (next): user visual pass.** Open the dev overlay (advanced menu → Interaction Session) and confirm each gesture shows the right token and they all END (no stranded `camera`/`gizmo`/`picker` after release / pointercancel / capture-loss): orbit/pan/pinch + custom left-drag + middle-drag + wheel = `camera`; light-gizmo drag = `gizmo`; focus/julia pick = `picker`. Then decide whether the primitive justifies the ~90-site P3b blast radius. (Test in **Orbit** mode — Fly-mode look is a documented P3a deferral.)

### P3b — Producers: sliders / knobs / vectors + drawing + scrub (~90 sites)  · Status: DONE (2026-05-30)
Wire `useDragValue.onDragStart/End` via `useInteractionDrag` across all input components; drawing tools; timeline scrub. Anchor `slider` begin/end to the existing `beginParamTransaction`/`endParamTransaction` boundaries (balanced + cleaned up) — don't hand-roll a parallel lifecycle. **No per-pointermove store writes** (but pointermove may throttle-poke the *ref* to keep the watchdog alive on long continuous drags). **Mechanical coverage check** (grep/lint every `useDragValue` consumer is wired). **Acceptance:** overlay shows sessions for every input type; no missed sites.
- **Lessons front-loaded from P3a (lower strand risk than camera):** (1) the hook genuinely fits these **JSX** slider/knob/vector components — spread it; P3a's "replicate inline" was the *imperative-addEventListener* exception, not the rule. (2) Open on the **actual drag, not press** — `useDragValue.onDragStart` already fires on drag-start, so tap-vs-drag is handled for free here (no click-pop, unlike camera's pointerdown paths). (3) Each slider is a **single balanced path**, so ref-counting is safe — the camera multi-path strand class does NOT apply. (4) Inheriting `beginParamTransaction`/`endParamTransaction`'s end+cleanup is the whole point: less new stranding surface. Region-select (`selecting_region`/`selecting_preview`) is a drawing-ish drag, not a focus/julia picker — wire it here, not as `picker`.
- **Go/no-go checkpoint (after P3a + start of P3b's parallel run):** overlay shows correct sessions AND latency baseline justifies the remaining blast radius? The "build the primitive" bet is contingent on this. **PASSED at P3a** (user visual pass) — P3b proceeded.

**Done (this session) — all INERT (no consumer switched, per-consumer flags OFF; only the dev overlay observes the new sessions):**
- **`useInteractionGesture(source)` core** added to `engine/hooks/useInteractionDrag.ts` — the balanced begin/end + unmount-cleanup the hook already had, now exposed as imperative `{ begin, end }` for producers whose drag surfaces as SEMANTIC callbacks (`onDragStart`/`onDragEnd`, `setIsScrubbing(true|false)`) rather than raw PointerEvents. `useInteractionDrag` now delegates to it (identical behaviour). Dispatch-only → mounting it in ~90 components costs ZERO renders (ADR Performance).
- **Token discipline:** all producers import `INTERACTION_SOURCES` (engine-gmt) — no raw string literals. There is established precedent for `components/ → engine-gmt/` imports (CompilableFeatureSection, Dock, NewSceneModal…), so the shared-UI sites import the constant directly. The token is **context-dependent** (slider vs scrub vs drawing) → wiring lives at the store-aware **connected wrapper**, never baked into the store-agnostic leaf primitive (a leaf doesn't know if it's a slider or a scrubbed keyframe value).
- **Architecture — wire at the connected wrapper, not the shim.** `handleInteractionStart('param')` / `handleInteractionEnd()` (→ `begin/endParamTransaction`) is the gesture boundary, but it ALSO fires for DISCRETE ops (dropdown select, toggle, menu actions, randomize, duplicate/remove, formula-switch, flow-node ops). Per the **scope boundary** (gesture activity only, NOT render-intent), the session is co-located ONLY at CONTINUOUS-DRAG sites — never broadened into the shim (which would catch discrete ops + put a GMT token in engine-core).
- **SLIDER token:**
  - **`components/Slider.tsx`** — connected `DraggableNumber` (178) + connected `Slider` (209) co-locate `slider.begin/end` with `handleInteractionStart/End`. Covers the whole `<Slider>`/`<DraggableNumber>`/`ScalarInput`-track/`RawDraggableNumber` chain (the dominant numeric-drag surface). Discrete reset clicks (context-menu / footer / onReset) deliberately NOT wired.
  - **`components/Knob.tsx`** — connected `Knob` (knob-rotate + its `RawDraggableNumber` field, both via the shared `onDragStart/End`).
  - **`components/vector-input/index.tsx`** — `Vector2/3/4Input` (covers `BaseVectorInput`/`VectorAxisCell`/`DualAxisPad`/`RotationHeliotrope` via their `onDragStart/End`).
  - **`engine-gmt/features/lighting/components/LightDirectionControl.tsx`** — the direction **pad** (raw-pointer owner) spreads `useInteractionDrag` (+ `pointercancel`/`lostpointercapture` on the element); its `<DraggableNumber>` fields ride the connected component.
  - **`components/AdvancedGradientEditor.tsx`** — the knot / bracket / bias **drags** (`startDrag` + `handleTrackMouseDown` begin → `handleMouseUp` end). Marquee-select + the discrete menu/keyboard/cycle handlers open NO session.
  - **`components/EmbeddedColorPicker.tsx`** — the H/S/V range drags (one shared `useInteractionDrag`, mutually exclusive under a pointer; `pointercancel`/`lostpointercapture` on each input). Strip-menu paste/quick-pick/history = discrete, skipped.
  - **Auto-covered (NO new wiring):** `scene_widgets.tsx` (dolly via `<Slider>`), `NodeParams.tsx` (via `<Slider>`), `LightPanelControls.tsx` (`<Slider>`/`<Vector3Input>`; its lone `handleInteractionStart` is a Cast-Shadows checkbox = discrete), `CenterHUD.tsx` (all `handleInteractionStart('param')` are discrete toggles/menus; value edits live in popups' `<Slider>`; light-drag-in is P3a `gizmo`), `AutoFeaturePanel` / `Histogram` / `NumberInput` / `EngineFeatureRow` / `KeyframeInspector` / `Logo` / `ConfigForm` / `renderDialogExtras` (all use the connected `<DraggableNumber>`/`<Vector*Input>`).
- **SCRUB token:** `components/timeline/TimelineRuler.tsx` (playhead drag) + `components/graph/GraphSelectionBBox.tsx` (keyframe bbox-scale), each anchored to `setIsScrubbing(true|false)`. `Timeline.tsx`'s `setIsScrubbing(false)` is an unmount-cleanup only (delegates to children) → no wiring. **KeyframeInspector** value/frame/tangent fields are the connected `<DraggableNumber>` → already `slider`; its `setIsScrubbing` only pauses playback, so it is NOT also tagged `scrub` (avoids double-tokening one gesture).
- **DRAWING token:** `engine-gmt/features/drawing/DrawingOverlay.tsx` (freehand shape drag; imperative canvas listener → `useInteractionGesture` + explicit `pointercancel`/`lostpointercapture` listeners) + `engine-gmt/hooks/useRegionSelection.ts` (region draw / move / resize — `selecting_region`, drawing-ish, NOT a picker; stable destructured begin/end so the window-listener effect doesn't churn). `usePreviewTarget` (`selecting_preview`) is a single CLICK placement, not a drag → discrete, no session.
- **Cancel/strand safety:** raw-pointer owners (light-dir pad, color-picker inputs, drawing canvas) get `pointercancel` + `lostpointercapture` → session end (ADR mitigation #1). Semantic-callback sites (Slider/Knob/vector/gradient/timeline) inherit the leaf's pointer-capture-backed `pointerup` + the hook's **unmount cleanup** + the 8s watchdog backstop; a touch-cancel mid-numeric-drag is the one residual (rare; INERT now; would show in the P4 divergence counter).
- **Mechanical coverage gate:** new `debug/test-interaction-coverage.mts` (`npm run test:interaction:coverage`, folded into `test:gate`). (A) every wired producer references its `INTERACTION_SOURCES` token via the constant (no bare literals); (B) **regression guard** — every `import …useDragValue` consumer is a wired slider producer or an allowlisted store-agnostic primitive/barrel (a new unwired slider fails here); (C) **regression guard** — every `setIsScrubbing(true)` writer is a wired scrub producer or the documented KeyframeInspector exception. Catches missed sites without eyeballing 90.
- **Verification:** `test:gate` **green** (machine 37 + wiring 26 + **coverage 36** = 99 assertions); `tsc --noEmit` **clean**; `app-gmt.html` boots clean via the live dev server (0 page/console errors, the real slider UI renders); a headless **wired-not-dead probe** (real mouse drag on a `[data-role="value"]` field, throwaway) confirmed a drag opens `sources:['slider']` (interacting true), release empties `sources` immediately while the polled `isInteracting()` rides the 200ms tail then collapses to idle — same lifecycle P3a verified for camera. **User owns the visual overlay pass** (per the verification protocol): open the dev overlay and confirm slider/knob/vector drags show `slider`, the freehand-draw + region-select show `drawing`, the timeline playhead + graph bbox-scale show `scrub`, and all END on release.
- **NB:** shared `components/` producers (Slider/Knob/vector/gradient/colorpicker/timeline/graph) are used by the sibling toys too; firing `slider`/`scrub` there is still **inert** for siblings (no session consumer; the worker `interacting` derivation is GMT-only in `GmtRendererTickDriver`). They're pre-wired for a future sibling opt-in.
- **Deferred (out of the enumerated P3b scope):** DopeSheet/Graph keyframe **translation** drags use `animationStore.snapshot` (animation-undo domain), set no `isScrubbing`, and signal no interaction today — not a slider/scrub/drawing producer per the explicit scope; wire alongside P4/animation work if adaptive should engage on them.

### P4 — Switch consumers (GMT), ADDITIVE — not rip-and-replace  · Status: TODO
Point adaptive input + `FractalEngine` hold (composed with render-dirty, per E3) + HUD-fade + idle-pause at the session, **each behind its per-consumer flag**, **with the accum-drop proxy still running in parallel**. Overlay shows session vs. accum-drop with an explicit **divergence counter** (frames they disagreed + last source/context — not just current state); confirm near-zero divergence across real use before trusting the session. Undo-coalescing stays (kept). **Behavior-changing — visual verification + re-run the P2 harness: latency MUST beat the baseline; if not, root cause was elsewhere (main-thread starvation) → stop & reassess, don't ship.** **Acceptance:** harness shows latency beats baseline + outcome holds (quality drop / fps→target); divergence counter near-zero; sliders reach target FPS; engagement lag fixed; idle converges; no mid-gesture pops; HUD/undo/pause unbroken.

### P5 — Cleanup + hardening (only after P4 parallel run confirms)  · Status: TODO
Remove the GMT proxies (accum-drop activity, `mouseOverCanvas`, `gateOnAccumOnly` for GMT); delete the dual gizmo state (repoint `selectMovementLock`'s gizmo check at the unified source — don't drop it); finalize watchdog + dev warnings; remove the per-consumer flags. **Sibling-app (fluid-toy/fractal-toy) migration is a separate follow-up** (they keep `gateOnAccumOnly` until then).

---

## Open questions (carry from ADR)
- `DEBOUNCE_MS` final value — tune in P2 from measured drag-commit / wheel-burst spacing (candidate 150–200ms).
- Undo coalescing → **KEEP SEPARATE** (transaction concept). Resolved (E3).
- **Movement threshold before `beginInteraction`?** The old 200ms buffer accidentally filtered out taps/micro-drags; instant-begin may pop resolution on a click. Decide in P3a (a small px threshold vs. accept the pop) — the latency instrument won't catch this, it measures engage speed not short-gesture flicker.
- **Watchdog liveness after accum-drop removed:** long continuous drags must throttle-poke the ref on pointermove (ref write, no store write) or the ~8s watchdog force-clears mid-gesture. Bake into P3a/P3b producer wiring.
- **Verify `frameloop` is `'always'`** (top of P2) so the idle `false` edge is actually delivered to the worker; if `'demand'`, the disengage edge needs an explicit invalidate.

## Status log
- **2026-05-30:** ADR-0061 finalized (5 review angles); RC A + interim adaptive fixes committed (`f8fa698`); ADR hardening (`9c6c301`, `7efd0c9`); this plan created.
- **2026-05-30:** **E1 done** — touch/multi-pointer/pinch investigated; no new token, touch rides drei OrbitControls (camera) + shared PointerEvents (everything else); `pointercancel` added to stranded-session mitigations. ADR "Touch / multi-pointer / pinch" subsection + mitigation #1 + open-Q #7 updated.
- **2026-05-30:** **E2–E5 done** (all edges closed). E2 export-safe by construction; E3 consumer audit (migrate/keep/remove table) + open-Q #6 resolved (undo stays separate) + camera-block orthogonal; E4 final API (`isIdle` + `useIsInteracting` + source-filter); E5 pure `now`-injected machine + tsx test plan + `debug_tools` overlay. ADR gained an "Edge investigation outcomes (E1–E5)" section + API additions; open-Qs #6–#10 resolved. Commits `3d13d73` (E2), `ee455cf` (E3+E4), `8590e2c` (E5).
- **2026-05-30:** **Architecture hardening pass** (after a "is this sufficient / are we going the wrong way?" review). Verdict: direction sound, continue — but three corrections folded into ADR + plan: (1) **Scope boundary** — session = *gesture activity only*, NOT a render-intent god-object; render-invalidation stays in the dirty/accum system; consumers compose. (2) **Idle-pause migrates by COMPOSITION** (`!isIdle || recentlyDirty`), not replacement — `markInteraction` is a wake/invalidation signal, not a gesture signal. (3) **P4 cutover is ADDITIVE** — run session in parallel with accum-drop behind per-consumer flags, prove agreement, remove proxy only in P5; latency baseline is a P4 gate. Plus: slider producers reuse the existing `beginParamTransaction`/`endParamTransaction` lifecycle (less stranding surface); P3a/P3b go/no-go checkpoint; new open-Qs (movement threshold, watchdog ref-poke liveness, verify `frameloop:'always'`). Title reframed to "gesture activity". Plus new open-Qs (tap-vs-drag threshold, watchdog ref-poke liveness, verify `frameloop:'always'`).
- **2026-05-30:** **ADR Accepted + impl branch + verification protocol.** Branched `feat/interaction-session` for all P2+ code. ADR → Accepted. Folded in: P2 builds a latency+outcome harness → `debug/interaction-latency-baseline.json` (store values, not pixels) re-run+diffed in P4; P2 acceptance now includes sibling-app inertness; P4 overlay gets a session-vs-accum-drop **divergence counter**; three-tier verification protocol written into "How to run". **Ready to start P2.**
- **2026-05-30:** **P2 wiring DONE** — `createInteractionSlice` (module-ref machine, ~50ms poke throttle, edge-only `interacting` boolean; watchdog also writes the boolean on force-clear, per a mid-session coordination note) composed into the engine store; `useInteractionDrag` dispatch-only hook + unmount cleanup; worker bridge (`interacting` + `isSceneAnimating` in `renderState` via the pure `renderInteractionState` SSOT, SENT BUT UNUSED); per-consumer kill-switch flags (off); dev overlay (`debug_tools` toggle + `InteractionSessionBadge` with divergence-counter stub); latency+outcome harness + baseline captured (legacy: camera p50 66ms / p95 330ms, slider invisible — the bug); read-path + inertness tests (`test:interaction:wiring`, 26 ✓); pre-push gate (`test:gate` → `.githooks/pre-push`). All INERT — no consumer switched. `test:interaction` 37 ✓, `test:interaction:wiring` 26 ✓, tsc clean (3 pre-existing FlowEditor errors aside), engine-gmt-smoke boots clean. **Open-Qs carried to P3a:** `DEBOUNCE_MS` left at 200ms (matches the legacy input buffer; final tune needs real wheel/drag spacing from producers); `frameloop:'always'` idle-edge delivery only bites once a consumer reads the disengage edge (P4); tap-vs-drag threshold + watchdog ref-poke liveness are P3a producer-wiring decisions. **Next: P3a** (camera/gizmo/picker producers + begin/end safety + OrbitControls touch wiring from E1).
- **2026-05-30:** **P3a DONE** — ~10 camera/gizmo/picker producers wired via `begin/end/pokeInteraction` behind a new canonical `INTERACTION_SOURCES` token table (`engine-gmt/interaction/interactionSources.ts`). Camera: OrbitControls onStart/onEnd (touch-free, E1) + custom left-drag + middle-drag (begin/end) + wheel (poke). Gizmo: single-source `applyGizmoInteracting` drives the legacy dual flags + session together (mitigation #4; dual state + `selectMovementLock` left for P5). Picker: focus+julia begin/end + RAF guards (canvas-null + pick-reject `.catch`, mitigation #3). Safety (mitigation #1): `pointercancel` + unmount cleanup on every custom imperative drag, `lostpointercapture` on the gizmo (only one with `setPointerCapture`). Watchdog (e) wired into `GmtRendererTickDriver` useFrame; resolved the watchdog-liveness open-Q by throttle-poking on real movement in all four continuous-drag move handlers (ref-only, no store write). **All INERT** — no consumer switched, flags OFF. `test:gate` 63 ✓, tsc clean (3 pre-existing FlowEditor errors aside), `app-gmt.html` boots clean (0 page errors) + a headless gesture probe proved a real drag opens a `camera` session and ENDs on release (debounce tail correct, no strand). **Deferred:** Fly-mode mouse-look + WASD/joystick (entangle the lagged `markActivity` proxy P4 migrates) — flagged for P4/follow-up. **→ user GO/NO-GO visual pass** (overlay shows right token per gesture, all END) before P3b.
- **2026-05-30:** **P3a follow-up** (after the user's first visual pass): fixed a multi-button strand (`dreiCamSession` force-close when a custom handler disables drei mid-gesture — drei's onEnd is suppressed while disabled); wired the HUD light-drag-in gesture under `gizmo`; added `getRecentInteractionSources()` so the overlay names a wheel poke as `camera (tail)` instead of an anonymous tail; moved the badge to left-centre. `test:gate` 63 ✓, tsc clean, headless probe confirms two-button press→release no longer strands and wheel registers as camera. Still INERT.
- **2026-05-30:** **`/simplify` pass** (`c9011b0`). Reuse + efficiency clean; the dominant simplification/altitude finding was that the camera-session bookkeeping was an over-complex special-case pile. Consolidated `dreiCamSession`+`dreiArmed`+`closeDreiCamSession`+per-handler `orbitOpened`/`midOpened`+the per-frame reconciliation+3 manual `closeDrei()` calls+the cursor-anchor `onStart` branch into ONE `camSessionOpen` boolean: `openOrPokeCamSession()` (all three move paths open on first real move + poke after) and a single **release-driven close** (window pointerup `e.buttons===0` / pointercancel / unmount — no longer dependent on each path's end or drei's onEnd). Also deduped `endFocusDrag`/`endJuliaDrag`→`endPickDrag()`. Net −120/+74. As a bonus this makes the residual concurrent-button case unable to strand the *session* (release-driven close), though the underlying drei-level gesture glitch on middle+another-click is separate (accepted as a known limitation). Skipped (out of scope): gizmo dual-flag→slice (P5), machine idempotent-by-default, auto-poke. `test:gate` 63 ✓, tsc clean, full headless lifecycle probe green.
- **2026-05-30:** **P3a follow-up round 2** (second visual pass): **tap-vs-drag** across all camera paths (click/right-click opens no session — custom orbit+middle defer begin past a 4px threshold; drei arms on onStart, opens in the useFrame only on real camera movement). **Concurrent-button strand fixed at the real cause** — the per-frame enable-gating also disables drei mid-gesture (not just the 3 gesture-start points), so added a per-frame reconciliation that closes drei's session whenever the gating disables it. **Native canvas context menu suppressed** (was blocking gesture testing). **Overlay** adaptive-scale + divergence rows removed (both meaningless while inert — adaptive scale is the legacy proxy's, divergence is a P4 stub); honest "inert · wire in P4" line instead. `test:gate` 63 ✓, tsc clean, headless probe confirms click→no-session, drag→opens+ends, right-click menu suppressed, two-button click+drag no strand, wheel→camera tail. Still INERT.
- **2026-05-30:** **P3b DONE** — the ~90 slider/knob/vector + drawing + scrub producers wired, **all INERT** (no consumer switched, flags OFF). Added `useInteractionGesture(source)` (the hook's balanced begin/end + unmount-cleanup core, exposed for SEMANTIC-callback sites; `useInteractionDrag` delegates to it). **SLIDER** at the store-aware connected wrappers (`Slider.tsx`, `Knob.tsx`, `vector-input/index.tsx` — covering the whole `DraggableNumber`/`ScalarInput`/`Vector*Input` chain) co-located with `handleInteractionStart/End`, plus the custom raw-pointer drags `LightDirectionControl` (pad), `AdvancedGradientEditor` (knot/bracket/bias), `EmbeddedColorPicker` (H/S/V). **SCRUB** at `TimelineRuler` (playhead) + `GraphSelectionBBox` (bbox-scale), anchored to `setIsScrubbing`. **DRAWING** at `DrawingOverlay` (freehand) + `useRegionSelection` (region draw/move/resize — drawing-ish, not a picker). Token from `INTERACTION_SOURCES` everywhere (no raw literals). **Scope boundary held:** wired only continuous-drag sites, NOT the discrete `handleInteractionStart('param')` ops (dropdown/toggle/menu/randomize/duplicate) nor the shim — KeyframeInspector value drags stay `slider` (connected `DraggableNumber`), `usePreviewTarget` is a click (discrete). Raw-pointer owners get `pointercancel`/`lostpointercapture` (mitigation #1); semantic sites rely on pointer-capture `pointerup` + hook unmount-cleanup + watchdog. **Mechanical coverage gate** `debug/test-interaction-coverage.mts` (`test:interaction:coverage`, in `test:gate`): asserts each producer's token + regression-guards every `useDragValue` consumer and `setIsScrubbing(true)` writer is wired/allowlisted. `test:gate` **green** (37 + 26 + 36 = 99), tsc clean, `app-gmt.html` boots clean (0 errors), throwaway headless probe confirmed a real slider drag opens `['slider']` and ENDs on release (200ms tail then idle). **→ user GO/NO-GO visual overlay pass** (slider/drawing/scrub tokens per input type, all END on release). **Next: P4** (switch consumers ADDITIVELY behind per-consumer flags, accum-drop proxy in parallel; re-run `bench:interaction-latency --mode=session --check` — slider going FINITE without camera regressing is the hard gate; idle-pause migrates by COMPOSITION).
- **2026-05-31:** **Latency root-cause pinned (the symptom that motivated the whole refactor) — it is GPU/present-bound, which RECONFIRMS P4 as the fix.** User reported, under a heavy scene (~15fps): ~0.75s before a slider responds / the resolution drops. Diagnosed with two zero-code console probes + a 78MB Performance trace (`h:/GMT/stuff/Trace-20260531T082243.json`, analysed offline):
  - **Input-dispatch latency = 1ms**, but the event is *delivered* ~0.75s after the physical press → the handler is prompt; the browser can't *deliver* the event sooner. Worst main-thread **rAF gap ≈ 300ms (spike 822ms)**.
  - **Trace verdict:** renderer main thread **88% idle**, render worker **98% idle**, React **~1%** — NOT CPU/JS/re-render bound. The long tasks (589/346/324ms) are all `CrGpuMain`, in `DXGISwapChainImageBacking::Present` / `SwapBuffers` / `ScheduleOverlays` (~4.6s of a ~5s trace; only ~89ms actual GPU command execution). **Chrome gates input dispatch + rAF on frame production**, which is stalled behind the GPU presents → the 0.75s. Confirmed: at no load, no lag.
  - **Implication (corrects a mid-investigation detour):** an earlier note said "P4 won't fix the lag (it's main-thread starvation)." That was WRONG once the trace showed GPU/present-bound. The dominant lever for a GPU-bound frame is **render fewer pixels during interaction** = adaptive resolution, and **P4's prompt session-driven engagement + seed-the-downscale-on-session-start is exactly that.** The P3b session is the enabler (instant `isInteracting()` at gesture-start vs the ~10-frame accum-drop guess). With adaptive OFF the lag is permanent (frames never get cheaper); that's why the user saw it "even with adaptive disabled."
  - **P4 expectations / don't-overclaim:** (a) irreducible floor — the *first* event that triggers engagement still waits ~1 heavy frame (input can't outrun frame production), so expect ~1 slow frame then responsive, not instant at 15fps; (b) **baseline static-frame cost is separate** — adaptive masks heaviness *during* interaction but the converged full-res frame is still GPU-expensive (scene/shader/quality workstream, not the session); (c) the `Present`/`ScheduleOverlays`-dominated profile may also carry a Windows DXGI/overlay present-path cost — lower-priority, separate look. The P4 latency harness (`bench:interaction-latency --mode=session --check`) measures input→downscale; the gate is "slider finite + beats baseline," NOT "instant."
