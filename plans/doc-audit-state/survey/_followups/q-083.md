---
question_id: q-083
source_subsystem: e12-mobile-layout
investigated_at: 2026-05-20T00:00:00Z
files_consulted:
  - path: plans/doc-audit-state/survey/e07-plugins-host.md
    blob_sha: f36148d5827ae175571d38b4b57bee308ad1fae2
  - path: plans/doc-audit-state/survey/e06-adaptive-resolution.md
    blob_sha: 944cdbcee1486b226494c4ffb22b08bc8638d3c5
  - path: plans/doc-audit-state/survey/a01-boot-shell.md
    blob_sha: 09d91a1fe272394a0b480357e7ef5f2cb65d5302
  - path: plans/doc-audit-state/survey/_followups/q-001.md
    blob_sha: 1adf21fd4a9748c8074e1e6b8fa5c98c196e5579
  - path: plans/doc-audit-state/survey/e12-mobile-layout.md
    blob_sha: 93c1b43b720096cc2fe9e6b40c5e072b77be3f62
  - path: plans/doc-audit-state/subsystems.json
    blob_sha: 3f53804426742719fb55da4db432659fc974528b
---

## Question
> `mobileMenu` façade, `MobileMenuHost`, `Menu.tsx`, `TopBar.tsx`, `useAppStartup.ts`, `HardwareDetection.ts`, and the topbar `when:` mechanism are referenced by the existing doc but were outside the claim list. They look like they belong to other subsystem audits (e07-plugins-host? a02-panels-layout?) — confirm before any rewrite of `17_Mobile_Layout.md` does not collide.

## Answer

All six items are already claimed and audited by other subsystems — none of them belong to e12-mobile-layout, and a17_Mobile_Layout.md rewrite must defer to those audits for any prose that touches them. **e07-plugins-host owns `engine/plugins/Menu.tsx`, `engine/plugins/TopBar.tsx`, the `mobileMenu` façade, `MobileMenuHost`, and the topbar `when:` mechanism** — confirmed by `subsystems.json` listing both files under e07 (`plans/doc-audit-state/subsystems.json:123-124`) and by the e07 survey covering exactly these surfaces: `mobileMenu` API at (`plans/doc-audit-state/survey/e07-plugins-host.md:72`), `MobileMenuHost` component at (`plans/doc-audit-state/survey/e07-plugins-host.md:74`), `TopBarHost`'s subscription to `uiModePreference` + `isDeviceMobile` so `when:` predicates re-evaluate at (`plans/doc-audit-state/survey/e07-plugins-host.md:118`), and the dual desktop-popover vs mobile-side-panel route at (`plans/doc-audit-state/survey/e07-plugins-host.md:123`). Confidence: high.

**e06-adaptive-resolution owns `engine/HardwareDetection.ts`** — `subsystems.json:102-105` places it in the "AdaptiveResolution + HardwareDetection + viewport" subsystem, and the e06 survey audits `detectHardwareProfile` / `detectHardwareProfileMainThread` plus the tier-derivation logic (`plans/doc-audit-state/survey/e06-adaptive-resolution.md:38-44, 73-76`). The `engine-gmt/engine/HardwareDetection.ts` duplicate is covered by g01-renderer (`plans/doc-audit-state/survey/g01-renderer.md:25, 91, 145-146`). Note that e06's mobile detection (`matchMedia('(pointer: coarse)') || innerWidth < 768` at `engine/HardwareDetection.ts:14-16`) duplicates e12's detector in `hooks/useMobileLayout.ts:5-7` — that's a documented cross-subsystem duplication, not an overlap in claims. Confidence: high.

**a01-boot-shell owns `hooks/useAppStartup.ts`** via followup q-001 — the file was originally flagged as "outside the audit scope" by a01 (`plans/doc-audit-state/survey/a01-boot-shell.md:69-70`), then resolved in q-001 which read `hooks/useAppStartup.ts:14-16, 113-161, 157-160` and documented the boot/hardware-detect/hydration handshake (`plans/doc-audit-state/survey/_followups/q-001.md:6, 20`). Crucially, q-001 confirms that `useAppStartup`'s mount effect is where "mobile auto-pick / adaptive-resolution overrides" are applied at boot — so any e12 rewrite that wants to describe how `uiModePreference='auto'` becomes a concrete mobile decision should *reference* this followup rather than redocument the startup hook. Confidence: high.

**Collision risk for the 17_Mobile_Layout.md rewrite is therefore low but real**: the e12 survey already restricts its drift table to its own claim list and explicitly marks the `mobileMenu` / `MobileMenuHost` / topbar-`when:` material as "Out of scope for this audit (those files were not in the claim list)" at (`plans/doc-audit-state/survey/e12-mobile-layout.md:92` open-questions list, and the drift-table row marked n/a). The rewriter should keep e12's lane narrow — `useMobileLayout`, `isMobileSnapshot`, `LandscapeGate`, `MobileScrollIntro`, `MobileViewportShell`, `MobileControls`, plus the `uiModePreference` / `isDeviceMobile` / `isPortrait` store fields — and for any cross-references into menu/topbar/hw-detect/boot wording, link to the e07/e06/a01 outputs rather than restating implementation details. The one open seam left is `engine-gmt/topbar.tsx:576-588`'s `UiModePreferenceMenuItem` (System-menu tri-state pill) — e12 flagged it as belonging to either the engine doc or an app-gmt doc, and that is a separate question (not q-083). Confidence: high.

Confidence: high

## Related findings

- e07-plugins-host already documents that `mobileActiveMenu` (the engine-store field e12's survey flagged at `store/slices/uiSlice.ts:184`) is the backing store for the `mobileMenu` façade (`plans/doc-audit-state/survey/e07-plugins-host.md:72, 123`). No additional audit is needed for that field.
- The `_hostMounts` counter that lets `Menu.tsx` switch render strategies only when a `MobileMenuHost` is mounted is a load-bearing dev-StrictMode guard (`plans/doc-audit-state/survey/e07-plugins-host.md:123`). If the e12 rewrite alludes to "mobile menus open in a side panel", it should defer the lifecycle detail to e07.
- HardwareDetection's mobile detector and e12's `useMobileLayout` detector are independent copies of the same heuristic (`matchMedia('(pointer: coarse)') || innerWidth < 768`) — e06 at `engine/HardwareDetection.ts:14-16`, e12 at `hooks/useMobileLayout.ts:5-7`. Worth a cross-doc note if either subsystem ever changes the threshold.
- `g01-renderer.md:206` already records that §4.3 of the g01-targeted doc mentions `useAppStartup` and `LoadingScreen` as a cross-layer narration concern — same pattern likely applies to the e12 rewrite: name-drop is OK, but the source of truth is q-001 + a01.
