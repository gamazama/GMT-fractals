---
question_id: q-104
source_subsystem: g03-formula-registry
investigated_at: 2026-05-20T09:55:09Z
files_consulted:
  - path: engine-gmt/types/fractal.ts
    blob_sha: 74584e314c74f4b6adf46385e5e605f4bf5321d7
  - path: plans/doc-audit-state/survey/g03-formula-registry.md
    blob_sha: 4e7fe1e6d00b476803f10e16ce2a992406265bb4
  - path: plans/doc-audit-state/survey/g05-engine-gmt-features.md
    blob_sha: f6c479bac5bd33f9eaa9ce72a7e6b6329b0d3a87
---

## Question
> `Preset.features: Record<string, any>` (`types/fractal.ts:50`) is intentionally untyped — the DDFS feature catalogue (g05) owns those types. Documenting per-feature schema is g05's responsibility.

## Answer
Confirmed. `Preset.features?: Record<string, any>` is at `engine-gmt/types/fractal.ts:50`, with the comment "GENERIC FEATURE STORAGE (Primary) — All module state lives here" at lines 48-49. The untyped `Record<string, any>` is deliberate: the concrete per-feature schemas are owned and documented by g05. The g05 survey (`plans/doc-audit-state/survey/g05-engine-gmt-features.md`) explicitly enumerates the per-feature type contracts in its "Public API surface" — `FeatureStateMap` / `FeatureCustomActions` at `engine-gmt/features/types.ts:26-50` compose 19 state slices and 3 action interfaces, and per-feature state types are re-exported via `export type { … }` from `features/index.ts:85-109` for store typing (line 128 of g05 survey). The "Architecture" section then walks each feature's own `index.ts` with its concrete state shape: `coreMath` (`core_math.ts:122`), `geometry` (`geometry/index.ts:168`), `lighting` (`lighting/index.ts:101`), `coloring` (`coloring/index.ts:44`), `volumetric` (`volumetric/index.ts:27`), `interlace` (`interlace/index.ts:117`), plus 10 flat-file features and 16 directory features summarized at lines 157-183.

Verdict: g05 genuinely owns this. The per-feature schema is NOT orphaned — each feature module is the source of truth for its own slice of `Preset.features`, and g05's survey already cites every feature file with line numbers. The g03 surveyor's deferral is correct; no Phase 2 home needs to be invented. If anything, Phase 2 documentation should cross-link `Preset.features` (in any g03-derived doc) to the g05 catalogue so readers can find the per-feature schemas, but the ownership boundary is sound.

## Confidence
high — `Preset.features` line confirmed verbatim; g05 survey explicitly lists `FeatureStateMap` (the type-level composition of all feature states) in its Public API surface and walks per-feature state shapes file-by-file with line numbers.

## Related findings
- `engine-gmt/features/types.ts:26-50` — `FeatureStateMap` + `FeatureCustomActions` composition (cited by g05 survey line 127).
- `engine-gmt/features/index.ts:85-109` — per-feature state type re-exports for store typing (cited by g05 survey line 128).
- g05 survey line 132 ("Three registration surfaces") documents that state features, UI components, and type augmentations are paired in `features/index.ts`, `features/ui.tsx`, and `features/types.ts` — this is the per-feature schema home.
- One asymmetric coupling worth noting (not orphan, but cross-subsystem): `Preset` also contains `savedCameras` (`engine-gmt/types/fractal.ts:46`), whose state per the g05 survey lives in a non-DDFS `cameraSlice` rather than `features.cameraManager` (g05 survey lines 156, 199, "CameraManager has empty params"). So a small slice of `Preset` state escapes the `features` bucket; that lives outside both g03 and g05's per-feature schema responsibility.
