---
id: q-021
parent: e02-tick-registry
status: resolved
verdict: PASS
target_hint: installRenderLoop
files_read:
  - engine/plugins/RenderLoop.tsx
  - docs/engine/01_Architecture.md
  - docs/engine/03_Plugin_Contract.md
  - docs/engine/04_Core_Plugins.md
---

# q-021: Where is `installRenderLoop({ store })` defined?

## TL;DR

**PASS (stale doc confirmed).** `installRenderLoop` does not exist anywhere in the
source tree — there is no function with that name, no barrel re-export, and no
`engine/plugins/index.ts`. The actual API exported by
`engine/plugins/RenderLoop.tsx` is the React component `<RenderLoopDriver />`,
which is mounted in the JSX tree rather than called imperatively at boot.

The references in `docs/engine/01_Architecture.md:90` and
`docs/engine/03_Plugin_Contract.md:43,54` are **stale**. Doc 04 already
acknowledges this in a comment at line 197: `// (No installRenderLoop — mount
<RenderLoopDriver /> in the React tree.)`. Docs 01 and 03 were not updated to
match.

## Evidence

### Grep across `h:/GMT/workspace-gmt/dev/` for `installRenderLoop`

Only five hits, all in docs or audit notes — zero in `.ts` / `.tsx` source:

| File | Line | Context |
|------|------|---------|
| `docs/engine/04_Core_Plugins.md` | 197 | `// (No installRenderLoop — mount <RenderLoopDriver /> in the React tree.)` |
| `docs/engine/03_Plugin_Contract.md` | 43 | `import { installShortcuts, installUndo, installTopBar, installRenderLoop } from '@engine/core-plugins';` |
| `docs/engine/03_Plugin_Contract.md` | 54 | `installRenderLoop();` |
| `docs/engine/01_Architecture.md` | 90 | `installRenderLoop({ store: engineStore });` |
| `plans/doc-audit-state/...` | — | this audit's own notes |

### `engine/plugins/RenderLoop.tsx` exports (verbatim)

```
20: export interface RenderLoopDriverProps {
27: export const RenderLoopDriver: React.FC<RenderLoopDriverProps> = ({ paused }) => {
```

No function exports. No default export. The module exposes one React component.

### Barrel / module checks

- `Glob h:/GMT/workspace-gmt/dev/engine/plugins/index.*` → no match (only
  `engine/plugins/RenderDialog/index.tsx`, which is the RenderDialog component,
  not a barrel).
- `Glob h:/GMT/workspace-gmt/dev/engine/core-plugins*` → no match. The
  `@engine/core-plugins` import path used in doc 03 does not resolve to any
  file in the tree.

## What the doc *should* say

Replace the `installRenderLoop({ store: engineStore });` boot example in
`docs/engine/01_Architecture.md:88-91` with the actual pattern used by the app
shell, e.g.:

```tsx
<EngineProvider engine={engine}>
  <RenderLoopDriver paused={paused} />
  {/* other plugin components */}
</EngineProvider>
```

`docs/engine/03_Plugin_Contract.md:43,54` similarly needs the `installRenderLoop`
import and call removed (the other `install*` helpers in that line —
`installShortcuts`, `installUndo`, `installTopBar` — also need verification but
are out of scope for q-021).

## Verdict

**PASS.** Question resolved: `installRenderLoop` does not exist; it is stale
documentation. Doc 04 is already correct. Docs 01 and 03 need to be fixed in a
later doc-rewrite pass. No code change required.
