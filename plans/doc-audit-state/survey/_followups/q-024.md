---
question_id: q-024
source_subsystem: e02-tick-registry
investigated_at: 2026-05-20T00:00:00Z
files_consulted:
  - path: engine/TickRegistry.ts
    blob_sha: 6b23e85eac534ebed2161a0ad6d13adec5ff64e9
  - path: docs/engine/01_Architecture.md
    blob_sha: 2e28552b34b192beac915d788875d8bf01eca537
  - path: docs/gmt/01_System_Architecture.md
    blob_sha: 8a4e35e14347de931d3c57933cfd900fa05d7541
---

## Question
> The dev warning message at TickRegistry.ts:71 cites `docs/01_Architecture.md § render-loop` — is that the canonical path now that docs live at `docs/engine/01_Architecture.md`?

## Answer
No — the path cited in the warning string is stale. `engine/TickRegistry.ts:71` emits the literal text `docs/01_Architecture.md § render-loop`, but no such file exists at the docs root: a Glob for `docs/01_*.md` returns no matches. The architecture doc tree now has two `01_*` files: `docs/engine/01_Architecture.md` and `docs/gmt/01_System_Architecture.md`.

The canonical render-loop reference for the engine substrate (which is what TickRegistry belongs to) is `docs/engine/01_Architecture.md`. That file has an explicit `## The render-loop contract` H2 at line 81, describes the `<RenderLoopDriver />` component and `installRenderLoop({ store })` one-liner the warning's prose points users toward (line 87–90), and even calls out this same 3-second dev warning as the mitigation for risk "F4 — Implicit render-loop contract" at line 185. The GMT doc `docs/gmt/01_System_Architecture.md` describes the render loop in app terms (`## 3. The Render Loop (FractalEngine.ts)` at line 142) but is not engine-substrate material and doesn't cover `runTicks`/`registerTick`.

Recommended fix: update the warning string at `engine/TickRegistry.ts:71` from `docs/01_Architecture.md § render-loop` to `docs/engine/01_Architecture.md § The render-loop contract` (matching the actual H2 wording).

## Confidence
high — the warning text is exact, the file-existence check is definitive (no `docs/01_Architecture.md`), and the target heading was located by Grep in the engine doc with matching subject matter (runTicks, RenderLoopDriver, the 3s warning itself).

## Related findings
- `docs/engine/01_Architecture.md:185` self-references the TickRegistry 3-second warning as the mitigation for the implicit render-loop contract — so this doc is unambiguously the intended target, and a Phase 2 writer can wire the citation both directions.
- The exact H2 in the engine doc is `## The render-loop contract` (line 81), not `§ render-loop`. If keeping fragment-style citations, either rename the heading or adjust the citation to match (`§ The render-loop contract`).
- `docs/gmt/01_System_Architecture.md:142` (`## 3. The Render Loop`) is a separate app-level discussion and should not be confused with the engine contract.
