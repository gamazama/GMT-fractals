# Phase 2 handoff — 2026-05-20 08:35Z → next session at ~11am

## Where we are

Phase 2 prep + 8 module docs verified. Phase 2 is mid-flight; nothing is broken.

**Sub-phase B status (separate session):** unknown — running independently. Check `plans/doc-audit-state/open-questions.json` for live count of pending/answered. Last known state from this session's POV: ~40 low-priority questions remaining.

**Phase 2 module docs already verified** (all PASS `verify-doc.mjs`):
1. `docs/modules/engine/tick-registry.md` — e02
2. `docs/modules/engine-fork-rules.md` — cross-cut (only one we said to do in P2; other 9 cross-cuts deferred to P2.5)
3. `docs/modules/engine/feature-system.md` — e01
4. `docs/modules/app-gmt/boot-shell.md` — a01
5. `docs/modules/engine/shortcuts-undo.md` — e08
6. `docs/modules/engine/animation.md` — e03
7. `docs/modules/engine/plugins-host.md` — e07
8. `docs/modules/engine-gmt/navigation.md` — g04

## Phase 2 state files (all in `plans/doc-audit-state/`)

- `phase-2-disposition.json` — 87 docs classified (7 rewrite / 1 migrate / 51 preserve / 10 minor / 18 keep)
- `phase-2-carry-in.json` — 24 subsystems with cross-cutting signals indexed; 10 cross-cut themes
- `phase-2-new-subsystems.json` — e09b + g11 profiled, ready to add (decision: dual-claim on `camera_manager/index.ts` overlap)
- `phase-2-brief-draft.md` — the writer brief, refined 5x across pilots. **Ready to copy into LOOP_PROMPT.md once Sub-B finishes.**
- `scripts/extract-bugs.mjs` — auto-generates `docs/modules/bugs.md` from module-doc Known Issues
- `scripts/verify-doc.mjs` — extended with `additional_sources` (backward-compatible)

## Decisions locked in (last session, awaiting execution)

1. **g11/g05 overlap on `engine-gmt/features/camera_manager/index.ts`**: dual-claim (both subsystems list it; harmless).
2. **Cross-cutting docs**: only `engine-fork-rules.md` in Phase 2 (DONE). Other 9 (uniform-plugin-contract, DDFS string-contract, etc.) deferred to Phase 2.5 with user review.
3. **Production bugs**: surface in each module doc's "Known issues" + auto-generated `bugs.md` index via `extract-bugs.mjs`.

## Remaining work (next session, in priority order)

### Immediate (Phase 2 bulk writer run)

21 subsystems still need module docs. Each takes ~3-5 min wall-clock per writer agent at PASS rates. Parallelize 3-5 at a time:

**Standard (19, parallelizable):**
- e04-shader-builder (5 files, no existing doc, 8 followups)
- e05-render-pipeline
- e06-adaptive-resolution
- e09-camera-plugin
- e10-engine-features (10 files, no existing doc, 6 answered + 4 pending — q-072..076)
- e11-worker-contract
- e12-mobile-layout
- e13-shared-ui
- g01-renderer (8 files, rewrite disposition)
- g02-shader-pipeline
- g05-engine-gmt-features
- g06-bucket-render
- g07-mesh-export
- g08-save-load-gmf
- g09-modular-graph
- g10-audio-fps-sync
- a02-panels-layout (5 files, minor-edits)
- a03-tutorial
- t02-fractal-toy (14 files)

**Heavy — special handling needed:**
- g03-formula-registry (~56 formula files; consider chunked approach or accept longer budget)
- t01-fluid-toy (80 files; same)

**New (depend on `subsystems.json` edit first):**
- e09b-state-library-ui (entry in `phase-2-new-subsystems.json` ready)
- g11-gmt-camera-manager (entry ready)

### After bulk writer run

1. Add e09b + g11 entries to `subsystems.json` (use `phase-2-new-subsystems.json`). Then write module docs for them.
2. Run `node plans/doc-audit-state/scripts/extract-bugs.mjs` to generate `docs/modules/bugs.md`.
3. Re-blob-sha d03's 10 doc entries in coverage.yaml (was deferred — coverage.yaml race risk; safe to do once Sub-B is done).
4. Replace the "TBD" Phase 2 section in `LOOP_PROMPT.md` with the contents of `phase-2-brief-draft.md` (only safe once Sub-B has finished — until then it might collide with review-tick edits to LOOP_PROMPT.md).
5. Phase 2.5: 9 deferred cross-cutting docs (uniform-plugin-contract, DDFS-string-contract, etc.) — needs user review before authoring since they're prescriptive.

## Brief that works — paste into agent spawn

See `phase-2-brief-draft.md`. Five refinements landed across pilots:
- `additional_sources` for multi-file public_api
- REPO_ROOT-relative paths (no `dev/` prefix)
- Real wall-clock `audited` timestamps
- `depends_on: []` valid for leafs; types-only imports count
- Bare-filename anchors trap the verifier (always prefix directory)
- Pending followups: skip defensively + note "may need regen"
- No-existing-doc subsystems: use Historical context for in-source rationale comments

## Coordination protocol (CRITICAL — re-read at session start)

The Sub-B session and this session share files:
- **Sub-B owns**: `open-questions.json`, `coverage.yaml`, `review-state.json`, `reviews/`, `_followups/`, `LOOP_PROMPT.md`, `protocol-feedback.json`.
- **Phase 2 session owns**: `subsystems.json` (e09b/g11 additions), `phase-2-*.json/md` files, `docs/modules/**`, the two scripts.
- **NEVER** call `record-coverage.mjs` from Phase 2 work (race risk).
- **NEVER** edit `LOOP_PROMPT.md` from Phase 2 until Sub-B is fully done.

## Quick state-check commands

```bash
# Sub-B progress
cd h:/GMT/workspace-gmt/dev
node -e "const a=JSON.parse(require('fs').readFileSync('plans/doc-audit-state/open-questions.json','utf8')); const c={pending:0,answered:0,investigating:0}; a.forEach(q=>c[q.status]=(c[q.status]||0)+1); console.log(c);"

# Phase 2 progress
ls docs/modules/**/*.md 2>/dev/null | wc -l

# Verify everything still passes
for f in $(find docs/modules -name '*.md'); do node plans/doc-audit-state/scripts/verify-doc.mjs "$f"; done

# Regenerate bugs.md
node plans/doc-audit-state/scripts/extract-bugs.mjs
```

## Notes for orchestrator at 11am

1. First action: run the three state-check commands above to see what changed during the gap.
2. If Sub-B finished (no pending), proceed with step 1-5 in "After bulk writer run" above.
3. If Sub-B still mid-flight, continue parallel Phase 2 writer batches from "Standard (19)" list. Stay out of LOOP_PROMPT.md and coverage.yaml.
4. Always batch parallel writers in 3-5 — single calls underuse the budget; >5 increases coordination overhead with diminishing returns.
5. Each writer agent uses ~100k tokens. Budget accordingly.

Session ended ~08:35Z on 2026-05-20 with the user near their context/quota limit.
