---
review_id: review-2026-05-20-14
generated_at: 2026-05-20T07:25:00Z
feedback_items_reviewed: [f-005]
iterations_window: [q-083, q-084, q-086, q-088, q-089, q-092, q-093, q-094, q-095, q-096]
---

## Patterns observed

- **Frontmatter shape: 10/10 canonical.** Every followup in this window uses the four-name canonical shape (`question_id`, `source_subsystem`, `investigated_at`, `files_consulted`). None of the nine blacklisted alternative names appear at the top level. Edit 1 from review #13 (the "if your draft contains ANY of `id:`, `parent:`, `status:`, `verdict:`, `files_read:` — stop and start over" self-check sentence) appears to have worked: window-by-window deviant count is now `q-001..q-015: 2/10 → q-016..q-028: 0/10 → q-029..q-042: 4-5/10 → q-043..q-061: 0/10 → q-062..q-078: 1/10 → q-083..q-096: 0/10`. Two clean windows after the regression, with the explicit self-check sentence as the only intervening change. The variance has stabilised; orchestrator-side validation is not needed.

- **Window timing trues up to 4.16 min/iter (slightly slower than prior 3.7-3.95).** Per open-questions.json completion timestamps: q-083 04:41:42Z → q-096 05:23:20Z = 41.6 min for 10 iters. The slower pace is consistent with this window's heavier content — q-083 reads 6 survey files (cross-subsystem boundary verification), q-094 reads 4 worker+pipeline files, q-096 reads 5 cross-doc files. Mean files_consulted in this window ≈ 3.6, vs ~2.5 in the q-062..q-078 window. Not a regression — just a heavier-investigation tranche. Sub-B finish projection updates: 57 pending × 4.16 min ≈ 3.95 hours wall-clock remaining (down from 4.4h projected last review, given 10 iters consumed).

- **Followup-corrects-survey rate this window: 4/10.** Falls within the 20-50% band review #12 called stable. Concrete corrections:
  - **q-083** (mobile-layout boundary): Confirms six items the e12 survey flagged as "outside the audit scope" are claimed by other subsystems (e07 owns Menu/TopBar/mobileMenu/MobileMenuHost/topbar `when:`, e06 owns HardwareDetection, a01-via-q-001 owns useAppStartup). No drift in survey claims — but it overrides e12's implicit framing of those files as orphans. Acts as a cross-subsystem **claim-graph verifier** — a new kind of correction (the survey didn't make a wrong factual claim, it failed to follow up on what was already claimed elsewhere).
  - **q-092** (BucketRenderer defaults): Verifies three default sites agree byte-for-byte (FractalEngine seed, BucketRunner config cache, renderControlSlice seed). No drift today, but documents the redundancy explicitly so a future change can be flagged. Cleanup-target finding: store doesn't seed `accumulation`, and `samplesPerBucket` is typed optional but always set — type-tightening candidates.
  - **q-094** (checkHalfFloatAlphaSupport stub): Surveyor flagged the main-thread stub returning `true` as a potential "lying proxy". Followup proves it's harmless: zero main-thread callers exist (cross-app grep), the worker-side probe is itself dead code (no internal consumers; only logs a dev message), and RenderPipeline keys off `_qualityState.bufferPrecision` not the probe. Three-way dead-code confirmation. Reframes "lying proxy" as "deliberate no-op with vestigial real-side probe".
  - **q-096** (ShaderBuilder class-name collision): Confirms the `engine/ShaderBuilder.ts` (136L generic) vs `engine-gmt/engine/ShaderBuilder.ts` (634L GMT 17-position assembler) split, documents that `21_Code_Review_2026-04-25.md` already recommended renaming to `GmtShaderBuilder` (rename NOT done), and confirms `docs/gmt/08_File_Structure.md:20` points the path `engine/ShaderBuilder.ts` to the GMT-semantic description (silent path-vs-semantics mismatch). **Genuine doc-vs-code mismatch caught by the followup.** Recommends both renaming + cross-callouts in 08_File_Structure.md, with `RenderVariant` enum sharing called out as the cleanest hand-off point at rename time.

  Three of these four (q-092, q-094, q-096) are **dead-code / vestigial-code identifications**, which is a sub-pattern worth noting separately — see next bullet.

- **New sub-pattern: dead-code / dead-feature-flag accumulation across engine-gmt.** Three findings in this single window:
  - **q-094**: `FractalEngine.checkHalfFloatAlphaSupport` is public but zero internal callers. Probe runs, logs to dev console, return value is discarded. Twin on main-thread `WorkerProxy` is hardcoded `true`. Three vestigial layers total.
  - **q-095**: `ShaderBuilder.buildFragment` Main variant builds an 18-entry `sections` array, derives a `profile` string and `totalSize` sum — but the `return` template literal that follows interpolates raw section strings directly and never touches `sections` / `profile` / `totalSize`. ~25 lines of dead computation per Main-variant build. Comment `// Section size profiling — log where the bytes are` suggests a one-off diagnostic where the `console.log(profile)` was removed but the setup was left behind.
  - **q-093**: Three-fragility chain (`renderer.properties` private Three API, `WEBGL_debug_shaders` extension dependency, defensive type-check for `fragmentShader` being string vs WebGLShader object). Not strictly dead, but the consumers are diagnostic tools (`debug/bench-shader.mts`, `debug/inspect-state.mts`) — a Three minor bump silently degrades them.

  Cumulative count for Phase 2 carry-in "dead-code / cleanups": review #13 said ~15; this window adds 3 (q-094 probe chain, q-095 profile block, q-092 type-optional-but-always-set). Running estimate now ~18-20. Phase 2 "engine-gmt cleanup pass" or a code-health doc section becomes well-justified.

- **New sub-pattern: shared-UI-primitive coupling to GMT-app concepts.** q-088 and q-089 both surface coupling between `components/` (the shared engine UI tree) and GMT-app conventions.
  - **q-088** (Knob vs Slider context coupling): Knob uses `useEngineStore` granular selectors; Slider uses `StoreCallbacksContext`. Mechanically a one-line migration; the only real blockers are (a) verifying every host memoises `storeCallbacks` so the Provider value is stable across re-renders, and (b) the fluid-toy max-depth-guard scar tissue should be re-tested. Same migration applies to Dock, DropZones, Vector*Input.
  - **q-089** (AutoFeaturePanel hardcoded compile route): `AutoFeaturePanel` (in shared `components/`) imperatively calls `movePanel('Engine', 'left')` and emits `FractalEvents.emit('engine_queue', ...)` — both are GMT-app-only concepts. Recommends a `compileRouter` capability on the store, defaulted to GMT's behaviour, overridable per app. The 50ms `setTimeout` is racing against EnginePanel mount.

  Both findings are doc-rewrite candidates for `docs/engine/05_Shared_UI.md` (Phase 2 module doc). Together they document a **real "primitives must not know about app shape" violation** that affects four primitives (Knob, Dock, DropZones, Vector*Input) and one panel system (AutoFeaturePanel). Phase 2 carry-in.

- **Followup q-070 frontmatter (the lone deviant from review #13) is now the unique exception across 62 followups.** Recommended hand-patch (rather than further brief tightening) stands.

- **Cadence at ≥10 — hold for #15. Don't extend to ≥12 yet.** Three reasons:
  - **(a) Phase-2-priority findings continue to surface.** This window: q-094's three-layer dead-code confirmation, q-095's ~25-line dead profile block, q-096's `08_File_Structure.md` mismatch + pending rename, q-088+q-089's shared-UI-coupling pattern. None of these are emergencies, but extending cadence to ≥12 would have meant the q-096 mismatch surfaces ~2 iterations later — mildly bad for Phase 2 module-doc planning since `08_File_Structure.md` is one of the affected docs.
  - **(b) f-005 was opened this window** (record-coverage flag-vs-stdin mismatch raised by the q-083 agent — see Feedback section below). Holding at ≥10 means a single open feedback item entering review #14 within ~6 iters of opening; ≥12 would push that to ~8 iters.
  - **(c) Two consecutive 0/10 frontmatter windows isn't enough evidence to relax.** Review #13's stated condition for ≥12 extension was "all three lagging indicators come back clean" — variance YES (0/10), bugs NEUTRAL (no new production bugs this window — see below), followup-correcting-followup NEUTRAL (q-094 reframes a survey, not another followup). Conditions partially met; one more clean window in #15 would justify ≥12 starting at #16.
  - **Verdict:** Hold at ≥10 for #15. If #15 also has (i) ≤1/10 frontmatter variance, (ii) no new f-NNN entries opened, and (iii) no followup-correcting-followup case, propose ≥12 at #16. Don't jump straight to ≥15.

- **Bug surface this window: zero new production bugs.** Running rate stays at 2-in-62 = 1-in-31, drifting toward 1-in-35 if the next window also has zero. q-094 looks like a "lying proxy" bug at first read but turns out to be deliberately dead (no consumer); q-092 is a redundancy not a bug; q-095 is dead code not a bug. The 2 production bugs (q-019, q-064) remain the only true production-bug surfaces of the Sub-B run.

## Proposed LOOP_PROMPT.md edits

**One small edit** to address f-005's `confidence` value mismatch.

### Edit 1 — fix stale confidence values in coverage-recording instructions

- **Where:** Section "### 3. Dispatch by action" → "Action: `summarize-existing`" block, and Section "### Sub-phase B — Resolve" step 6.
- **Current text (line 142):**
  ```
  After agent returns: same coverage-recording flow as audit/create, with `agent_run_id: <id>-summary` and `confidence: catalog`. Mark `status: "summarized"`.
  ```
- **Proposed text:**
  ```
  After agent returns: same coverage-recording flow as audit/create, with `agent_run_id: <id>-summary` and `confidence: partial` (note: `record-coverage.mjs` validates confidence ∈ {high, medium, low, partial}; the legacy `catalog` value is rejected). Mark `status: "summarized"`.
  ```
- **Current text (line 342):**
  ```
  6. For each file in the answer's `files_consulted`: append a coverage entry (same direct-append pattern as Phase 1) with `agent_run_id: <id>-followup`, `confidence: targeted`. Overlap with Phase 1 coverage is fine — the file gets a second entry attesting a deeper look.
  ```
- **Proposed text:**
  ```
  6. For each file in the answer's `files_consulted`: append a coverage entry (same direct-append pattern as Phase 1) with `agent_run_id: <id>-followup`, `confidence: high` (or `partial` if the followup only read a portion of the file; `record-coverage.mjs` validates confidence ∈ {high, medium, low, partial} — the legacy `targeted` value is rejected). Overlap with Phase 1 coverage is fine — the file gets a second entry attesting a deeper look.
  ```
- **Why:** `record-coverage.mjs:27` declares `VALID_CONFIDENCE = new Set(['high', 'medium', 'low', 'partial'])` and rejects any other value with a die-on-invalid error. The orchestrator coverage-write currently uses only `high` and `partial` in practice (grep against coverage.yaml: 627×high, 75×partial, zero×catalog, zero×targeted). The LOOP_PROMPT.md references to `catalog` and `targeted` are aspirational/stale and would have caused the script to fail if the orchestrator had ever followed them literally. Addresses f-005's secondary observation that confidence values are `high|medium|low|partial` (no `full`).
- **Risk:** Low. Pure clarification. Orchestrator behaviour is already correct; this just makes the docs match the code so a future re-reader doesn't introduce a regression by copying the stale value.

### Not proposing: f-005's primary observation (record-coverage interface)

f-005's headline complaint — "brief tells agents to call record-coverage.mjs with --path/--blob-sha/etc flags, but the script reads stdin YAML" — does NOT match the current LOOP_PROMPT.md. The current brief at line 76-89 describes the **orchestrator** appending a coverage entry (with the YAML shape shown), not the agent. The agent brief at lines 39-72 (audit/create) and 99-139 (summarize-existing) and 301-336 (Sub-B) does NOT mention `record-coverage.mjs` at all. So the issue f-005 describes is in the **orchestrator's spawn brief text** (the in-flight description it sends to agents about coverage recording), which the orchestrator has reportedly already updated. The LOOP_PROMPT.md itself does not contain the stale `--path` / `--blob-sha` flag examples — confirmed by `grep -n "record-coverage" LOOP_PROMPT.md` returning zero matches. **No LOOP_PROMPT.md edit is needed for f-005's primary observation.** Mark f-005 addressed.

### Not proposing: any structural / cadence / resumption edits

- Resumption logic, iteration step structure, priority/sort: untouched.
- Sub-B cadence stays at ≥10 (see Patterns observed → cadence bullet for rationale).
- Sub-A retrospective edit (Edit 1 from review #11) remains held back per "DO NOT modify open-questions.json mid-Sub-B" constraint.

## Feedback items to mark wont-fix

(none new)

## Feedback items to keep open

(none — f-005 should be marked **addressed** after this review:
- Primary observation (flag-vs-stdin interface): already fixed in the orchestrator's spawn brief per the tick prompt; LOOP_PROMPT.md doesn't reference the stale interface so no edit needed there.
- Secondary observation (confidence values): addressed by Edit 1 above.)

## Notes for the human

- **f-005 confidence-value cleanup is a Phase 2 input too.** When the Phase 2 writer touches `06_Code_Health.md` or whichever doc covers the audit infrastructure, the `record-coverage.mjs` interface (stdin YAML, valid confidence values) should be documented in the script header itself if not already — `record-coverage.mjs:14-16` does document required fields but doesn't enumerate the valid confidence set in the header comment (only at line 27 in code). Low priority; one-line doc addition.

- **Three new dead-code findings this window (q-094 probe chain, q-095 profile block, q-092 typed-but-always-set field) push the engine-gmt cleanup carry-in to ~18-20 items.** Recommend Phase 2 spin a dedicated "engine-gmt cleanup pass" sub-task or a section in `docs/engine/06_Code_Health.md` (if that's the right home) rather than scattering the findings across module docs.

- **q-088 + q-089 are a coherent shared-UI-purity violation story.** Phase 2's `docs/engine/05_Shared_UI.md` rewrite (already flagged as aspirational per review #4) should fold both:
  - The `useEngineStore` granular-selector pattern (Knob, Dock, DropZones, Vector*Input) — documented as a project-wide convention that's mechanically migratable to `StoreCallbacksContext` once the per-host memoisation invariant is verified.
  - The `movePanel('Engine', 'left')` + `engine_queue` GMT-app coupling in `AutoFeaturePanel` — documented as a known leak with a `compileRouter` capability fix proposal.
  Together they're a clean Phase 2 story: "primitives currently know about app shape; here's the migration target".

- **q-096's `08_File_Structure.md` mismatch is a small but visible doc bug.** `docs/gmt/08_File_Structure.md:20` lists `engine/ShaderBuilder.ts` with semantics that actually describe the GMT-side 634-line class. A contributor opening the listed path lands on the wrong file relative to the description. Two ways to fix: (a) Phase 2 rewrites `08_File_Structure.md` with both engine-core and engine-gmt entries, or (b) do the `engine-gmt/engine/ShaderBuilder.ts` → `GmtShaderBuilder.ts` rename recommended by `21_Code_Review:168` (still pending) and then update the doc. The rename is the right fix; the doc edit alone would mask the underlying naming collision.

- **q-093 fragility-audit gap is a small one-paragraph addition to `docs/engine/20_Fragility_Audit.md`.** `FractalEngine.getCompiledFragmentShader` / `getTranslatedFragmentShader` reach into `(this.renderer as any).properties.get(material)` — private Three API, pinned at `^0.162.0`. Blast radius is diagnostic-only (bench-shader, inspect-state) but the failure mode is silent (returns null). Phase 2 task: add one paragraph + the Three-version pin reference.

- **q-083's "claim-graph verifier" pattern is a new kind of correction worth noting.** Most followup-corrects-survey cases catch wrong facts. q-083 instead catches a **gap in the surveyor's awareness of what other subsystems already claim** — the e12 survey labelled six items "outside the audit scope" when they were already claimed by e06/e07/a01. A Phase 2 indexer could surface this proactively by cross-referencing every survey's "outside scope" / "out of audit" mentions against the `files_claimed` of other subsystems. Out of scope for the loop, but worth noting for Phase 2 tooling design.

- **Watchlist for review #15 (fires after ~q-106):**
  - (a) Frontmatter variance: holding at 0/10 for a third consecutive window cements Edit 1 from review #13. ≥1/10 means another self-check tightening or orchestrator-side validation.
  - (b) Bug surface: if #15 also has zero, the 1-in-31 rate is the stable floor; expect ~1-2 more bugs across the remaining ~50 iterations.
  - (c) Cadence decision: if (a) holds AND no new f-NNN entries open AND no followup-correcting-followup case, extend to ≥12 at #16.
  - (d) Dead-code findings continue accumulating: if #15 adds 2+ more, the "engine-gmt cleanup pass" Phase 2 sub-task is firmly justified.
  - (e) confidence-value cleanup (Edit 1 above) verification: the change is doc-only; nothing should change in coverage.yaml since orchestrator already writes `high`/`partial`. Spot-check #15's coverage.yaml diff to confirm no regression.

PASS.
