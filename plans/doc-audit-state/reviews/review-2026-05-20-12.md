---
review_id: review-2026-05-20-12
generated_at: 2026-05-20T03:40:00Z
feedback_items_reviewed: []
iterations_window: [q-043, q-045, q-046, q-052, q-053, q-054, q-055, q-056, q-060, q-061]
---

## Patterns observed

- **Edit 2 from review #11 worked — frontmatter shape is now 10/10 canonical on field names.** Every followup in this window uses `question_id`, `source_subsystem`, `investigated_at`, `files_consulted`. None of the prior deviants (`id`, `parent`, `parent_survey`, `subsystem_id`, `target`, `status`, `verdict`, `files_read`, `target_hint`) appear. The window-by-window deviant count is now `q-001..q-015: 2/10 → q-016..q-028: 0/10 → q-029..q-042: 4-5/10 → q-043..q-061: 0/10`. The cleaner brief (explicit nine-name blacklist + the pointer-back `files_consulted` requirement) reverted the regression. One sub-variance remains: **q-043's `files_consulted` block uses a bare string list (`- engine/worker/ViewportRefs.ts`) instead of nested `- path: ... blob_sha: ...` entries.** Same shape that broke `files:` parsing in d03 / d08, and the orchestrator's resumption logic for followups is more tolerant than the survey parser (Phase 2 may or may not be), so it's not breaking today — but if a Phase 2 indexer tries to walk `files_consulted` by struct, q-043 will be the lone non-conforming entry across 52 followups. Not worth a brief edit this tick — it's 1/52 — but worth surfacing as a Phase-2 input-cleanliness note.

- **Followups catching surveyor errors is the dominant signal pattern this window — 5/10 corrections, not just 2/10.** The brief flagged q-052, q-053, q-056, q-060, q-061 as correction cases; verified all five hold and quantified:
  - **q-052** (BucketRenderPanel imports): Surveyor claimed three imports might reach into `engine-gmt/`. Followup confirms (a) `../../FractalEvents` → `engine/FractalEvents.ts` (correct, engine-core), (b) `../../../store/engineStore` → `store/engineStore.ts` (correct), and (c) **the third import path the question lists — `../../export/BucketRenderTypes` — does not exist in the file at all**. Surveyor invented an import. The only `engine-gmt` token is an attribution comment on line 4.
  - **q-053** (Help.tsx _supportModal DOM leak): Surveyor framed `uninstallHelp` as causing a portal DOM leak. Followup shows (a) the portal is rendered by React `createPortal` from `SupportModalHost`, which lives inside `<HelpOverlay/>`, which `uninstallHelp` does not touch — no DOM leak possible, only a state leak across reinstall cycles (one-line fix). (b) `uninstallHelp` is **never called** in the codebase — the bug is doubly theoretical. Survey framing inverted "state leak" as "DOM leak".
  - **q-056** (Ctrl+Shift+Z priority-10 override): Surveyor implied the `priority: 10` was load-bearing. Followup traces the registration order (Undo plugin at main.tsx:239 registers `Mod+Shift+Z` first; camera-undo at main.tsx:415 registers later) — under the stable-sort tie-break rule (most-recently-registered wins), the camera-undo would already win at `priority: 0`. **`priority: 10` is defensive, not necessary.** Also catches that the comment in `app-gmt/main.tsx:408-414` ("engine-plugin shortcuts register first and would steal the binding") inverts the tie-break direction — registering first means losing ties.
  - **q-060** (animation store undo defensive check): Surveyor flagged `typeof fn === 'function'` guard as potentially needed. Followup confirms `undo`/`redo` are declared in `SequenceSlice` interface (`types.ts:142-143`), always spread into the store, never optional — guard cannot fail. Dead defensiveness, not a real concern.
  - **q-061** (StateLibrary UI primitives subsystem boundary): Surveyor put `<StateLibraryPanel>` + `<StateLibraryToast>` + `<ActiveSnapshotFeatures>` under e09-camera-plugin. Followup shows all three are **app-agnostic generic UI primitives** keyed on `createStateLibrarySlice`'s public contract (`StateSnapshot<T>`, `toastFieldKey`, `activeIdKey` convention). Camera plugin is just one consumer. **Recommends spinning a dedicated `e09b-state-library-ui` audit slice** instead of folding them into e09.

  Five corrections in ten followups is a 50% correction rate — substantially higher than prior windows. This is not a problem; **it's strong evidence the followup phase is doing real verification work, not rubber-stamping surveys**. q-019 (production bug) and q-040 (wrong field names in source survey) were the first signs of this; q-052 / q-053 / q-056 / q-060 / q-061 cement the pattern. The "trust the survey verbatim" risk for Phase 2 writers is now well-quantified: **followups overrule surveys on facts, not the reverse.** Phase 2 module-doc writers should walk `_followups/q-NNN.md` for any survey claim that overlaps a followup target.

- **Phase 2 carry-in density still high — ~8/10 actionable iterations.** Spot-check:
  - q-043 (`mouseOverCanvas` is ref-based, not store-based): Confirms intentional design — pointer hover must not trigger React reconciliation in the adaptive-resolution hot path. Doc-worthy invariant.
  - q-045 (`HardwareProfile` full caps shape): Documents the 4-field shape with enum semantics for `precisionMode` (0=High, 1=Standard) and `bufferPrecision` (0=Float32, 1=HalfFloat16). Catches that `types/viewport.ts` is misnamed-by-scope — single source of truth for the whole viewport-quality + adaptive-res + hardware-caps surface. Flags `supportsFloat32` and `caps.bufferPrecision` as redundant (drift risk).
  - q-046 (CompositionOverlay): Confirms it's a shared cross-app viewport overlay subsystem (engine-core uiSlice, shared controls, auto-mounted by ViewportFrame), not GMT-only. Belongs in the viewport subsystem doc.
  - q-052: Three concrete confirmations of engine-core path resolution + one phantom-import correction.
  - q-053: Documents singleton lifecycle invariant (`_supportModal` not cleared by `uninstallHelp`, state-leak-not-DOM-leak) — Phase 2 should describe the actual residual concern (cross-install state leak) and the one-line fix, not the inverted surveyor framing.
  - **q-054 (SceneIO loadSceneFile vs loadScene vs loadPreset)**: Highest-density iteration this window. Documents the three-layer loader pipeline (`File → loadSceneFile → loadScene → loadPreset`), every consumer of each layer, the GMT-specific parser injection (`gmtRegistry.register + REGISTER_FORMULA emit`), and the post-boot fallback-sphere bug if `loadPreset` is called directly. **Phase 2's docs/engine/04_Core_Plugins.md and the SceneIO docstring both need this.** Also proposes a `loadSceneFromFile` convenience helper to make the layering uncrossable.
  - q-055 (`pushCameraTransaction` + `timeline-hover` scope): Confirms both doc 06 references; locates `pushCameraTransaction` at `store/slices/historySlice.ts:79+199`, `useShortcutScope('timeline-hover', isTimelineHovered)` at `components/Timeline.tsx:59`. Notes the `priority: 10` convention for hover-shadowed timeline shortcuts (consistent with q-056's finding).
  - q-056: Correction + cleanup target (the inverted comment in `app-gmt/main.tsx:408-414`).
  - q-060: Cleanup target only (dead defensiveness — not load-bearing for Phase 2).
  - q-061: **New audit slice recommendation** — e09b-state-library-ui. Subsystem-shape correction with concrete naming.

  Carry-in count: ~8/10 substantive (q-043, q-045, q-046, q-052, q-054, q-055, q-056, q-061). Two cleanup-only (q-053 framing reframe, q-060 dead-code note). **Density holds at 80%, matching the last three windows.**

- **Bug surfaces this window: zero new production bugs beyond q-019.** Running rate now 1 in 32 followups. The "1 per 10-15" projection from review #10 is firmly disproven; the more honest projection is **1 production bug per ~25-35 followups**. Across the remaining ~77 pending questions, expect ~2-3 more genuine bug finds. That's a meaningful Phase-2 input but no longer a strong cadence argument for keeping reviews tight.

- **Sub-A retrospective from review #11 is **partially** vindicated by this window.** Re-checking the window for explicit-defer phrasing in question text: q-045 says "out of audit", q-055 says "live outside the two audited files... Not verified here", q-061 says "not audited here". Three of ten questions are explicit hand-offs that **could have been triaged with `defer:<subsystem-id>` hints** if Sub-A had run the Edit-1 logic from review #11. They weren't pure pointer-backs (they were genuine cross-subsystem investigations, not "the answer is over there, go look") so the savings would have been smaller than q-033/q-036's pointer-backs — maybe 30-60 seconds each from "go straight to file X" guidance rather than full triage. But it does suggest the explicit-defer signal is **more common than the q-033/q-036 sample implied**: ~30% of questions in this window, not ~20%. The Edit-1 proposal stands; the human-action one-shot re-triage option from review #11 looks more attractive if anyone wants to spend 5 min on it.

- **Cadence at ≥10 — hold for one more cycle, then consider ≥12.** Arguments:
  - **For extending:** Zero protocol-feedback in 5 consecutive windows. Frontmatter variance is now 0/10 (Edit 2 from review #11 fully worked). Brief edits are decelerating naturally — the last two reviews proposed Edit 1 + Edit 2; review #11 only applied Edit 2; this review has no edits to propose. Three clean windows in a row is genuine signal the loop is self-stable. Each tick costs ~5 min wall-clock + one agent context.
  - **Against extending:** This window had 5/10 correction iterations and 1 subsystem-boundary recommendation (q-061's e09b proposal). If reviews fired at ≥15, the e09b recommendation and the q-052 phantom-import correction would have surfaced ~5 iterations later, which is mildly bad for Phase 2 planning. The bug-surface rate (1 in 32) is now low enough that "lose 5 iterations of visibility" is the dominant downside.
  - **Verdict:** Hold at ≥10 for window #13. If #13 also has no edits + frontmatter variance stays at ≤1/10 + correction rate stays in the 20-50% band, extend to ≥12 starting at window #14. Don't jump to ≥15 — that would be a 50% extension on three clean windows of evidence, which is overfit. Incremental.

## Proposed LOOP_PROMPT.md edits

**None.** All three patterns observed (frontmatter cleanup landed, correction rate is signal not noise, q-043's bare-string `files_consulted` is 1/52 and not worth tightening over) point to no brief change needed this tick. The Sub-A retrospective edit from review #11 (Edit 1) remains the only outstanding proposal and is correctly held back per the "DO NOT modify open-questions.json mid-Sub-B" constraint.

## Feedback items to mark wont-fix

(none — `protocol-feedback.json` still shows all four entries in terminal state: f-001/f-002 `wont-fix`, f-003/f-004 `addressed`. Zero new entries opened this window. Five consecutive windows without protocol feedback.)

## Skip list — feedback that should remain open

(none open.)

## Notes for human

- **Followup-overrules-survey is now a documented contract.** With this window's evidence (q-019 + q-040 + q-052 + q-053 + q-056 + q-060 + q-061 = 7 correction cases across the Sub-B run), Phase 2 module-doc writers MUST consult `_followups/q-NNN.md` for any subsystem where a followup exists. Specifically:
  - **e06-adaptive-resolution**: q-040 corrects field names; q-043 confirms ref-based pattern; q-045 documents `HardwareProfile`; q-046 reclassifies CompositionOverlay as shared.
  - **e07-plugins-host**: q-052 corrects phantom import; q-053 reframes singleton concern (state-leak not DOM-leak); q-054 documents three-layer loader pipeline.
  - **e08-shortcuts-undo**: q-055 confirms cross-file references; q-056 reveals inverted comment in `app-gmt/main.tsx:408-414`; q-060 dead-defensiveness cleanup.
  - **e09-camera-plugin**: q-061 recommends spinning **new audit slice e09b-state-library-ui** for three generic primitives.

- **q-061's e09b subsystem proposal is a Phase-1-completion task.** The three UI primitives (`StateLibraryPanel`, `StateLibraryToast`, `ActiveSnapshotFeatures`) plus `createStateLibrarySlice` are genuinely a separate audit unit from the camera plugin. Two options:
  - (a) Append `e09b-state-library-ui` to subsystems.json with the four files as `files_claimed`, status `pending`, priority `primary` — the loop will pick it up automatically once Sub-B finishes (or whenever any pending subsystem survives into Phase 2 transition).
  - (b) Roll it into Phase 2 as a "split e09 into e09a + e09b" task at module-doc-writing time.
  Option (a) is cleaner (keeps the survey corpus complete); option (b) is faster (avoids a fresh Sub-A+B mini-cycle on 4 files). Recommend (a) — the audit cost is ~10 min wall-clock and the slice would otherwise leak into Phase 2 as a documented-but-unaudited subsystem.

- **q-054 is the single highest-value followup in the entire Sub-B run so far.** The three-layer loader pipeline (`loadSceneFile → loadScene → loadPreset`) is undocumented anywhere except an inline comment in SceneIO.tsx:298-305 that plugin authors are unlikely to discover. The Phase 2 docs/engine/04_Core_Plugins.md SceneIO section must absorb the full pipeline + the GMT parser-side-effect contract + the post-boot fallback-sphere failure mode. If only one followup gets a verbatim quote in a module doc, this is the one.

- **Sub-B progress: 42 of 76 primary answered (~55%), 78 pending overall** (1 duplicate + 34 primary + 43 low remaining). Last window I projected ~5.4 hours remaining; recomputing at the steady ~3.5 min/iter (per progress.json timestamps, q-043 03:00:06 → q-061 03:37:07 = 37 min for 10 iters = 3.7 min/iter, same as last two windows): **34 primary × 3.7 min ≈ 2.1 hours**, then **43 low × 3.7 min ≈ 2.6 hours**. Total ≈ **4.7 hours remaining**. Comfortably under the 200-iteration cap (120 + 0 still resolved).

- **Watchlist for the next review (#13, fires after ~q-072):**
  - (a) Bug-surface rate: if #13 also has zero new bugs, the running 1-in-32 rate stabilises and the bug-surface argument for tight cadence weakens to "useful but not critical". If #13 has 1+, the rate stays at 1-in-25 and tight cadence has more justification.
  - (b) Frontmatter variance: holding at 0/10 means Edit 2 from review #11 is stable. Reverting to 2+/10 means the brief tightening was a transient effect and orchestrator-side validation would be the next lever.
  - (c) Correction rate: this window's 50% was the highest yet. If #13 stays in the 30-50% band, that's a stable feature of Sub-B and Phase 2 writers can plan around it. If it drops to 10-20%, the easy-to-spot survey errors are exhausted and the remaining followups are more confirmatory.
  - (d) Cadence decision point: if #13 conditions match (no edits + variance ≤1/10 + correction rate 20-50%), extend cadence to ≥12 for windows #14 onwards. Don't extend on weaker evidence.
  - (e) q-043 bare-string `files_consulted`: if #13 produces another bare-string variant, the brief may need an explicit example showing the nested `- path: / blob_sha:` form for `files_consulted` (mirroring what review #7's positive-frontmatter-example block did for summarize-existing). One occurrence in 52 isn't enough; two in 62 would be.

PASS.
