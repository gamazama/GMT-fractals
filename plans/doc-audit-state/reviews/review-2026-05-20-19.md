---
review_id: review-2026-05-20-19
generated_at: 2026-05-20T10:01:59Z
feedback_items_reviewed: []
---

## Patterns observed

- **Fifth consecutive clean tick on the protocol-feedback channel.** `protocol-feedback.json` has zero `status: "open"` entries; the last new f-NNN (f-006) was raised pre-review-#17 and closed wont-fix in #17. Across the ten iterations covered by this window (q-080..q-105 sampled, plus q-107, q-110, q-111, q-112, q-114, q-115, q-117, q-119 in `_followups/`) no agent has emitted a fresh protocol complaint outside its PASS/FAIL report. Combined with #15/#16/#17/#18 this is now the longest quiet stretch of the run and the protocol is in genuine steady state, not just an inter-tick lull.

- **Investigations are still producing actionable Phase 2 signal — quality has not decayed.** All ten spot-checked followups carry file:line citations on every claim, substantive `Related findings` sections, and explicit confidence reasoning. q-105 is the standout: it surfaces a real coverage gap — `importSource` lifecycle / FormulaWorkshop has no owning subsystem despite g05 disclaiming it twice (`g05-engine-gmt-features.md:179,216`) — and proposes a concrete `g11-formula-workshop` or `f01-fragmentarium-importer` slice. q-103 elevates an existing "partial coverage" entry on `data/nodes/definitions.ts` (lines 38-167 of 405 read) from invisible to trackable. q-091 + q-080 + q-087 produce three coherent relocation/keep-as-is verdicts on engine-vs-app boundary files. q-090 confirms with git-log evidence that `components/topbar/` is an empty extraction-leftover safe to delete. Bug-surface rate this window: zero new production bugs (cumulative still 3 — q-019/q-064/q-112).

- **No-sibling-context-handoff rule (f-006 wont-fix) has held in practice.** Spot-check of briefs implied by followups in this window: q-082 references q-081 as a cross-reference in `Related findings` but the agent's own framing — "That changes the calculus from q-081… the `RenderDialog` precedent that justified q-081's generic doesn't apply here either" — is investigator-side reasoning, not orchestrator-paraphrased anchoring. q-103 similarly cites q-117 from its own file reads (`_followups/q-117.md` in `files_consulted`), which is the desirable pattern (file path passed/found, not paraphrase). No followup in the sample treats a prior investigation's conclusion as established fact handed down by the orchestrator. The orchestrator-side discipline established at #17 is observable in agent output.

- **Sub-B output shape is now dominated by orphan-sweep nominations and Phase-2 cleanup signal, exactly as #18 noted.** This window: q-090 (empty `components/topbar/` directory deletion), q-091 (LoadingRenderer pair relocation to app-gmt — confirms a `g01-renderer.md:224` orphan), q-080 (ViewportRefs rename to `engine/viewport/`), q-087 (MobileControls stays where it is — a "no-move" verdict that closes an open question rather than mining a new orphan), q-104 (sound deferral verdict — `Preset.features` ownership genuinely belongs to g05; no new home needed). Orphan-sweep prefix discipline remains 100% canonical where used; no variant slippage observed. The cumulative orphan-sweep backlog called out in #18 (~40 candidates) continues to grow at the same cadence.

- **Frontmatter shape is at five-windows-clean. Field-name canonicalisation is fully stuck.** All ten sampled followups use `question_id` / `source_subsystem` / `investigated_at` / `files_consulted` with block-YAML `files_consulted` entries (path + blob_sha). Zero resurgence of `id:` / `parent:` / `status:` / `verdict:` / `files_read:`. The midnight-placeholder fix from #16 is also still effective — every sampled `investigated_at` is a realistic UTC timestamp tracking actual investigation flow (range 09:23:42Z–09:58:51Z across the window).

## Proposed LOOP_PROMPT.md edits

No proposed edits this tick — fifth consecutive clean window. Zero open feedback items, frontmatter at 0/N variance across five windows, midnight-placeholder fix holding, orphan-sweep prefix at 100% canonical, no-sibling-context-handoff rule observably enforced in agent output, no new production bugs this window, no novel failure modes. The protocol has stopped surfacing actionable drift; further intervention would risk over-fitting to the homogeneous Sub-B low-priority sweep that is about to end (7 low-pending → near zero in coming iterations). The right move is to let the remaining iterations land and reserve protocol attention for the Phase-1.5 → Phase-2 transition.

Observation for the human reviewer (no edit): q-105's finding that the Workshop / fragmentarium-importer subsystem has no owning slice — and that g05 explicitly disclaims it — promotes the post-Phase-1 orphan-sweep planner item (noted since review #3, reinforced in #18) from a cleanup-only activity into a coverage-gap-fix activity. Recommend the post-Sub-B planner pass create `g11-formula-workshop` (or equivalent) as a real new subsystem before Phase 2 module-doc writing begins, otherwise `importSource` lifecycle / V3/V4 importer pipeline / FormulaWorkshop UI will be undocumented by construction. Same shape as the g11-gmt-camera-manager gap surfaced by q-062. This is a planner decision, not a protocol edit.

## Feedback items to mark wont-fix

None — no open items.

## Skip list — feedback that should remain open

None — no open items.
