# Triage — 2026-08-02

Verdicts for the 46 severity-tagged sections of `PROPOSALS.md` that survived the
staleness pass — **11 do now, 30 do later, 5 won't do** — plus five items that are
not among those 56 but need a verdict all the same: `B1` and `B2` from the guard
sweep, the missing palette rule, the leftover cosmetic half of the closed
reflections finding, and the unguarded bucket path the triage prompt named.

Grouped by verdict, not by severity. Line numbers are into `PROPOSALS.md`; entries
marked `*` are the five extras.

**A "do now" here means: small, well-specified, and either already guarded or
provably behaviour-neutral.** Where a guard already covers the change it is named.
Where nothing covers it, that is said — because per the run's own central finding,
"a guard is green" is only worth something if the guard can go red.

---

## Do now — 11 open sections, plus 2 extras

> **CLOSED 2026-09-01 — `d5ed60dd`, `e33a4fa7`, `e184a661`:** all 13 done.
> Each was re-verified as still true against the tree on the day rather than
> taken from this list: every one was. Three notes for the record.
>
> The Modal flip was checked by walking every `<Modal>` opening tag with depth
> tracking rather than by grep — a grep says 2 sites rely on the default, the
> real answer is 3, because `NewSceneModal` has two Modals and it is the second
> one. After the change: 12 explicit, 0 relying on the default, so the flip
> provably changes nothing.
>
> The gradient-explorer comment had drifted from :314 to :418 in the month since
> this list was written — the exact rot the "cite grep targets, not line numbers"
> rule exists for.
>
> The palette rule dropped one thing all three auditors proposed: "cite specific
> harness links because `check:rule-guards` resolves a chained npm script by its
> first command only". True when they wrote it, fixed later in the same audit
> (2026-07-29 — direct-file composites now expand to the union, and the fix names
> `test:palette`). Writing it in would have planted a fresh false claim inside a
> rule about false claims.

Eleven are one-liners or near it. Two are small enumerated changes with every call
site already listed in the finding.

| # | Item | Why now |
|---|---|---|
| L1938 | **Loading a scene force-selects camera 1** → `activeCameraId: null` at `utils/defaultPresetFields.ts:123` | One word. Removes a spurious `*Camera 1` modified marker on every scene load and changes no pose. Recommendation (b), unchanged. |
| L2040* | **Delete the dead reflections manifest item** — `engine-gmt/panels.ts:356`, `groupFilter: 'shading'` | The only thing left after the owner review. It renders a group description with zero widgets. One-line deletion. |
| L824 | **Delete the "GLSL Debugger" `menuItems` entry** — `engine/features/debug_tools/index.ts:23` | Recommendation (1): drop the entry, keep the param so the six formula presets carrying `shaderDebuggerOpen: false` still round-trip. The component was removed at extraction; the menu item was not. |
| L1190 | **Soften gradient-explorer's ADR-0039 comment** — `gradient-explorer/GradientExplorerApp.tsx:314` | Still says "matches app-gmt"; GE mounts only the sticky shell, not `MobileScrollIntro`. Recommendation (b), one line, no behaviour risk. |
| L2480 | **Delete the stale flag list from `debug/helpers/webglHarness.ts:76-77`** | The header lists `--disable-gpu` / `--use-gl=swiftshader`; `chromium.launch()` passes neither, and the comment directly beneath says the opposite. Recommendation (a) only — do **not** add `noWaitAfter` to `dragPath`, which is (b) and changes every dragging smoke. |
| L2706 | **One-line note in both img2grad harness headers** saying the overshoot sweep is not a subset of the main one | Removing `if (i > n - 2) i = n - 2;` reds only the overshoot sweep; the main harness stays green. They sit adjacent and are named as though one subsumes the other, so a consolidation pass silently reopens a real `TypeError`. |
| L2726 | **Add a palette entry to `.claude/rules/mobile-layout.md`'s `paths:`** | It names `palette/store/favientsPanelPersist.ts` in prose but the frontmatter has no palette glob, so the rule never loads when you open that file. Confirmed still true. |
| L2735 | **Drop the two dead palette rows from `plans/context-protocol/subsystems.json`** | `palette/components/FavStar.tsx` and `QualityRangePad.tsx` — both confirmed absent from disk today, both costed 0 tokens, and 0-token rows sort **first**, so they are the first thing an agent scoping palette sees. |
| L390 | **Fix `HANDOFF.md:12`** — it still says the driver throttles `runTicks` to 1 Hz and that this "collapsed analysis and lost whole seconds of transients" | `runTicks` is called on both sides of that branch; only the worker dispatch yields. The corrected mechanism is already in ADR-0110's Update block. Left for you deliberately because `HANDOFF.md` is your log — you are here now. |
| L286 | **Downgrade "feature isolation is enforced"** in `.claude/rules/ddfs.md:31` **and** `CLAUDE.md` | Nothing implements it — `dependsOn` produces a register-time warning and a topological sort, nothing more. Confirmed still worded as enforcement today. Both edits must land together or the two steering documents contradict each other. Recommendation (1). |
| L1095 | **Flip `Modal`'s `dismissOnBackdrop` default to `false`** — `components/ui/Modal.tsx:41` | Provably behaviour-neutral: all 13 call sites were enumerated, 9 already pass `false`, and the 3 that want backdrop-close are named (NewSceneModal, FormulaPicker, Lightbox). Makes the primitive agree with the rule that governs it instead of making every real dialog remember the opt-out. |
| L32 | **Drop the reset from `RenderPipeline.setSampleCap`** — `engine/RenderPipeline.ts:525` | Confirmed still present. Dragging Auto-Stop downward past the current count throws away a converged buffer once per step during the drag. `render()` already no-ops once `accumulationCount >= sampleCap`, so the image simply freezes. Resolves the contradiction with `installAccumulationBindings.ts` for free, and no guard depends on the current behaviour. This is the one "do now" that is a genuine product call — but it is a small one and you can see it in ten seconds. |
| L2746* | **Write `.claude/rules/palette.md`** | The only subsystem-sized area of the tree with no rule. Three auditors drafted it and none wrote it (correctly — they would have collided); the merged material is sitting in the three `results/p01*.json` `notes` fields. It also carries the L2717 item for free by citing specific links rather than the `test:palette` chain. |

---

## Do later — 30 open sections, plus 3 extras

> **CLOSED 2026-09-02 — 15 of these, in one session on `main` (commits
> `a468ad6e`…`16445855`):**
>
> | # | Verdict taken | Commit |
> |---|---|---|
> | L1765 | The five hoists + `test:modular-parity` (17 red before the fix). Saved Modular scenes using those nodes render differently now, by decision. | `a468ad6e` |
> | L1855 | (c) then (b): Auto checkbox removed; `structureKey(nodes, edges)` replaces the edge-blind `isStructureEqual`; COMPILE pulses only when stale; scene load records the compiled key. | `a4cad336` |
> | L1816 | ErrorBoundary half only: root `AppErrorBoundary` in all five app entries, `smoke:boot` fails on a caught error, the pre-mount boot-preset load is wrapped. The Supabase query is still yours. | `abd6c7a3` |
> | L1311 | V4 branches above the V3 guard, `r.def.id` not the raw name, name field reachable on the V4 path. Manual check still owed — no guard mounts the Workshop. | `ac50fbf8` |
> | L769 | Guard first (`test:worklet-analysis`, 33 assertions, three pre-fix variants measured), then the count-based drain. | `5fbf1a2b` |
> | L324 | (b): try/catch per tick, one report per tick name, `test:tick-registry`. | `2c80ecfa` |
> | L739 | Blank cancels; trim decided yes. | `212c11ae` |
> | L555 | Stash the root, detach from it, reset the counter, clear subscribers; warn on a second install with options; `test:shortcuts-teardown`. | `6aed1bf8` |
> | L855 | (b)+(c): `resolveSlotCount` for both, menu skips its slot section at 0, title uses the configured modifier. | `ad04d3a4` |
> | L2401 | Blind spot 1 closed: per-row scoping. It caught a negative mention written as a citation on the first run. | `1fc7166d` |
> | L2503 | (b): globs expanded, zero-cost Layer 2 exits 1. | `46dc8b36` |
> | L1380 | (c): no-progress watchdog on a worker heartbeat; `smoke:export-watchdog`. | `5f0fb03b` |
> | L260 | Detector + memoisation + `test:share-dictionary` (full-slice round-trip). | `16445855` |
> | L2811 | (2): `proxy.lastCompileFailed` fed by the ERROR→COMPILE_FAILED bridge, cleared when a cycle ends clean; `CompileProgressStore` gains a sticky `failed` phase and the CompilingIndicator shows a dismissable red pill with the first GLSL error line; `smoke:compile-failed` falsified both signals independently. The latch's timing is untouched; (1) the rename is not done. | see `git log -1 -- debug/smoke-compile-failed.mts` |
> | L3063 | (b): `debug/run-smokes.mts` runs every member and ends with "N of M failed"; `--only`, `--from`, `--bail` (the old stop-on-first-red), `--list`. `smoke:all` now points at it; `check:rule-guards` expands its member list. Falsified with a missing first member: 1 of 3 failed, the other two still ran. | see `git log -1 -- debug/run-smokes.mts` |
> | L2125 | (4)+(3)+(2): `test:frag:catalog-drift` gates HARD staleness only (catalog pass, errors today) and reports soft drift; the two hard rows (Knot, KIFS — v4) were demoted in place with a `corrections` array in the catalog, so the guard is green on arrival and red when either is flipped back; the Workshop's browse pickers now GREY `recommended: 'none'` rows with a tooltip instead of hiding them, and "show broken" enables them. Visual pass owed. | see `git log -1 -- debug/check-catalog-drift.mts` |
> | L2172 | (1)+(3): renamed `report:frag:scan`; the script prints a REPORT ONLY banner at start; the rule's prose mention no longer carries an `npm run` prefix. | see `git log -1 -- debug/scan-frag-parse.mts` |
> | L1128 | (a): the probe promoted to `smoke:mobile-layout` with 14 assertions over a Pixel 5 and a desktop context; falsified through the uiSlice boot seed (768→2000) and through the resize listener; cited from the mobile rule; in the smoke:all list. | see `git log -1 -- debug/smoke-mobile-layout.mts` |
> | L2633 | (a): `test:palette-gradientseam` pins linear/srgb forcing, layer routing, pass-through stops and the 128-stop cap against the real store with stubbed setters; falsified by forcing srgb (3 red) and by capping at 8 (1 red). Last link of `test:palette` and cited directly from the palette rule. | see `git log -1 -- debug/test-palette-gradientseam.mts` |
> | L2658 | `test:palette-favients` runs the real Favients store over a localStorage shim: add/reload, signature dedupe, the load gate, import admission and disk, `__proto__` labels on a populated group, undo write-through. Falsified by opening the gate (the run dies on a null entry, the shelf-bricking case) and by emptying UNSAFE_KEYS (prototype pollution red). `test-palette-editorstore.mts` renamed `test-palette-editorconfig.mts`. | see `git log -1 -- debug/test-palette-favients.mts` |
> | housekeeping | `debug/render-harness.ts` was NOT an orphan — `render-harness.html` loads it by script tag, which knip cannot see; registered as an entry instead of deleted. `debug/engine-gmt-smoke.png` ignored. HANDOFF header corrected. | `a4cad336`, `be12e005` |
>
> Also from the same session, outside this list: the 30fps-while-focused `@bug`
> moved from the hover pre-pick to `connectSystemAudio` (owner attribution,
> `5367ec74`). Still open here: B1/B2, one of the three palette coverage rows (L2604), the
> bucket runtime guard, L921, L511, L73, L2125, L2090, L2172,
> L1128, L3063, L3016, L2670, L2581, L355, L3098, and both sweeps.

Real value, but each wants a session: a design decision, a visual pass, or a new
guard that has to be falsified before it is worth anything.

### Coverage holes — the three the triage prompt called out, plus their siblings

| # | Item | What it needs |
|---|---|---|
| B1* | **Nothing fails when the Fragmentarium importer emits shaders that cannot compile** | Confirmed still open: `test-frag-integration.mts` classifies GLSL issues `warn` and exits on `failed > 0`. **Decide RecFold's `OrbitStrength` gap first** — that is the whole difficulty. Recommendation (4) if it is a real importer gap worth closing (one formula, and it makes the gate honest with no allowlist to rot); otherwise (3) with the single baseline entry named and dated in source. Gate it wrong and you get a permanently-red guard, which is how `test:frag:integration` got into trouble in the first place. |
| B2* | **`test:frag`'s GLSL channel is advisory** | Recommendation (2): gate only on issues whose string starts `GLSL parse error:`. Baseline 0, objective, no allowlist. Cheap belt-and-braces **after** B1 — the integration suite's full-shader strict parse is strictly stronger. Doing B2 first buys the least. |
| L2604 | **The palette component layer — 6,257 lines — has zero automated coverage** | Confirmed: `grep -rln 'PickerWall\|FavientsPanel\|ChannelGraphEditor' debug/ .claude/rules/` is still empty, and breaking every gradient strip still leaves every guard green. Partial progress exists — `PickerWall` imports `shouldSquare`/`squareCols` from `palette/core/wallLayout.ts`, which **is** guarded — but the column-major index math, chunk split and carve commit are still inline, as is `FavientsPanel`'s drop-index. Recommendation (a) then (b): finish the extraction into the modules that already have harnesses. **Do (d) — record the gap in the palette rule — regardless**, and it is free if you write the rule now. |
| L2633 | **Eight palette core files unreachable from any guard, worst is `gradientSeam.ts`** | The doc's own "single conversion point" to GMT, encoding real contracts (presets pass `colorSpace: 'linear'`, `applyEnvGradient` forces `'srgb'`, `SEAM_MAX_STOPS` 128). A swapped colour space miscolours every applied gradient invisibly. Recommendation (a): a new `debug/test-palette-gradientseam.mts` against a stubbed store — falsified on a quiet tree. |
| L2658 | **23 of 24 palette store files have no node-side guard** | Start with `favientsStore` — the only one that ingests untrusted input *and* persists it, so a malformed favourite survives reloads. Also rename `debug/test-palette-editorstore.mts`, which tests `editorConfig` and no store. |
| —* | **`engine-gmt`'s bucket accumulation path has no runtime guard at all** (from the triage prompt, not a numbered section) | Not in `PROPOSALS.md` as its own item; it sits in the cycle-8 mesh/bucket housekeeping. Worth its own entry when someone opens that area. |

### Real defects with a product decision attached

| # | Item | The decision |
|---|---|---|
| L1765 | **Adding a Scale, Twist, Bend, Smooth Union or Mix node corrupts every node after it** | Confirmed still present — all five double-read their param in `engine-gmt/data/nodes/definitions.ts` (`:35-36`, `:282-283`, `:300-301`, `:375-376`, `:391-393`). The fix is five one-line hoists matching what `IFSScale` already does, and needs no migration — **but it changes the rendered result of any saved Modular scene using those nodes.** Do the ~15-line node-only parity harness in the same change; it locks this shut permanently and needs no browser. Highest-severity open defect in the file. |
| L1311 | **The Formula Workshop's V4 escape hatch is dead** | Confirmed: the V3-only guard at `FormulaWorkshop.tsx:721` still sits above the `useV4Pipeline` branch at `:725`. Not a hoist — move the V4 branch up, move `setError(null)` with it, use `r.def.id` not the unsanitised `formulaName` for `setFormula`, and decide whether to un-gate the name field. Bounded at 3 of 511 library entries, so not urgent; no guard mounts the Workshop, so it needs a manual pass. |
| L1855 | **The modular graph never auto-compiles, and clicking Auto throws** | Confirmed: `setAutoCompile` is still called from `FlowEditor.tsx:569` and still not implemented. Recommendation (c) first — remove the control that throws — then (b), the edge fingerprint in `isStructureEqual`. Loud and recoverable, so lower than the slot bug above. |
| L1816 | **Pre-2026-04-25 scene blast radius** (the crash itself is fixed) | Two things only you can answer. (1) Query Supabase for stored shares / gallery scenes predating 2026-04-25 — if any exist, users hitting those links have been getting a white screen. (2) **There is still no ErrorBoundary anywhere in the codebase** — re-confirmed today. A root-level one turns "app dead" into "panel broken" for every future instance of this class. Note the catch recorded in cycle 10: `loadScene({ preset: bootPreset })` runs *above* the `<AppGmt />` render, so a wrapper does not cover the pre-mount throw. |
| L921 | **The five worker-stub captures** | Half-answered (`d525c72c`): the undo consequence is masked. `loadScene`'s post-boot branch was **not** re-measured and is the half that may be user-visible. Recommendation (1), the mechanical sweep — but pair it with a decision on that branch, because making the capture live re-activates it, which is a behaviour change wanting a visual check. |
| L260 | **`getDictionary()` collision detection + a share round-trip guard** | **This is now unblocked.** It was demoted because two collisions existed and any detector would fire immediately — those landed in `8f59b143`, so a `console.warn` detector (or an assertion of zero duplicates) is green on arrival today. The guard is the valuable half: nothing asserts share round-trip fidelity beyond the two feature slices `smoke:share-link` samples. Memoise `getDictionary` or gate on `import.meta.env.DEV` — it runs on every share encode/decode. |
| L769 | **`takeMaxFlux` drops a full ring of onsets at an exact wrap** | The aliasing is real and measured (512 unread hops at flux 30 → max `fluxRate` 0). Narrow reachability. **The bigger gap is that `WorkletAnalysis.ts` has no guard coverage at all** — write that first, feeding synthetic `AnalysisBatchMessage` payloads into `onBatch`, then fix. Do not delete the dead line without keeping the `@bug` note. |
| L1380 | **`renderExportFrame` has no timeout** | Recommendation (c), a no-progress watchdog rather than a fixed ceiling — it is the only option that cannot abort a legitimate multi-minute 4K path-traced frame. |
| L511 | **`holdAdaptive` is inert and its seed bypasses the hold** | No recommendation was offered on purpose and that still stands: it is a product judgement about what "hold" should mean, with no usage to infer intent from. Both call sites are documented either way, so nothing is silently wrong while you decide. |
| L2811 | **`hasCompiledShader` means "a compile was issued"** | Confirmed still set at `CompileScheduler.ts:253` before the link result. Recommendation (2): add `lastCompileFailed` fed by the `COMPILE_FAILED` event `WorkerProxy` already re-emits — additive, does not touch the latch's concurrency role. Today a failed compile in the wild renders empty frames indefinitely with no in-app signal. |
| L73 | **Graph-editor curve tools write wrong tangents on log tracks** | Five-file signature change across UI with thin guard coverage, and **fixing half of it leaves the Pencil worse** — the Douglas-Peucker epsilon is in linear value units and collapses a 101-sample stroke to 3 keyframes on a 1→1e-30 track. Do both halves in order, when you can eyeball the graph editor. Confirmed unchanged: `reTangentBezier` still takes no `isLog`. |

### Guard and tooling work

| # | Item | Verdict |
|---|---|---|
| L2125 | **Fragmentarium catalog drifted 17%** | Recommendation (4) — a `test:frag:catalog-drift` guard failing when the hard-stale count exceeds 0 — **plus (3)**, greying out `recommended: 'none'` rows instead of hiding them. Hiding 21 shipped formulas on three-month-old data is the actual user harm. A reusable probe is at `debug/_g12-catalog-drift.mts`. |
| L2090 | **18 real importer parser failures** | Not a regression — all 18 were red before cycle 10's narrowing. It is a genuine parser gap: unsupported `samplerCube`/texture uniform syntax, two null-derefs, and a Knighty/Kashaders/neozhaoliang cluster. `test:frag:integration:discover` exists and nothing runs it. Pairs naturally with B1. |
| L2172 | **`test:frag:scan` is a report wearing a test's name** | Confirmed: still `"test:frag:scan"` in `package.json`, still no assertion. Recommendation (1) rename to `report:frag:scan` plus (3) a banner. Deferred only because a cross-file rename touches every doc and result file naming it. |
| L2401 | **`check:rule-guards` blind spot 1** | Blind spot 2 is closed. Blind spot 1 is not: `parseRule` still flattens every `npm run X` in a rule file into one list matched against the rule's whole `paths:` set, so in a multi-app rule any guard satisfies every row. Recommendation (b): parse guards per table row. This is the checker that certifies every other guard citation, so it is worth more than its size. |
| L1128 | **Nothing guards mobile layout** | The probe at `debug/_mobile-layout-probe.mts` already demonstrated it can discriminate. Recommendation (a): promote it — rename, add assertions (it logs rather than throws), wire the npm script, cite it in the rule. Confirmed still absent from `package.json`. |
| L3063 | **`smoke:all`'s `&&` chain hides everything after the first red** | Recommendation (b): a `debug/run-smokes.mts` driver that runs every member and exits with an "N of 42 failed" summary. This batch's own finding is the argument — 14 of 42 members never ran, undetected long enough that one pointed into a directory deleted six weeks earlier. (c) is the cheaper half-measure. |
| L3016 | **`test:hybrid` / `test:hybrid-adv` are byte-identical to `test:baseline`** | Recommendation (a) is deletion, which is yours. The measurement is already recorded in the sweep header and `gmt-renderer.md`, so nothing rests on a false premise — the cost is only that the names promise coverage that no longer exists. Low urgency, ~7s of a 14min chain. |
| L2503 | **`context:cost` reports both toy subsystems at 0 tokens** | Recommendation (b): make `context-cost.mjs` **fail loudly** on a zero-cost Layer 2 rather than printing it as a result. A zero-token "source of truth" layer is indistinguishable from a correct answer — the same failure class as a guard that cannot fail. Also catches the two ghost palette rows in the do-now list. |
| L2670 | **`.ugr` collection export exempt from the lossy warning** | The first step is a **measurement, not a change**: does 64 nodes stay under `AI_LOSSY_DELTA`? Then either justify the exemption with a number or thread the per-format budget into `aiLossyGradients`. Already recorded as an `@assumption` at the call site. |
| L2581 | **OKLab primitives are hand copies with no working pin** | Recommendation (a): export the four primitives from `utils/colorUtils.ts` and re-export here, then remove the tautological pin honestly. Deferred because it widens engine-core's public API, which is an architecture call — but the precedent exists (`gmtGradient.ts` was collapsed to a pure re-export for the same reason). |

### Small, latent, no urgency

| # | Item | Verdict |
|---|---|---|
| L324 | **One throwing tick can freeze the whole renderer** | Recommendation (b): try/catch per tick, warning once per tick *name*. Converts a silent whole-renderer freeze into a named console error. 4–8 entries, so the cost is nil. Design fork, not a defect — which is why it waited. |
| L355 | **The double-run guard would kill a frame-stepping harness** | Latent — three `runTicks` call sites, none hits it. Worth doing *when* someone writes the 4K/offline frame-sequence exporter, because that is exactly the shape that trips it. Recommendation (b), an explicit opt-out. |
| L555 | **`uninstallShortcuts` is not the inverse of `installShortcuts`** | Decisively latent: zero callers, no barrel re-export, no `import.meta.hot` anywhere. ~6 lines when someone is in the file. The verifier's adjacent suggestion is better value: warn when a second `installShortcuts` silently drops its options. |
| L739 | **The input-side half of the blank-rename fix** | The robust half already shipped. What is left is `handleRenameSubmit` accepting blank — confirmed unchanged at `StateLibraryPanel.tsx:159`. Trivial, but **decide the trim question explicitly** (`" My Cam "` → `"My Cam"` is a behaviour change beyond "treat blank as cancel"). |
| L855 | **`installStateLibrary` reads its slot count two ways** | Confirmed: `cfg.count ?? 9` at `:137`, `(typeof … === 'object' && …) || 9` at `:236`. Latent — both current consumers agree on 9. A trap for the third library. Read ADR-0031 first. |
| L3098 | **Three deliberate exit-0 paths** | All three are intentional; the question is whether they stay. The `smoke:gallery-link` skip is the one worth a decision — as a member of `smoke:all` its `⊘` is one line in an 8-minute log. An opt-in `REQUIRE_GALLERY=1` costs nothing. |

---

## Won't do — 5 items

Not "ignore" — these are decided, and the decision is to leave them. They stay in
the file as the record of why.

| # | Item | Why not |
|---|---|---|
| L1162 | **Do NOT mechanically collapse the eight breakpoint copies** | This was never a task — it is a warning written so a future cleanup pass does not do it blindly. `FormulaPicker.tsx:832` tests a *locally measured* width, not `window.innerWidth`, and swapping it changes which width drives the popover-vs-sheet branch. It has done its job by existing. (If anyone does want the win: the `uiSlice` initializer is the only one of the eight producing observable boot-state divergence — but get the mobile guard in place first.) |
| L1401 | **The pre-boot outbox** | The finding's own recommendation is (1) leave it, and the JSDoc has already been corrected to say the outbox is empty at both creation sites. Option 2 changes worker message ordering — the highest-risk surface in that subsystem — and no boot-ordering guard would catch a mistake. Revisit only if a feature actually needs to post before `initWorkerMode`. |
| L2685 | **The Gradient-Explorer-only store living in `palette/`** | Recommendation is explicitly "leave it". Fourteen harnesses reach it by hardcoded URL string, so neither `tsc` nor `knip` can see the edge. The `@invariant` added in `bf981def` names the five exports and its guard. If it is ever moved, move it **and** sweep all 14 harnesses in one commit. |
| L2696 | **The unenforced ordering directive in `palette/installFavients.ts`** | Falsified negatively — moving `mountFavientsPanel()` above `applyPanelManifest()` leaves `smoke:boot` green with zero errors, because the store actions are optional-chained. The cost of getting it wrong is a remembered panel position, and it is recorded as an `@assumption` (`55661556`). Enforcing it is a behaviour change out of proportion to the harm. |
| L2717 | **`test:palette`'s twelfth link touches no palette file** | Not a defect in anything — `test-engine-dnd-kernels.mts` is a fine guard, just not a palette one. The fix is "cite specific links rather than the chain", which the palette rule in the do-now list does. Folded there; nothing separate to do. |

---

## Two sweeps deliberately not started

Both are listed at the bottom of `PROTOCOL.md` and neither is urgent.

- **`@invariant` → `@assumption` across ~152 files.** The highest-value remaining
  pass, and the reason `CLAUDE.md` now demands an invariant name its guard.
- **NUL-byte / unsearchable-file scan.** Three instances found so far, all
  incidentally — one made `components/CategoryPickerMenu.tsx` invisible to grep for
  an entire audit run (fixed, `b3f271a5`), another hid a palette guard
  (`37345901`). The class is silent blindness and the check is cheap.
  > **DONE 2026-09-02:** scanned every tracked text file (2821): 0 NUL, 0 stray
  > control bytes. Now a standing check, `npm run check:text-bytes`, listed in
  > CLAUDE.md's Automated Checks.

## Housekeeping still true today

- A **Vite dev server is running on :3400** right now. Kill it when you want a
  genuinely quiet tree.
- `debug/render-harness.ts` is still the only file `npm run orphans` reports
  tree-wide. Deletion is yours.
- **~115 gitignored `debug/_*.mts` scratch probes** are on disk. None can be
  committed (the `debug/_*` rule), and the run was forbidden from deleting any.
  Several are worth keeping — `_mobile-layout-probe.mts`, `_g12-catalog-drift.mts`,
  `_proxy-probe.mts` — the rest are bin fodder.
- `debug/engine-gmt-smoke.png` is untracked and regenerated by `smoke:engine-gmt`.
  Worth a `.gitignore` line alongside the other smoke artefacts.
